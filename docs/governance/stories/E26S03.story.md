<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E26S03.story.md at 339b1b9e217ccc3d36bc8571754e9feec9bd4b30 2026-04-28 -->
---
type: artefact
artefact_type: story
id: E26S03
related_backlog_id: E26S03
track: discovery
created_at: 2026-04-27
updated_at: 2026-04-27
mandatory_ac_categories: [error-handling, testing, governance, security]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-27-e26-timer-resumption
related_decs: [DEC-13, DEC-15, DEC-21, DEC-22, DEC-31, DEC-32, DEC-36, DEC-38, DEC-40, DEC-41, DEC-44, DEC-45]
delivery_mode: source
---

# Story: Timer web Q-1a TDD rebuild — `TimerController` + `TimerViewController` + `AudioController` at `de.vvwt.tm.web.timer.*` (NO web-tier DTO authoring per DEC-40 §2026-04-27 Pattern A; controllers consume bounded-context-owned records cross-module); `web/package-info.java` allowedDependencies += `"timer"`; `web/GlobalExceptionHandler` 5 import-line updates; `auth.internal.SecurityConfig` 3 URL-pattern updates; Svelte URL-string updates; fresh ITs under DEC-44 D1 framework

## User Story

**As a** Tournament Manager Delivery Agent operating under DEC-21/-22/-40/-44/-45 + Brief v1.1 governance,
**I want** to delete legacy `infrastructure.{timer,web.timer,web.audio}.*` controllers + tests (the 4 timer DTOs at `infrastructure/web/timer/dto/*` are deleted by E26S01 per Pattern A relocation; the legacy `AudioMetadataResponse.java` is deleted by E26S02 per Pattern A consolidation; S03 deletes ONLY the 3 controllers + 3 ITs + the now-empty `infrastructure/web/timer/*` and `infrastructure/web/audio/*` directories) and author fresh Q-1a TDD RED-first replacements at canonical FQNs `de.vvwt.tm.web.timer.*` (3 controllers) within ONE feature-branch lifecycle per D-7 Coexistence Option γ + apply Wave-2-aligned URL-Schema (per user choice 2026-04-27 Frage 1) + update `web/package-info.java` allowedDependencies + `web/GlobalExceptionHandler` 5 import-lines + `auth.internal.SecurityConfig` 3 URL-patterns + Svelte URL-strings,
**so that** the timer + audio REST + MVC primary-adapter surface joins `de.vvwt.tm.web.timer` Modulith sub-package (per DEC-40 Clause A + E36-photo+cert sub-package precedent); URLs Wave-2-aligned per Brief v1.1 D-10; Svelte SPA continues working unchanged (only URL-strings updated, no rebuild per user Frage 2); fresh ITs land under DEC-44 D1 + §"2026-04-27 Empirical Refinement" framework. **NO web-tier DTO authoring at `de.vvwt.tm.web.internal.dto.*`** per DEC-40 §2026-04-27 Clarification Pattern A — the 4 timer query-shape records live at `de.vvwt.tm.timer.*` (E26S01) and the 1 audio query-shape record at `de.vvwt.tm.timer.audio.*` (E26S02); controllers consume them cross-module via web's `"timer"` allowedDependency entry and serialize directly via Jackson without web-tier mapping.

---

## Context

E26S01 authored `timer.*` domain (TimerDataService + 2 exceptions + 1 enum + 4 query-shape records `Timer{Data,Phase,ScheduleEntry,Audio}Response` per DEC-40 §2026-04-27 Pattern A + package-info). E26S02 authored `timer.audio.*` sub-package (AudioStorageService + `AudioFileMetadata` Pattern A consolidated record + 4 other supporting types + 3 exceptions). E26S03 builds the web-tier on top: 3 controllers + multiple cross-cutting mechanical edits + fresh ITs. **NO REST DTOs authored at `de.vvwt.tm.web.internal.dto.*` per Pattern A** — the 5 query-shape records (4 timer + 1 audio) are bounded-context-owned and accessed cross-module via web's `"timer"` allowedDependency entry; controllers serialize them directly via Jackson without any web-tier mapping layer. This avoids the `timer↔web` Modulith cycle that broke E25S01 first-attempt delivery (outer-repo commit `23d947b`).

Per audit (iv): 5 production code-level consumers require updates (all in `web/GlobalExceptionHandler.java`):

- Line 11: `import de.vvwt.tm.domain.timer.InvalidTimerUrlException;` → `import de.vvwt.tm.timer.InvalidTimerUrlException;`
- (one line near line 11): `import de.vvwt.tm.domain.timer.NoActiveTournamentException;` → `import de.vvwt.tm.timer.NoActiveTournamentException;`
- (lines near 11): `import de.vvwt.tm.domain.audio.AudioFormatException;` → `import de.vvwt.tm.timer.audio.AudioFormatException;`
- (lines near 11): `import de.vvwt.tm.domain.audio.AudioStorageException;` → `import de.vvwt.tm.timer.audio.AudioStorageException;`
- (lines near 11): `import de.vvwt.tm.domain.audio.AudioSizeLimitException;` → `import de.vvwt.tm.timer.audio.AudioSizeLimitException;`

(Empirical line numbers verified at delivery via `grep -n` on the file.)

**TDD-provenance verified for §refactor-clause invocation** (Brief v1.1 D-12+D-13 + Cycle-1 reviewer F#4):

- `web/GlobalExceptionHandler.java` is **E36S08-Q-1a-reconstructed** (TDD-authored per E36S08 RED-first).
- `auth.internal.SecurityConfig.java` is **E15S07-Q-1a-reconstructed** (TDD-authored per E15S07 RED-first).
- Per `feedback_dec22_refactor_phase_first.md`: §refactor-clause is RED-GREEN-REFACTOR phase 3 on TDD-authored code; both targets pass the TDD-provenance check. Mechanical edits (import-lines, URL-patterns, URL-strings) qualify as phase-3 refactor; NO new RED-first test required for the mechanical edits themselves.

Per Brief v1.1 D-13 + D-14 + DEC-44 §"2026-04-27 Empirical Refinement": new web-module ITs use `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT, classes = de.vvwt.tm.TournamentManagerApplication.class) + @ActiveProfiles("test") + @Import({WebModuleTestConfig.class, <IT>.TestAdminCredentials.class})` where TestAdminCredentials inner class provides `@Bean("webItAdminCredentialsProvider") @Primary AdminCredentialsProvider`.

**Wave-2 URL-Schema (Brief v1.1 D-10 + audit (iv) URL-Schema table):** REST timer/audio + MVC timer paths migrate per the table; SecurityConfig permitAll list updates correspondingly; Svelte URL-strings update.

---

## Acceptance Criteria

### Pre-flight: legacy deletion

- [ ] **AC-DELETE-LEGACY-FIRST:** Single commit on the story branch deletes the following BEFORE any RED-first authoring:
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/timer/TimerViewController.java` (98 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/TimerController.java` (82 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/audio/AudioController.java` (verify LOC at delivery)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/timer/TimerViewControllerIT.java` (161 LOC)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/web/timer/TimerControllerIT.java` (384 LOC)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/web/audio/AudioControllerIT.java` (441 LOC)
  - The (now-empty) `infrastructure/timer/`, `infrastructure/web/timer/`, `infrastructure/web/audio/` directories. (`infrastructure/web/timer/dto/` is already empty after S01's Pattern A relocation deletions.)
  - **NOT deleted by S03 (deleted by predecessor stories per Pattern A):**
    - 4 legacy DTOs at `infrastructure/web/timer/dto/{TimerDataResponse,TimerPhaseResponse,TimerScheduleEntryResponse,TimerAudioResponse}.java` (517 LOC total) — deleted by **E26S01** (relocated to `de.vvwt.tm.timer.*` per Pattern A).
    - Legacy `infrastructure/web/audio/AudioMetadataResponse.java` — deleted by **E26S02** (1:1 wrapper consolidated into `de.vvwt.tm.timer.audio.AudioFileMetadata` per Pattern A).
- [ ] **AC-NO-LEGACY-CODE-REFERENCE:** Per Brief v1.1 C-17.

### Pattern A cross-module record consumption (per DEC-40 §2026-04-27 Clarification)

- [ ] **AC-NO-WEB-TIER-DTO-AUTHORING:** S03 authors **ZERO** Java records under `de.vvwt.tm.web.internal.dto.*` for timer/audio. The 5 query-shape records (4 timer at `de.vvwt.tm.timer.*` from E26S01 + 1 audio at `de.vvwt.tm.timer.audio.*` from E26S02) are bounded-context-owned per Pattern A. S03 controllers reference them via cross-module FQN imports through web's `"timer"` allowedDependency entry.
- [ ] **AC-PATTERN-A-CONSUMPTION-JUSTIFICATION:** Story impl-report cites DEC-40 §2026-04-27 Clarification Decision Rule §289-296 verbatim. **Per-record verification table** confirms each of the 5 records satisfies "projection == wire shape; no Clause B (a)/(c)/(d) condition fires" → Pattern A → bounded-context placement, NOT web-tier placement. Cross-references E25S01 escalation (outer-repo commit `23d947b`) as the architectural-lesson precedent.
- [ ] **AC-NO-LEGACY-AUDIO-METADATA-RESPONSE-RE-AUTHORING:** S03 does NOT author a replacement `AudioMetadataResponse` record anywhere (legacy 1:1 wrapper consolidated into `de.vvwt.tm.timer.audio.AudioFileMetadata` by E26S02 per Pattern A). `AudioController.list` returns `List<de.vvwt.tm.timer.audio.AudioFileMetadata>` directly to JSON; no factory-method mapping invoked.

### REST controller Q-1a TDD authoring (`de.vvwt.tm.web.timer.*`)

- [ ] **AC-RED-FIRST-TIMER-CONTROLLER:** New `de.vvwt.tm.web.timer.TimerController` `@RestController @RequestMapping("/api/timer/tournaments")` authored RED-first. Wave-2-aligned URL per Brief v1.1 D-10:
  - `@GetMapping("/{tournamentId}")` `getTimerData(@PathVariable("tournamentId") UUID tournamentId)` → `ResponseEntity<de.vvwt.tm.timer.TimerDataResponse>` (200 OK). Return type imported cross-module from `timer.*` per Pattern A; controller is thin Jackson-serialization adapter (delegates to service, wraps in `ResponseEntity`).
  - Constructor-injects `TimerDataService` (interface FQN `de.vvwt.tm.timer.TimerDataService` per DEC-36 cross-package).
- [ ] **AC-RED-FIRST-TIMER-VIEW-CONTROLLER:** New `de.vvwt.tm.web.timer.TimerViewController` `@RestController` authored RED-first. Wave-2-aligned URL per Brief v1.1 D-10:
  - `@GetMapping("/timer/tournaments/{tournamentId}")` `timerPage(@PathVariable("tournamentId") String tournamentId)` → `ResponseEntity<Resource>` with content-type `text/html`, body backed by `ClassPathResource("static/timer/index.html")`.
  - **Forward-loop avoidance pattern preserved verbatim per C-3** (legacy Javadoc lines 43-50 documents why `forward:` is rejected; same reasoning applies post-cutover).
- [ ] **AC-RED-FIRST-AUDIO-CONTROLLER:** New `de.vvwt.tm.web.timer.AudioController` `@RestController @RequestMapping("/api/audio/tournaments/{tournamentId}")` authored RED-first. Wave-2-aligned URL per Brief v1.1 D-10. **4 endpoints preserved per legacy:**
  - `@PostMapping(value = "/{category}", consumes = MULTIPART_FORM_DATA_VALUE)` `upload(...)` → 201 with `de.vvwt.tm.timer.audio.AudioFileMetadata` body (Pattern A — record imported cross-module from `timer.audio.*`; no web-tier mapping)
  - `@GetMapping("/{category}/stream")` `stream(...)` → 200 with `audio/mpeg` content + InputStream body
  - `@GetMapping` (root = list) `list(...)` → 200 with `List<de.vvwt.tm.timer.audio.AudioFileMetadata>` body (Pattern A consolidation — legacy `List<AudioMetadataResponse>` simplified to direct service-return passthrough; no `AudioMetadataResponse.from(...)` factory call because `AudioMetadataResponse` is consolidated away)
  - `@DeleteMapping("/{category}")` `delete(...)` → 204 (no content) on success, 404 on missing
  - Constructor-injects `AudioStorageService` (interface FQN `de.vvwt.tm.timer.audio.AudioStorageService` per DEC-36 cross-package, accessible via web's `"timer"` allowedDependency entry).

### `web/GlobalExceptionHandler` 5 import-line updates (D-11+D-13 + Cycle-1 reviewer F#4)

- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-1-INVALID-TIMER-URL:** `vvwt-tm-web/src/main/java/de/vvwt/tm/web/GlobalExceptionHandler.java` line ~11: `import de.vvwt.tm.domain.timer.InvalidTimerUrlException;` → `import de.vvwt.tm.timer.InvalidTimerUrlException;`. Per Brief v1.1 D-13 + `feedback_dec22_refactor_phase_first.md`: phase-3 §refactor-clause on **E36S08-Q-1a-reconstructed** code; the underlying class has RED-first provenance. Empirical line numbers verified by Delivery via `grep -n`.
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-2-NO-ACTIVE-TOURNAMENT:** Same pattern: `domain.timer.NoActiveTournamentException` → `timer.NoActiveTournamentException`.
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-3-AUDIO-FORMAT:** `domain.audio.AudioFormatException` → `timer.audio.AudioFormatException`.
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-4-AUDIO-STORAGE:** `domain.audio.AudioStorageException` → `timer.audio.AudioStorageException`.
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-5-AUDIO-SIZE-LIMIT:** `domain.audio.AudioSizeLimitException` → `timer.audio.AudioSizeLimitException`.
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-PRESERVATION:** `@ExceptionHandler(InvalidTimerUrlException.class)` annotation, `handleInvalidTimerUrl(InvalidTimerUrlException ex)` method body, response shapes (HTTP code + JSON body) PRESERVED verbatim. Same for the other 4 handlers. Existing Javadocs preserved (only the import FQN lines updated; any matching `@see` Javadoc tag updates from `domain.X` to canonical FQN if present).
- [ ] **AC-DEC32-NOT-INVOKED:** Per DEC-32 §Scope: import-line edits explicitly excluded from DEC-32 (test code + import-line edits both excluded per §Scope last bullet). Count stays #1 (E21S13).

### `auth.internal.SecurityConfig` 3 URL-pattern updates (D-12 + Cycle-1 reviewer F#4)

- [ ] **AC-SECURITY-CONFIG-URL-1-API-TIMER:** `vvwt-tm-web/src/main/java/de/vvwt/tm/auth/internal/SecurityConfig.java`: `requestMatchers("/api/timer/**")` → `requestMatchers("/api/timer/tournaments/**")` (in `permitAll()` block). Per Brief v1.1 D-12 + `feedback_dec22_refactor_phase_first.md`: phase-3 §refactor-clause on **E15S07-Q-1a-reconstructed** SecurityConfig. TDD-provenance verified.
- [ ] **AC-SECURITY-CONFIG-URL-2-MVC-TIMER:** `requestMatchers("/timer/**")` → `requestMatchers("/timer/tournaments/**")` (in `permitAll()` block).
- [ ] **AC-SECURITY-CONFIG-URL-3-AUDIO-STREAM:** `requestMatchers("/api/tournaments/*/audio/*/stream")` → `requestMatchers("/api/audio/tournaments/*/*/stream")` (in `permitAll()` block).
- [ ] **AC-SECURITY-CONFIG-PRESERVATION:** All other rules + their ordering preserved verbatim per C-8 (security wire-stability). Authentication chain logic preserved.

### Svelte URL-string updates (D-11 user Frage 2: URL-Anpassung, kein rebuild)

- [ ] **AC-SVELTE-TIMER-API-FETCH-URL:** `vvwt-tm-web/src/main/ui-timer/src/lib/timerApi.ts` line ~119: `fetch(\`/api/timer/${encodeURIComponent(tournamentId)}\`)` → `fetch(\`/api/timer/tournaments/${encodeURIComponent(tournamentId)}\`)`. Empirical line number verified at delivery.
- [ ] **AC-SVELTE-TIMER-URL-EXTRACTION-REGEX:** `timerApi.ts` line ~174: regex pattern + comments updated from `/timer/{uuid}` to `/timer/tournaments/{uuid}` to match new URL structure for SPA-side `tournamentId` extraction.
- [ ] **AC-SVELTE-AUDIO-URL-FETCHES:** Audio URL fetches in Svelte (audio playback for START/END/PAUSE — verify by `grep -rn "/api/tournaments.*audio\|/api/audio" ui-timer/src/`) updated to Wave-2-aligned URLs `/api/audio/tournaments/{tournamentId}/{category}/stream`. **Empirical scope verification at delivery; if audio-URL-refs not present in Svelte (timer SPA may construct audio URLs from `TimerDataResponse.audio.startUrl/endUrl/pauseUrl` server-provided strings), no Svelte changes for audio.**
- [ ] **AC-SVELTE-JAVADOC-COMMENTS-POLISH:** Javadoc-style comments in `timerApi.ts` lines 5/165/169 + `App.svelte` line 11 referencing legacy URL patterns updated to Wave-2-aligned URLs.
- [ ] **AC-SVELTE-NO-REBUILD:** Per user Frage 2: ONLY URL-strings updated. No rebuild of Svelte components, no logic changes, no new dependencies. The Vite build re-runs at next `mvn package` (Maven `generate-resources` phase) to regenerate `target/classes/static/timer/index.html` + asset bundle with the URL changes baked in.

### Web-module allowedDependencies expansion

- [ ] **AC-WEB-ALLOWED-DEPS-EXPANSION:** `vvwt-tm-web/src/main/java/de/vvwt/tm/web/package-info.java` `@ApplicationModule.allowedDependencies` array gains `"timer"`. Per `web/package-info.java` Javadoc precedent (e.g., the `print` row added at E24S06 + `display` row at E25S02): add per-entry justification entry: `timer — TimerController + TimerViewController + AudioController consume timer.TimerDataService interface + timer.audio.AudioStorageService interface; web.GlobalExceptionHandler catches 2 timer + 3 timer.audio exceptions`.
- [ ] **AC-DEC40-CLAUSE-E-PRE-APPROVED-FIRM:** Per Brief v1.1 C-15 + DEC-45 D2 FIRM verdict: E26 has unconditional FIRM L2-stays pre-approval (audit (vi) Trigger-(ii) NOT-satisfied). Trigger-α firing at this addition does NOT require fresh L2.5 examination. Story impl-report references the conversion paper-trail: "DEC-45 D2 PROVISIONAL → FIRM conversion recorded at Discovery time in `E26-AUDIT-WEBSOCKET-COUPLING`; operational consummation at this story's `web/package-info.java` update commit."

### Fresh web-module ITs under DEC-44 D1 framework

- [ ] **AC-RED-FIRST-TIMER-CONTROLLER-IT:** New `vvwt-tm-web/src/test/java/de/vvwt/tm/web/timer/TimerControllerIT.java` authored RED-first per Brief v1.1 D-13:
  ```java
  @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT,
                  classes = de.vvwt.tm.TournamentManagerApplication.class,
                  properties = {"spring.datasource.url=jdbc:h2:mem:e26s03timeritdb;…"})
  @ActiveProfiles("test")
  @Import({WebModuleTestConfig.class, TimerControllerIT.TestAdminCredentials.class})
  ```
  Inner `TestAdminCredentials` `@TestConfiguration` provides `@Bean("webItAdminCredentialsProvider") @Primary AdminCredentialsProvider`. Audit (v) baseline 9 (legacy `TimerControllerIT` `@Test` count).
- [ ] **AC-RED-FIRST-TIMER-VIEW-CONTROLLER-IT:** New `web/timer/TimerViewControllerIT.java` authored RED-first. Same annotation form. Audit (v) baseline 5.
- [ ] **AC-RED-FIRST-AUDIO-CONTROLLER-IT:** New `web/timer/AudioControllerIT.java` authored RED-first. Same annotation form. Audit (v) baseline **14** (corrected from grep-15 per Cycle-1 reviewer F#1).
- [ ] **AC-DEC44-D2-EMPIRICAL-REFINEMENT-RESPECT:** None of the 3 new ITs introduce a `UserDetailsService` substitute bean nor a `SecurityFilterChain` substitute bean. Authentication flows production `UserDetailsService` ← `@Primary AdminCredentialsProvider` per-IT-override per DEC-44 §"2026-04-27 Empirical Refinement".
- [ ] **AC-DEC36-CROSS-PACKAGE-TEST-TYPING:** All 3 ITs reference interface FQNs (`de.vvwt.tm.timer.TimerDataService`, `de.vvwt.tm.timer.audio.AudioStorageService`); `@MockitoBean` types reference interface FQN.
- [ ] **AC-DEC38-SLICE-TEST-OPTIONAL:** Optional slice tests (`*ControllerSliceTest` `@WebMvcTest`) per DEC-38 erratum. Story author choice; if introduced, follows DEC-38 slice/IT pattern. NOT required (analog E25S02 + E36S02 pattern).

### URL + JSON-wire preservation (Wave-2-shape adoption)

- [ ] **AC-URL-PATHS-WAVE-2-ALIGNED:** REST URL set Wave-2-aligned per Brief v1.1 D-10: `GET /api/timer/tournaments/{tournamentId}`, `POST/GET-stream/GET-list/DELETE /api/audio/tournaments/{tournamentId}/{category}`. MVC route: `GET /timer/tournaments/{tournamentId}` → `text/html` Resource body. Static asset path `/timer/assets/**` preserved (Vite-built bundle).
- [ ] **AC-JSON-WIRE-PRESERVED:** Response JSON shapes verbatim per audit (iv) DTO inventory. Field names + nested record fields preserved.
- [ ] **AC-AUTHENTICATION-FLOW-PRESERVED:** Token-less for timer + audio-stream endpoints (`/api/timer/tournaments/**`, `/timer/tournaments/**`, `/api/audio/tournaments/*/*/stream` permitAll); admin auth for audio upload/list/delete (`/api/audio/tournaments/**` excluding `/stream` matches the catch-all `/api/**` `authenticated()` rule).

### DEC-22 Iron Law + DEC-41 hierarchy

- [ ] **AC-DEC22-IRON-LAW-RED-EVIDENCE:** Impl-report enumerates each new test method's RED-state commit hash + GREEN-state commit hash.
- [ ] **AC-DEC41-FRESH-AUTHORING-NO-LEGACY-REUSE:** No reuse of legacy `TimerControllerIT` (9 @Test) + `TimerViewControllerIT` (5 @Test) + `AudioControllerIT` (14 @Test) per audit (v) 28/28 Snapshot-Driven verdict + DEC-41 §3 hierarchy item 1. Impl-report cites `E26-AUDIT-DEC41-TEST-CLASSIFICATION` (this Discovery commit) verbatim per DEC-41 §4.
- [ ] **AC-Q7-METHOD-COUNT-RECONFIRMATION:** Delivery counts `@Test` in fresh ITs; reports any deviation from audit (v) baseline (9+5+14=28) in impl-report.

### Cross-cutting

- [ ] **AC-MVN-VERIFY-GREEN:** `mvn verify` GREEN on staging post-merge. Includes Vite-rebuild of `ui-timer/` SPA at `mvn generate-resources` phase (URL strings updated; build output regenerated).
- [ ] **AC-APPLICATIONMODULESTEST-GREEN:** Post-S03 commit, `ApplicationModulesTest` GREEN per DEC-21. `timer` + `timer.audio` accessible from `web` via `"timer"` allowedDependency entry. **If `timer.audio` cross-module access fails** (unlikely per DEC-21 sub-package public API rule — verify at delivery), STOP-and-escalate per Brief v1.1 D-9 trade-off; remediation = add `timer::audio` named-interface declaration to `timer/package-info.java` (E26S01 retrofit) + `web.allowedDependencies += "timer::audio"` (this story).
- [ ] **AC-DEC31-PROPAGATE-GOVERNANCE:** Per DEC-31 §(c) two-commit contract at story closure.
- [ ] **AC-DELIVERY-MODE-SOURCE:** `delivery_mode: source`.
- [ ] **AC-NO-PRODUCTION-CODE-LOGIC-CHANGE-OUTSIDE-WEB-AND-MECHANICAL-EDITS:** Outside the explicit AC scope (web tier + 5 GlobalExceptionHandler imports + 3 SecurityConfig URL-patterns + Svelte URL-strings), no production code modified.
- [ ] **AC-SLICE-TESTS-NOT-REGRESSED:** All existing `*ControllerSliceTest.java` files (`@WebMvcTest`) UNCHANGED.
- [ ] **AC-DEC-26-DAO-IT-N/A:** Audio is filesystem-based (DEC-26 N/A per E26S02 confirmation). No DAO IT changes.

---

## Out of Scope

- Domain layer (`timer.*` core incl. 4 query-shape records per Pattern A + `timer.audio.*` sub-package incl. `AudioFileMetadata` Pattern A consolidated record) — **E26S01 + E26S02 scope** (already done before S03).
- Authoring of any Java records under `de.vvwt.tm.web.internal.dto.*` for timer/audio — **REVOKED per DEC-40 §2026-04-27 Pattern A** (was originally Brief v1.1 scope; revoked at Discovery 2026-04-27 post-DEC-40 inline clarification). The 5 query-shape records are bounded-context-owned at S01/S02 placement.
- Javadoc-historical-provenance sweep + Timer-track Success Metrics + propagate-governance commits beyond DEC-31 §(c) — **E26S04 scope**.
- Svelte UI rebuild beyond URL-string updates — preserved verbatim per D-11 user choice (kein rebuild).
- WebSocket Java code — timer has zero (audit (vi)).
- DEC-44 IT-annotation retrofit — E39S01 done.
- L2.5 examination at `web` Trigger-α firing — pre-approved per DEC-45 D2 FIRM verdict for E26.

---

## Dependencies

- **E26S01** — provides `de.vvwt.tm.timer.{TimerDataService,InvalidTimerUrlException,NoActiveTournamentException}` types AND the 4 query-shape records `de.vvwt.tm.timer.{TimerDataResponse,TimerPhaseResponse,TimerScheduleEntryResponse,TimerAudioResponse}` (Pattern A) referenced by S03 controllers + GlobalExceptionHandler imports.
- **E26S02** — provides `de.vvwt.tm.timer.audio.{AudioStorageService,AudioCategory,AudioFileMetadata,AudioFormatException,AudioStorageException,AudioSizeLimitException}` types referenced by S03 AudioController + GlobalExceptionHandler imports. `AudioFileMetadata` (Pattern A consolidated; absorbs legacy `AudioMetadataResponse`) is the controller-return type for upload + list endpoints.
- **E39S01** — provides `WebModuleTestConfig` post-E39S01 state required for new web-module ITs to land under DEC-44 D1 framework.

---

## Notes

**Estimated LOC:**

- Production: ~150-250 LOC across 3 files (3 controllers; web-tier DTOs ELIMINATED per Pattern A — was 5 records × ~70-200 LOC each ≈ 500 LOC of authoring-scope removed, relocated to S01/S02 inputs) + 5-line `web/GlobalExceptionHandler` import edits + 3-line `auth.internal.SecurityConfig` URL-pattern edits + 1-line `web/package-info.java` `allowedDependencies` extension + ~5-10 lines Svelte URL-string updates.
- Test: ~1500-2000 LOC across 3 fresh ITs (audit (v) baseline 9+5+14=28; concrete count by Delivery's RED-first authoring).

**TDD-provenance verified targets per Brief v1.1 D-12+D-13 + Cycle-1 reviewer F#4:**

- `web/GlobalExceptionHandler.java` is **E36S08-Q-1a-reconstructed** (commits visible in `git log -- vvwt-tm-web/src/main/java/de/vvwt/tm/web/GlobalExceptionHandler.java`). §refactor-clause invocation valid for the 5 import-line updates.
- `auth.internal.SecurityConfig.java` is **E15S07-Q-1a-reconstructed** (per memory + git log). §refactor-clause invocation valid for the 3 URL-pattern updates.
- Per `feedback_dec22_refactor_phase_first.md`: §refactor-clause is RED-GREEN-REFACTOR phase 3 on TDD-authored code; both targets pass.

**E25 + E26 parallel-Delivery merge-conflict awareness:** E25S02 (display) and E26S03 (timer) both modify `web/package-info.java` allowedDependencies + `web/GlobalExceptionHandler` imports. Daemon serializes story execution. If E25S02 lands first: E26S03 `git rebase`-on-conflict adds `"timer"` after `"display"` in array. If E26S03 lands first: E25S02 rebase adds `"display"` after E26S03's contributions. Neither outcome blocks delivery; standard rebase-on-conflict per E36 trio precedent. Same for `web/GlobalExceptionHandler` (E25S02 adds 1 import for display.NoActivePhaseException; E26S03 adds 5 timer/audio imports — disjoint imports, conflict-free except for the import-block surrounding lines).

**Audit (v) baseline 28 corrected from grep 29 per Cycle-1 reviewer F#1** (AudioControllerIT actual count = 14, not 15).

**Reviewer F#5 carry-forward (Brief v1.1 D-9 trade-off):** AC-APPLICATIONMODULESTEST-GREEN explicitly handles the option-A trade-off — if Modulith requires `timer::audio` named-interface for cross-module access (unlikely), the remediation path is documented (timer/package-info.java retrofit + web.allowedDependencies addition).

**Reviewer F#6 carry-forward (DEC-45 D2 paper-trail):** AC-DEC40-CLAUSE-E-PRE-APPROVED-FIRM cites the audit (vi) artefact for the conversion paper-trail.

**DEC-40 §2026-04-27 Pattern A application (Discovery 2026-04-27 post-Brief-v1.1 supplementation):** Brief v1.1 originally scoped 5 record DTOs at `de.vvwt.tm.web.internal.dto.*` per literal Clause B reading. After Brief v1.1 was authored, DEC-40 was inline-clarified at `last_updated_at: 2026-04-27` with §"2026-04-27 Clarification — Bounded-context-owned query-shape DTOs (Java records)" introducing Pattern A vs Pattern E placement rule for service-method-return records. Pattern A application supersedes the Brief v1.1 web-tier-DTO scope:
- 4 timer records (`TimerDataResponse`, `TimerPhaseResponse`, `TimerScheduleEntryResponse`, `TimerAudioResponse`) → relocated to E26S01 at `de.vvwt.tm.timer.*`.
- 1 audio record (`AudioMetadataResponse` legacy 1:1 wrapper) → consolidated into `AudioFileMetadata` at `de.vvwt.tm.timer.audio.*` by E26S02.
- E26S03 web tier authors NO records under `web.internal.dto.*` for timer/audio.
- Pattern A justification per record: projection == wire shape; no Clause B (a)/(c)/(d) condition fires.
- Architectural-lesson precedent: E25S01 first-attempt delivery (outer-repo commit `23d947b`) escalated due to `display↔web` Modulith cycle when 3 display query-shape records were placed at `web.internal.dto.*`. E26 applies the lesson prospectively at Discovery time, preventing a re-occurrence at S03 delivery.
