---
type: artefact
artefact_type: qa-report
id: E38S01
epic: E38
track: delivery
verdict: PASS
skills_invoked: [qa-review, memory-alignment-check]
related_decs: [DEC-3, DEC-10, DEC-11, DEC-13, DEC-22, DEC-23, DEC-29, DEC-30, DEC-31, DEC-42, DEC-43]
created_at: 2026-04-27
---

# QA Report — E38S01

## Verdict: PASS

All 11 acceptance criteria satisfied. No rule violations. No regressions. Clean PASS on first QA cycle.

---

## Per-Criterion Results

### AC1 (testing) — RED-first existence tests

**PASS.**

Three RED-first existence tests documented in impl-report.md:

- `vvwt-info-dto/src/test/.../InfoDtoModuleExistsTest.java` — RED: module dir + pom absent → compile failure. GREEN: after pom.xml + package-info.java created.
- `vvwt-info-server/src/test/.../InfoServerBootsTest.java` — RED: module absent → compile failure. GREEN: after `InfoServerApplication.java` created.
- `vvwt-info-client/src/test/.../InfoClientBuildExistsTest.java` — RED: module absent → compile failure. GREEN: after pom.xml + package-info.java created.

All three test classes compiled and ran GREEN at commit `4fdd4dc`. DEC-22 Iron Law satisfied: RED state precedes GREEN state for all new production classes. Test bodies are non-trivial (they test compilability, which IS the RED/GREEN distinction for skeleton modules).

### AC2 (testing) — Full verify GREEN

**PASS.**

`mvn verify -pl vvwt-info-dto,vvwt-info-server,vvwt-info-client -am --batch-mode` produces `BUILD SUCCESS`:
- `vvwt-info-dto`: 1/1 surefire test PASS
- `vvwt-info-server`: 2/2 surefire tests PASS + 1/1 failsafe IT PASS
- `vvwt-info-client`: 1/1 surefire test PASS + npm install + `npm run build` → `dist/`

Maven reactor builds `vvwt-info-dto` before `vvwt-info-server` (Java dep graph, `-am` flag). `frontend-maven-plugin` npm executions are wired to `initialize` (install-node-and-npm) and `generate-resources` (npm install, npm run build) phases per AC2.

### AC3 (testing) — Effective POM inheritance

**PASS.**

`mvn help:effective-pom -pl vvwt-info-dto` output (captured in impl-report.md §AC3) confirms:
- `failOnWarning=true` (DEC-29) — inherited from parent, no per-module override
- `-Xlint:all,-serial,-processing,-options` (DEC-29) — inherited, no per-module override
- `spotless-maven-plugin` with `googleJavaFormat AOSP + removeUnusedImports` (DEC-30) — inherited, no per-module override

TDD toolchain (DEC-22): JUnit Jupiter + AssertJ present in all three modules via test-scoped deps. No per-module override of parent's plugin management.

### AC4 (governance) — vvwt-tm-web → vvwt-info-dto dep

**PASS.**

`vvwt-tm-web/pom.xml` contains exactly one `<dependency>` on `de.vvwt:vvwt-info-dto`. `mvn dependency:tree -pl vvwt-tm-web | grep vvwt-info` shows:
```
\- de.vvwt:vvwt-info-dto:jar:1.0.0-SNAPSHOT:compile
```
No `vvwt-info-server` or `vvwt-info-client` in TM dep tree. DEC-42 D2 cross-subsystem edge correctly wired.

### AC5 (governance) — Skill attestation

**PASS.**

`impl-report.md` frontmatter declares:
```yaml
skills_invoked: [tdd-implement, evaluate-story, delivery-high-level-plan, prepare-execution-plan]
related_decs: [DEC-3, DEC-10, DEC-11, DEC-13, DEC-22, DEC-23, DEC-29, DEC-30, DEC-31, DEC-42, DEC-43]
```
`related_decs` matches story frontmatter exactly. Governance propagation per DEC-23 + DEC-31 (story file + impl-report + qa-report + memory-delta → `vvwt-prj/docs/governance/`) is AC5's remaining step, executed at story closure by the Delivery Orchestrator.

### AC6 (security) — bannedDependencies on info-server + info-client

**PASS.**

`vvwt-info-server/pom.xml`: Maven Enforcer execution `enforce-banned-deps-info-server` bans:
- `de.vvwt:vvwt-tm-web`, `de.vvwt:vvwt-tm-*`
- `de.vvwt:vvwt-worker-*`, `de.vvwt:vvwt-slotopt-worker-lib`, `de.vvwt:vvwt-slotopt-dispatcher`, `de.vvwt:vvwt-slotopt-*`
- `de.vvwt:vvwt-dispatcher`, `de.vvwt:vvwt-standalone-worker`, `de.vvwt:vvwt-benchmark`

`vvwt-info-client/pom.xml`: Maven Enforcer execution `enforce-banned-deps-info-client` bans the same list. `searchTransitive=true`, `fail=true`. Both modules verified GREEN under `mvn verify`.

### AC7 (security) — Symmetric ban on vvwt-tm-web

**PASS.**

`vvwt-tm-web/pom.xml` contains Enforcer execution `enforce-banned-deps-tm-web` banning `de.vvwt:vvwt-info-server` and `de.vvwt:vvwt-info-client`, `searchTransitive=true`, `fail=true`. Only `vvwt-info-dto` is permitted (the DEC-42 D2 allowed edge). `mvn verify -pl vvwt-tm-web -am` produces BUILD SUCCESS confirming the rule fires without false-positive rejection.

### AC8 (security) — Default-profile-is-self-host startup assertion

**PASS.**

`application.yml` configures `spring.profiles.default=self-host`. `InfoServerBootsTest` Spring log confirms:
```
No active profile set, falling back to 1 default profile: "self-host"
```
`InfoServerBootsTest.applicationBootsWithDefaultSelfHostProfile()` asserts that `self-host` is present in either `activeProfiles` or `defaultProfiles` — PASS.

`InfoServerBootsWithPrimaryProfileTest.applicationBootsWithPrimaryProfile()` uses `@ActiveProfiles("primary")` + `@TestPropertySource` H2 override. Spring log:
```
The following 1 profile is active: "primary"
```
AC8 both assertions pass. DEC-42 D3 guarantee structurally enforced.

### AC9 (security) — BannedDependenciesEnforcerIT

**PASS.**

`BannedDependenciesEnforcerIT.enforcerRejectsFixturePomWithForbiddenDependency()` runs as a Failsafe IT:
- Writes fixture pom with `de.vvwt:vvwt-tm-web` dependency + `bannedDependencies` Enforcer execution
- Invokes `mvn validate` (lifecycle-bound execution, not `enforcer:enforce` CLI — see impl-report §TDD Cycle Log AC9 REFACTOR note)
- AC9(a): `exitCode != 0` — PASS
- AC9(b): output contains `bannedDependencies` (case-insensitive) — PASS

Test is permanent. Provides durable Enforcer-fires evidence per story requirement.

### AC10 (error-handling) — Both profiles boot cleanly

**PASS.**

`InfoServerApplication.java` is a minimal `@SpringBootApplication`. Both profiles boot cleanly:
- Self-host: H2 embedded, no-config startup completes without errors
- Primary: H2 override in test; production primary uses PostgreSQL (no DB tables, no endpoints in E38S01 scope)

Declared deps per AC10: `spring-boot-starter-web`, `spring-boot-starter-data-jdbc`, `spring-boot-starter-validation`, `flyway-core` (compile), `h2` (runtime), `postgresql` (runtime, `optional=true`). Verified in `vvwt-info-server/pom.xml`.

### AC11 (error-handling) — Svelte 5 + TypeScript 5 scaffold

**PASS.**

`package.json` declares `svelte: ^5.55.3`, `typescript: ^5.8.3`, `vite: ^8.0.8`. SvelteKit absent:
```
npm ls @sveltejs/kit → (empty)
```
`npm run build` (via Maven `mvn verify -pl vvwt-info-client -am`) produces `dist/assets/` + `dist/index.html`. DEC-2 compliance confirmed.

---

## Scope Integrity Check

All 24 changed files in commit `4fdd4dc` are within expected scope:
- `pom.xml` (root reactor + dependencyManagement)
- `.gitignore` (vvwt-info-client SPA excludes)
- `vvwt-info-dto/**` (3 new files)
- `vvwt-info-server/**` (8 new files)
- `vvwt-info-client/**` (10 new files)
- `vvwt-tm-web/pom.xml` (AC4 + AC7 additions)

No files outside this set were modified. No out-of-scope refactors. Zero deletions of non-target files.

---

## Rule Enforcement

| Rule | Status | Notes |
|------|--------|-------|
| DEC-22 TDD Iron Law | PASS | RED-state cited for all 3 new module skeletons |
| DEC-29 Compiler hygiene | PASS | Inherited via parent POM, no per-module override |
| DEC-30 Spotless AOSP | PASS | `mvn spotless:apply` was run; sources pass Spotless check |
| DEC-36 Cross-package test typing | PASS | `InfoServerBootsTest` references `InfoServerApplication` — same package (`de.vvwt.info`). `BannedDependenciesEnforcerIT` (package `de.vvwt.info.build`) references no production classes. No `Default*` impl cross-package refs. |
| DEC-41 Spec-anchored test reuse | N/A | Not a reconstruction story; skip per qa-review §7 trigger condition |
| DEC-42 D2 service boundary | PASS | Build-time Enforcer + dep tree verified |
| DEC-42 D3 self-host default | PASS | Spring log + test assertion verified |
| DEC-3 OSS-only | PASS | All deps are Apache 2.0 / MIT / EPL (H2, Svelte, Vite, Spring, Flyway, frontend-maven-plugin) |

---

## Regression Scan

Three pre-existing failures in `vvwt-tm-web` are documented in impl-report.md §Pre-existing TM Test Failures:
- `DomainEventBridgeTest$IntegrationTest.matchResultChangedEvent_receivedOnEventsTopic`
- `WebSocketConfigTest.webSocketConfig_classPresent`
- `WebSocketConfigTest.stompClient_connectsToWsEndpoint`

These fail identically on `origin/staging` (baseline `6e23ca5`) and on the story branch. E38S01 does not modify any file touched by these tests. Not a regression introduced by this story.

---

## Remediation Log

No remediation cycles required. Clean PASS on first QA pass.
