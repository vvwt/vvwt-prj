<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E36S07.story.md at 3bfb2e6a7d7ebd69805725754d9ba695f133eedc 2026-04-26 -->
---
type: artefact
artefact_type: story
id: E36S07
epic: E36
track: discovery
related_backlog_id: E36S07
related_decs: [DEC-13, DEC-21, DEC-22, DEC-23, DEC-31, DEC-40]
depends_on: [E24S07, E36S04, E36S05, E36S06]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-26-e36-retro-correction
mandatory_ac_categories: [testing, governance]
created_at: 2026-04-26
updated_at: 2026-04-26
---

# Story E36S07: Certificate atomic cutover (Cutover-2 of E36) — DELETE legacy certificate references + Javadoc-historical-provenance sweep + propagate-governance + Certificate-track Success Metrics verification

## User Story

**As a** maintainer of `vvwt-tm-web` closing Track 2 (Certificate) of Epic E36,
**I want** the Certificate-track closing housekeeping commit pair: (1)
Javadoc-historical-provenance sweep + Cutover-2 verification AC bundle, (2)
governance snapshot propagation per DEC-31,
**so that** Track 2 reaches 0% Q-1b code at the in-scope canonical-FQN subset of
`de.vvwt.tm.certificate.*` + `de.vvwt.tm.web.certificate.*`,
ApplicationModulesTest is GREEN, and the Certificate-track Success Metrics from
Epic E36 are independently verified before Track 3 (web cross-cutting via
GlobalExceptionHandler migration in E36S08) begins.

---

## Context

Per Session Brief `discovery-2026-04-26-e36-retro-correction` D-2/D-7:
Track 2 uses Option γ (delete-then-rebuild within feature-branch lifecycle), so
production-code deletion already happened in E36S04 (domain), E36S05
(assembler impl), and E36S06 (controller). The "atomic cutover" here is the
closing housekeeping commit pair for Track 2 — no additional production-code
deletion (mirrors E36S03 pattern for the Photo track).

This story handles three closing concerns:
1. **Javadoc-historical-provenance sweep** (per Brief D-6): pre-existing files
   OUTSIDE the rebuild-set may carry `{@link de.vvwt.tm.certificate.*}` Javadoc
   refs to deleted-via-E23 cert classes (precedent: E24S07 identified analogous
   files for the print-track sweep).
2. **Governance propagation** (per DEC-31 §(c)): governance snapshots for
   E36S04-S07 propagated into `vvwt-prj/docs/governance/stories/`.
3. **Certificate-track Success Metrics verification**: 0% Q-1b code at the
   in-scope canonical-FQN subset; ApplicationModulesTest GREEN;
   record/exception/config signatures preserved per C-3; assembler interface
   contract intact.

Per Brief Q-3 + DEC-31 §(c): cutover stories deliver as TWO adjacent commits.

---

## Acceptance Criteria

### Common ACs (every E36 story)

- [ ] **AC-TDD-RED-FIRST-EVIDENCE** (per Brief Q-1): N/A for new production
  code (no new production code in this story); impl-report MUST explicitly state
  this story has no NEW production code.
- [ ] **AC-NO-DANGLING-REFS-EXECUTABLE** (per Brief Q-2): Post-sweep grep gate
  against legacy (pre-DEC-40) certificate FQNs that should have NO surviving
  refs in executable Java code (canonical FQN `de.vvwt.tm.certificate.*` is
  preserved per Option γ and is NOT the grep target):
  ```
  grep -rn "de\.vvwt\.tm\.\(infrastructure\.web\.certificate\|infrastructure\.print\|domain\.certificate\)" \
    vvwt-tm-web/src/main/java/ vvwt-tm-web/src/test/java/ \
    | grep -v -E '^\s*\*|//|@(code|link|see|literal|throws)'
  ```
  Returns ZERO matches in executable Java code (production + tests). Refs to
  the canonical FQN `de.vvwt.tm.certificate.*` are valid (they resolve to
  rebuilt classes per Option γ OR to preserved out-of-scope classes —
  `CertificateAssembler`, `CertificateTemplateRepository`,
  `DefaultCertificateTemplateRepository`) and are deliberately NOT in the
  grep target.
- [ ] **AC-RELATED-DECS-COMPLIANCE**: All DECs in `related_decs` frontmatter
  (DEC-13, DEC-21, DEC-22, DEC-23, DEC-31, DEC-40) respected.
- [ ] **AC-APPLICATION-MODULES-TEST-GREEN** (per Brief C-8): `ApplicationModulesTest`
  MUST remain GREEN end-of-story.
- [ ] **AC-TDD-IRON-LAW-NEW-CODE-ONLY** (per Brief C-4 + DEC-41 §3): N/A — no
  new production code.
- [ ] **AC-C3-SIGNATURE-PRESERVATION** (per Brief C-3): Sweep does not alter
  any production signatures.

### Story-specific ACs

- [ ] **AC-GOVERNANCE-COMMIT-ATOMIC-TWO-PART** (per Brief Q-3 + DEC-31 §(c)):
  Story delivers as TWO adjacent commits on vvwt-prj/staging:
  - **Commit 1**: `chore(E36S07): certificate track Javadoc-historical-provenance sweep + Cutover-2 verification`
    — sweep edits + impl-report assertions; zero production behavior change.
  - **Commit 2**: `docs(governance): propagate E36S07 snapshot per DEC-31`
    — propagates governance artefacts (E36S04, E36S05, E36S06, E36S07 stories +
    impl-reports + qa-reports) to `vvwt-prj/docs/governance/`. Idempotent per
    DEC-31 §(f).
- [ ] **AC-JAVADOC-PROVENANCE-SWEEP** (per Brief D-6): Sweep removes/updates
  Javadoc `@link` references to deleted-via-E23 cert classes in pre-existing
  files OUTSIDE the rebuild-set. Target set identified at delivery via:
  ```
  grep -rn "@link de\\.vvwt\\.tm\\.\\(infrastructure\\.web\\.certificate\\|infrastructure\\.print\\|domain\\.certificate\\)" vvwt-tm-web/src/main/java/
  ```
  For each match, either (a) update to the new canonical FQN if the type still
  exists, or (b) remove the `@link` and inline the class name as a non-link
  reference if the legacy FQN no longer resolves. Impl-report enumerates all
  swept files + before/after Javadoc deltas.
- [ ] **AC-LEGACY-FQN-RESIDUE-CHECK** (per Brief Q-2; complements
  AC-JAVADOC-PROVENANCE-SWEEP): After the sweep, verify zero remaining Javadoc
  `@link`/`@see`/`@code` refs to legacy FQNs in production or test sources:
  ```
  grep -rn "@\(link\|see\|code\) de\.vvwt\.tm\.\(infrastructure\.web\.certificate\|infrastructure\.print\|domain\.certificate\)" \
    vvwt-tm-web/src/main/java/ vvwt-tm-web/src/test/java/
  ```
  Returns ZERO matches.
- [ ] **AC-CERT-TRACK-METRICS** (Certificate-track Success Metrics verification
  per Epic E36 Success Metrics): Impl-report enumerates evidence for each
  metric:
  1. **0% Q-1b code at in-scope canonical FQN subset
     `de.vvwt.tm.certificate.*` (in-scope per audit (i)) and
     `de.vvwt.tm.web.certificate.*`**: every artefact enumerated in
     `audits/E36-discovery-legacy-inventory.md` Certificate groups (excluding
     repository preserved per audit (iii)) has been deleted-and-rebuilt under
     Q-1a TDD at the same FQN. Verified via `git log --follow` showing rebuild
     commits + grep showing no surviving Q-1b Javadoc markers.
  2. **ApplicationModulesTest GREEN** end-of-story (re-asserted per Brief C-8).
  3. **Record/exception/config signatures preserved per C-3**: re-verify
     `CertificateTemplateMetadata` field shape (5 consumers per audit (i)),
     `CertificateTemplateVariable` field shape (3 consumers), 3 cert exception
     constructor signatures, `CertificateTemplateStorageConfig` getter/setter
     signatures + bean name (`certificateModuleStorageConfig`) match pre-E36
     baseline.
  4. **Assembler interface contract intact**: `CertificateAssembler` interface
     (E23S08 Q-1a half) untouched; rebuilt `DefaultCertificateAssembler`
     impl satisfies it; `CertificateRenderController` continues to compile and
     execute against the unchanged interface.
  5. **DAO surface intact** (per audit (iii) PASS):
     `CertificateTemplateRepository` interface,
     `DefaultCertificateTemplateRepository` impl, and
     `DefaultCertificateTemplateRepositoryIT` all untouched throughout Track 2.
- [ ] **AC-MVN-VERIFY-GREEN-FINAL**: `mvn verify` GREEN at end-of-story.

---

## Out of Scope

- Any production code addition or modification beyond Javadoc edits.
- Certificate domain / assembler impl / controller rebuild — done in
  E36S04-E36S06.
- `CertificateAssembler` interface, `CertificateTemplateRepository` interface,
  `DefaultCertificateTemplateRepository` impl + IT (preserved).
- `CertificateRenderController` (preserved).
- `CertificateExceptionAdvice` deletion (E36S08 Phase 2 territory). Per Brief
  D-6 the sweep target is Javadoc only.
- GlobalExceptionHandler relocation + advice consolidation (E36S08 territory).
- Photo track (closed in E36S03).

---

## Dependencies

- **E24S07** per Brief C-1.
- **E36S04** + **E36S05** + **E36S06** (Certificate domain + assembler impl +
  controller all rebuilt at canonical FQN).
- **DECs**: DEC-13, DEC-21 (atomic cutover discipline applied to closing
  housekeeping), DEC-22, DEC-23 (governance propagation), DEC-31 (snapshot
  propagation §(c) two-commit shape), DEC-40.
- **Audits**:
  - `E36-discovery-legacy-inventory.md` (Certificate-group enumeration to verify
    against Certificate-track Success Metrics)
  - `E36-discovery-dec41-test-classification.md` (consulted; no new tests in
    this story)
  - `E36-discovery-dec26-dao-coverage.md` (PASS verdict — backs metric 5
    "DAO surface intact")
- **Session Brief**: `discovery-2026-04-26-e36-retro-correction`.

---

## Notes

- **Per Brief D-6** + E24S07 precedent: the Javadoc sweep target set is
  pre-existing files outside the rebuild-set with `{@link
  de.vvwt.tm.{infrastructure.web.certificate,domain.certificate}.*}`
  historical references.
- **Constraint** (Brief C-6): DEC-31 §(c) two-commit contract.
- **Risk**: A surviving dangling Javadoc `@link` after the sweep is enforced by
  AC-JAVADOC-PROVENANCE-SWEEP (its own quality concern beyond Q-2).
- **Per Brief C-2**: DEC-32 invocation count remains 1 (E21S13 only).
- **Per DEC-31 §(f)**: governance propagation is idempotent.
- Commit message templates:
  - Commit 1: `chore(E36S07): certificate track Javadoc-historical-provenance sweep + Cutover-2 verification — sweep N files outside the rebuild-set; Certificate-track Success Metrics verified; ApplicationModulesTest GREEN; DAO surface intact per audit (iii)`
  - Commit 2: `docs(governance): propagate E36S07 snapshot per DEC-31`

---

## Discovery Audit References

Per Brief Q-5, this story cites the in-session Discovery audits:

- **`audits/E36-discovery-legacy-inventory.md`** — enumerates the
  Certificate-group in-scope artefacts whose deletion-and-rebuild evidence backs
  the Certificate-track Success Metrics verification.
- **`audits/E36-discovery-dec41-test-classification.md`** — consulted to
  confirm no new test corpus is introduced; rebuilt E36S04/S05/S06 test corpora
  stand as the regression net.
- **`audits/E36-discovery-dec26-dao-coverage.md`** — PASS verdict backs
  metric 5 (DAO surface intact: repository interface + impl + IT all preserved
  throughout Track 2).
