<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E26S01.story.md at 339b1b9e217ccc3d36bc8571754e9feec9bd4b30 2026-04-28 -->
---
type: artefact
artefact_type: story
id: E26S01
related_backlog_id: E26S01
track: discovery
created_at: 2026-04-27
updated_at: 2026-04-27
mandatory_ac_categories: [error-handling, testing, governance, security]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-27-e26-timer-resumption
related_decs: [DEC-13, DEC-15, DEC-21, DEC-22, DEC-31, DEC-35, DEC-36, DEC-40, DEC-41, DEC-44, DEC-45]
delivery_mode: source
---

# Story: Timer domain Q-1a TDD rebuild — `TimerDataService` interface + `DefaultTimerDataService` impl + `InvalidTimerUrlException` + `NoActiveTournamentException` + `TimerBreakType` + 4 query-shape records (`TimerDataResponse`, `TimerPhaseResponse`, `TimerScheduleEntryResponse`, `TimerAudioResponse`) + `timer/package-info.java` (D-7 γ delete-then-author-fresh at canonical FQN `de.vvwt.tm.timer.*`; records placed per DEC-40 §2026-04-27 Pattern A)

## User Story

**As a** Tournament Manager Delivery Agent operating under DEC-21/-22/-40/-44/-45 + Brief v1.1 governance,
**I want** to delete legacy `domain.timer.{TimerDataService,InvalidTimerUrlException,NoActiveTournamentException,TimerBreakType}` AND legacy `infrastructure/web/timer/dto/{TimerDataResponse,TimerPhaseResponse,TimerScheduleEntryResponse,TimerAudioResponse}.java` (the 4 query-shape records relocate from `infrastructure.web.timer.dto.*` to the timer module root per Pattern A) and author fresh Q-1a TDD RED-first replacements at canonical FQN `de.vvwt.tm.timer.*` (interface in root, impl in `.internal`, exceptions + enum + 4 query-shape records in root) within ONE feature-branch lifecycle per D-7 Coexistence Option γ,
**so that** the `timer` bounded-context joins the Spring Modulith module set with DEC-22 Iron Law TDD compliance + DEC-35 package-layout discipline + DEC-36 cross-package test typing + canonical 6-arg constructor preserved per C-3 signature-preservation, AND the 4 timer query-shape records sit at the bounded-context module root per DEC-40 §2026-04-27 Pattern A (avoiding the `timer↔web` Modulith cycle that broke E25S01 first-attempt delivery at outer-repo commit `23d947b`).

---

## Context

Per audit (v) (this Discovery commit): all 28 in-scope timer/audio tests are Snapshot-Driven; ZERO reusable as Contract Tests per DEC-41 §1. Per audit (iv): legacy `TimerDataService` is 446 LOC fat orchestrator with 1 public method (`buildTimerData(UUID)`) + 6-arg constructor + complex phase-detection / timeline-mapping / audio-URL-construction logic. Per `feedback_dec22_refactor_phase_first.md`: legacy code is NOT a trustworthy oracle for §refactor-clause; fresh Q-1a TDD RED-first authoring is the only DEC-22-compliant path.

Per audit (vi) (this Discovery commit): Trigger-(ii) NOT satisfied; DEC-45 D2 PROVISIONAL → FIRM. E26 unconditional L2-stays per DEC-45 D2.

Per DEC-40 §2026-04-27 Clarification (Pattern A vs Pattern E placement rule for service-method-return records, authored 2026-04-27 in resolution of E25S01 escalation): the 4 timer query-shape records (`TimerDataResponse`, `TimerPhaseResponse`, `TimerScheduleEntryResponse`, `TimerAudioResponse`) are bounded-context-owned at `de.vvwt.tm.timer.*` (module root, public). Justification per Decision Rule §289-296 of DEC-40: `TimerDataService.buildTimerData(UUID)` returns `TimerDataResponse` with all 11 fields populated; the timer SPA (Svelte) consumes the JSON wire shape directly via `fetch().json()`; no Clause B (a)/(c)/(d) condition fires (no field omission, no cross-context aggregation, no field aliasing). Placement at `web.internal.dto.*` would create the exact `timer↔web` Modulith cycle resolved by E25S01 escalation (outer-repo commit `23d947b`).

This story authors the timer **domain core** (service + 2 exceptions + 1 enum + 4 query-shape records + package-info). The `timer.audio` sub-package is E26S02 scope. Web-side controllers + GlobalExceptionHandler import + SecurityConfig + Svelte URL strings + fresh ITs are E26S03 scope (E26S03 authors NO web-tier DTOs per Pattern A). Cutover-housekeeping is E26S04 scope.

---

## Acceptance Criteria

### Pre-flight: legacy deletion

- [ ] **AC-DELETE-LEGACY-FIRST:** Single commit on the story branch deletes the following BEFORE any RED-first authoring:
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/TimerDataService.java` (446 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/InvalidTimerUrlException.java` (20 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/NoActiveTournamentException.java` (20 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/TimerBreakType.java` (15 LOC)
  - **4 legacy timer DTOs at `infrastructure/web/timer/dto/*` (relocated to `de.vvwt.tm.timer.*` per Pattern A — see record-authoring ACs below):**
    - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerDataResponse.java` (207 LOC)
    - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerPhaseResponse.java` (68 LOC)
    - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerScheduleEntryResponse.java` (175 LOC)
    - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerAudioResponse.java` (67 LOC)
  - The `domain/timer/` directory + `infrastructure/web/timer/dto/` directory (both become empty post-deletion). **Audio domain files** at `domain/audio/*` (incl. `AudioFileMetadata`) + legacy `infrastructure/web/audio/AudioMetadataResponse.java` are deleted in E26S02 (Pattern A consolidation per E26S02 scope). **Web controllers + tests** at `infrastructure/{timer,web/timer,web/audio}/*` (NOT the 4 DTO files in `infrastructure/web/timer/dto/` — those are deleted by S01) are deleted in E26S03.
- [ ] **AC-NO-LEGACY-CODE-REFERENCE:** Per Brief v1.1 C-17 (analog to E25S01-S03 C-17): legacy source MUST NOT be inspected during RED-first authoring beyond audit-i empirical inventory (already captured in this Discovery commit's audits). Authoring proceeds from Brief + audits + DEC + URL/JSON-wire-contracts + tournament repository signatures.

### Q-1a TDD RED-first authoring

- [ ] **AC-RED-FIRST-TIMER-DATA-SERVICE-INTERFACE:** New `de.vvwt.tm.timer.TimerDataService` interface authored RED-first. Single public method signature preserved per C-3 signature-preservation:
  - `TimerDataResponse buildTimerData(UUID tournamentId)`
  - `TimerDataResponse` record resides at `de.vvwt.tm.timer.TimerDataResponse` per DEC-40 §2026-04-27 Clarification Pattern A (E26S01 authors it — see AC-RED-FIRST-TIMER-DATA-RESPONSE below). Interface and return-type are in the same module-root package; cross-module web tier accesses both via the `"timer"` allowedDependency entry.
- [ ] **AC-RED-FIRST-DEFAULT-TIMER-DATA-SERVICE:** New `de.vvwt.tm.timer.internal.DefaultTimerDataService` impl class implements `TimerDataService`; authored RED-first. **6-arg constructor signature preserved verbatim** per C-3:
  ```
  DefaultTimerDataService(
      TournamentRepository, PhaseRepository, MatchRepository,
      PhaseBreakRepository, TimelineCalculationService, AudioStorageService)
  ```
  - All 5 tournament repos + `TimelineCalculationService` interface from `tournament` ROOT (per audit (iv) `@ApplicationModule` Declarations § + E22S08 split precedent).
  - `AudioStorageService` from `de.vvwt.tm.timer.audio` (per E26S02 — same module, public sub-package).
  - `@Service`. `buildTimerData` annotated as appropriate (read-only logic).

### Domain-exception authoring

- [ ] **AC-RED-FIRST-INVALID-TIMER-URL-EXCEPTION:** New `de.vvwt.tm.timer.InvalidTimerUrlException` authored RED-first as public class extending `RuntimeException`. UUID-argument constructor preserved per C-3: `new InvalidTimerUrlException(UUID tournamentId)` builds appropriate message. Caught cross-module by `web.GlobalExceptionHandler.handleInvalidTimerUrl` (E26S03 updates the import).
- [ ] **AC-RED-FIRST-NO-ACTIVE-TOURNAMENT-EXCEPTION:** New `de.vvwt.tm.timer.NoActiveTournamentException` authored RED-first as public class extending `RuntimeException`. (UUID, String) constructor preserved per C-3: `new NoActiveTournamentException(UUID tournamentId, String status)` builds appropriate message. Caught cross-module by `web.GlobalExceptionHandler.handleNoActiveTournament` (E26S03 updates the import).

### Domain-enum authoring

- [ ] **AC-RED-FIRST-TIMER-BREAK-TYPE:** New `de.vvwt.tm.timer.TimerBreakType` enum authored RED-first. Two public values preserved per C-3: `REGULAR`, `ADDITIONAL`. Used by `TimerScheduleEntryResponse` factory methods (also authored by S01 — see below).

### Query-shape records authoring (Pattern A per DEC-40 §2026-04-27 Clarification)

- [ ] **AC-RED-FIRST-TIMER-DATA-RESPONSE:** New `de.vvwt.tm.timer.TimerDataResponse` Java record authored RED-first at the timer module ROOT (public). **11 fields preserved verbatim** per C-3 + C-8 (JSON wire-format parity): `tournamentName`, `tournamentId`, `tenantId`, `tournamentStatus`, `currentPhaseNumber`, `currentLapNumber`, `hasStartTime`, `emptySchedule`, `schedule` (`List<TimerScheduleEntryResponse>`), `phases` (`List<TimerPhaseResponse>`), `audio` (`TimerAudioResponse`). Pattern A justification (DEC-40 Decision Rule §289-296): projection == wire shape; no Clause B (a)/(c)/(d) condition fires. Service `TimerDataService.buildTimerData(UUID)` returns this record directly; controller (E26S03 `TimerController.getTimerData`) serializes via Jackson without web-tier mapping.
- [ ] **AC-RED-FIRST-TIMER-PHASE-RESPONSE:** New `de.vvwt.tm.timer.TimerPhaseResponse` Java record authored RED-first at timer module ROOT (public). Field set preserved per audit (iv) (`sequenceNumber`, `description`, `status`, `lapCount` per legacy `infrastructure/web/timer/dto/TimerPhaseResponse.java` line 191-194). Used as nested element of `TimerDataResponse.phases`.
- [ ] **AC-RED-FIRST-TIMER-SCHEDULE-ENTRY-RESPONSE:** New `de.vvwt.tm.timer.TimerScheduleEntryResponse` Java record authored RED-first at timer module ROOT (public) with two static factory methods preserved verbatim per C-3:
  - `round(int phase, int lap, String start, String end)` — schedule entry for a round.
  - `breakEntry(String type, String label, String start, String end)` — schedule entry for a break.
  - Type field uses `TimerBreakType.name()` strings (`"REGULAR"` / `"ADDITIONAL"`) for break entries; null/empty for round entries.
  - Used as nested element of `TimerDataResponse.schedule`.
- [ ] **AC-RED-FIRST-TIMER-AUDIO-RESPONSE:** New `de.vvwt.tm.timer.TimerAudioResponse` Java record authored RED-first at timer module ROOT (public). 3 fields per audit (iv): `startUrl`, `endUrl`, `pauseUrl` (each `String`, nullable; null when no audio file uploaded for that category). Used as `TimerDataResponse.audio` field; URL strings constructed by `DefaultTimerDataService` per Wave-2-aligned scheme `/api/audio/tournaments/{tournamentId}/{category}/stream` (verified at AC-AUDIO-URL-CONSTRUCTION-WAVE2 below).
- [ ] **AC-PATTERN-A-PLACEMENT-JUSTIFICATION:** Story impl-report cites DEC-40 §2026-04-27 Clarification Decision Rule §289-296 verbatim for each of the 4 records, confirming all satisfy "projection == wire shape; no Clause B (a)/(c)/(d) condition fires" → Pattern A. Cross-references E25S01 escalation (outer-repo commit `23d947b`) as the architectural-lesson precedent. **NO web-tier wrapper DTOs authored by E26S01, E26S02, or E26S03 for these 4 records** (Pattern E carve-out N/A).

### Modulith module declaration

- [ ] **AC-RED-FIRST-PACKAGE-INFO:** New `vvwt-tm-web/src/main/java/de/vvwt/tm/timer/package-info.java` authored RED-first; declares `@org.springframework.modulith.ApplicationModule`. ApplicationModulesTest verifies post-S01 GREEN.
- [ ] **AC-TIMER-ALLOWED-DEPS-TOURNAMENT-ONLY:** Initial `allowedDependencies` array contains `{"tournament"}` (1 entry). Per Brief v1.1 D-7 + Cycle-1 reviewer F#3 OMIT-IF-UNUSED:
  - **Empirical compile-attempt requirement:** Delivery MUST grep the new `DefaultTimerDataService` source for `de.vvwt.tm.tournament.exceptions.*` references AT RED-first authoring.
  - **If ZERO references found** (expected per audit (iv) empirical inspection of legacy at line 1-29 — no tournament.exceptions imports): omit `"tournament::exceptions"` from `allowedDependencies`.
  - **If references found** (unexpected): add `"tournament::exceptions"` and document the necessity in impl-report.
  - The omit-default avoids allowedDependencies-bloat per DEC-40 "per-entry justification (mandatory)" principle.
- [ ] **AC-TIMER-AUDIO-SUB-PACKAGE-NOT-IN-DEPS:** `timer.audio` is a public sub-package of timer per Brief v1.1 D-9 (option A). It does NOT appear as a separate entry in `timer/allowedDependencies` (sub-packages of own module are accessible by definition).

### Tournament-types empirical reference (audit (iv) §`@ApplicationModule` Declarations consumer-list)

- [ ] **AC-TOURNAMENT-TYPES-EMPIRICAL-CHECK:** At RED-first authoring of `DefaultTimerDataService` constructor + `buildTimerData` body, Delivery verifies all consumed `tournament.*` types resolve to `tournament` ROOT (NOT `tournament.internal.*`). Audit (iv) lists 13 candidate types; expected ROOT placement (`Tournament`, `Phase`, `Match`, `PhaseBreak`, `PhaseBreakConfig`, `PhaseConfig`, `TournamentRepository`, `PhaseRepository`, `MatchRepository`, `PhaseBreakRepository`, `TimelineCalculationService`, `TimelineEntry`, `TimelineEntryType`). If any type is at `tournament.internal.*`, STOP-and-escalate per C-3 + base.rules.md (Modulith boundary violation candidate; resolved either by adding to a `tournament` named-interface or by Discovery-decision).

### Service-method-coverage RED-first authoring (per audit (v) Snapshot-Driven verdict + DEC-41 §3 hierarchy)

- [ ] **AC-BUILD-TIMER-DATA-SUCCESS-PATH:** RED-first test for `buildTimerData(tournamentId)` with valid ACTIVE/PLANNED/COMPLETED tournament + phases + matches + phase-breaks + audio files + start-time → returns `TimerDataResponse` with all 11 fields populated (tournamentName, tournamentId, tenantId, tournamentStatus, currentPhaseNumber, currentLapNumber, hasStartTime, emptySchedule=false, schedule list, phases list, audio).
- [ ] **AC-BUILD-TIMER-DATA-EMPTY-SCHEDULE:** RED-first test for tournament with NO phases configured → returns `TimerDataResponse` with `emptySchedule=true`, empty `schedule` + `phases` lists, audio still present (per legacy AC7).
- [ ] **AC-BUILD-TIMER-DATA-INVALID-URL:** RED-first test for unknown `tournamentId` → throws `InvalidTimerUrlException` per legacy line 129. Cross-module catch by `web.GlobalExceptionHandler` (E26S03 sub-test) → 404.
- [ ] **AC-BUILD-TIMER-DATA-DRAFT-STATUS:** RED-first test for tournament with `status="DRAFT"` → throws `NoActiveTournamentException(tournamentId, "DRAFT")` per legacy line 133. Cross-module catch → 404.
- [ ] **AC-BUILD-TIMER-DATA-CANCELLED-STATUS:** RED-first test for tournament with `status="CANCELLED"` → throws `NoActiveTournamentException(tournamentId, "CANCELLED")`. Cross-module catch → 404.
- [ ] **AC-TIMER-ACCESSIBLE-STATUSES-INVARIANT:** RED-first test verifying the 3 timer-accessible statuses (PLANNED, ACTIVE, COMPLETED) per legacy line 83 — all permit access; any other status throws `NoActiveTournamentException`.
- [ ] **AC-BREAK-TYPE-MAPPING:** RED-first test verifying the timeline → break-type mapping per legacy line 50-53 + 279-306: `LAP_BREAK → REGULAR`, `INTRA_PHASE_BREAK → ADDITIONAL`, `SECTION_BREAK → ADDITIONAL`, `MATCH_ROUND` (lapNumber>0) → round entry. Authored as `@ParameterizedTest` over enum values per DEC-41 §1(d) alignment guidance (qualified algebraic invariant; quantified body).
- [ ] **AC-AUDIO-URL-CONSTRUCTION-WAVE2:** RED-first test verifying `buildAudioUrl` constructs Wave-2-aligned URL `/api/audio/tournaments/{tournamentId}/{category}/stream` (per Brief v1.1 D-10 + audit (iv) URL-Schema table). Replaces legacy line-430 pattern `"/api/tournaments/" + tournamentId + "/audio/" + category.name() + "/stream"` with `"/api/audio/tournaments/" + tournamentId + "/" + category.name() + "/stream"`.

### DEC-22 Iron Law + DEC-41 hierarchy

- [ ] **AC-DEC22-IRON-LAW-RED-EVIDENCE:** Impl-report enumerates each new test method's RED-state commit hash + GREEN-state commit hash; reviewer-verifiable per E36/E25 precedent.
- [ ] **AC-DEC41-FRESH-AUTHORING-NO-LEGACY-REUSE:** No reuse of the 28 Snapshot-Driven legacy tests (per audit (v) Aggregate Verdict + DEC-41 §3 hierarchy item 1). Impl-report cites `E26-AUDIT-DEC41-TEST-CLASSIFICATION` (this Discovery commit) verbatim per DEC-41 §4 audit obligation.
- [ ] **AC-Q7-METHOD-COUNT-RECONFIRMATION:** Delivery counts `@Test` in fresh `DefaultTimerDataServiceTest` (same-package `de.vvwt.tm.timer.internal` per DEC-36 white-box-allowed). Audit (v) baseline 9 (legacy `TimerControllerIT` `@Test` count covering service-layer behavior derivable from controller IT ACs) is reference floor; concrete number determined by Delivery's RED-first authoring + impl-report justification.

### DEC-35 + DEC-36 package layout + cross-package test typing

- [ ] **AC-DEC35-INTERFACE-IN-ROOT:** `TimerDataService` interface in `de.vvwt.tm.timer` root; `DefaultTimerDataService` impl in `de.vvwt.tm.timer.internal`. Naming canon: no `I`-prefix.
- [ ] **AC-DEC36-CROSS-PACKAGE-TEST-TYPING:** Any cross-package test (e.g., E26S03's `TimerControllerIT` referencing the service via `@MockitoBean TimerDataService` from web test pkg) MUST type-reference the interface FQN `de.vvwt.tm.timer.TimerDataService`, never `DefaultTimerDataService` impl class. Same-package tests in `de.vvwt.tm.timer.internal` MAY white-box.

### Cross-cutting

- [ ] **AC-MVN-VERIFY-GREEN:** `mvn verify` GREEN on staging post-merge.
- [ ] **AC-APPLICATIONMODULESTEST-GREEN:** Post-S01 commit, `ApplicationModulesTest` GREEN per DEC-21 invariant. New `timer` module + its `allowedDependencies` recognized by Spring Modulith.
- [ ] **AC-DEC31-PROPAGATE-GOVERNANCE:** Per DEC-31 §(c) two-commit contract at story closure via `coordinate-handoffs` → `propagate-governance` skill.
- [ ] **AC-DELIVERY-MODE-SOURCE:** Story declares `delivery_mode: source` (modifies production-code under `vvwt-tm-web/src/main/java/`); triggers DEC-31 propagate-governance per §(g).
- [ ] **AC-DEC32-NOT-INVOKED:** No FQN-rewrite consumer-pattern usage in this story. New timer root-package types created at canonical FQN; no existing-consumer-FQN-substitution. DEC-32 invocation count remains #1 (E21S13).
- [ ] **AC-DEC22-REFACTOR-CLAUSE-N/A:** Per `feedback_dec22_refactor_phase_first.md`: §refactor-clause is phase 3 of RED-GREEN-REFACTOR on TDD-authored code. E26S01 authors TDD-fresh code; refactor-clause N/A here. No legacy code modified by S01 (legacy domain.timer/* DELETED per AC-DELETE-LEGACY-FIRST).
- [ ] **AC-DEC45-D2-FIRM-CONVERSION-RECORDED:** Story impl-report cites `E26-AUDIT-WEBSOCKET-COUPLING` audit verbatim — DEC-45 D2 PROVISIONAL → FIRM conversion is recorded at Discovery time (Brief v1.1 C-15); E26S01 confirms the conversion is operational at this story closure.

---

## Out of Scope

- `timer.audio` sub-package (interface + impl + 5 supporting types incl. `AudioFileMetadata` Pattern A consolidation absorbing legacy `AudioMetadataResponse`) — **E26S02 scope**.
- Web controllers (3) + `web/package-info.java` allowedDependencies + `web/GlobalExceptionHandler` import-line updates + `auth.internal.SecurityConfig` URL-pattern updates + Svelte URL-string updates + fresh ITs — **E26S03 scope**. **NO web-tier DTOs authored by E26S03 per Pattern A** (E26S03 was originally scoped to author 5 records at `web.internal.dto.*` per Brief v1.1; that scope is REVOKED post-DEC-40 §2026-04-27 Clarification — 4 timer records authored here at S01, 1 audio record authored at S02, S03 strictly controllers + cross-cutting edits + ITs).
- Javadoc-historical-provenance sweep + Timer-track Success Metrics + propagate-governance commits beyond DEC-31 §(c) — **E26S04 scope**.
- WebSocket Java code — timer has zero (audit (vi)); WS plumbing in `tournament.internal.web` (already E21-reconstructed) is OUT-of-scope.
- DEC-44 IT-annotation retrofit — operationalized by E39S01 (done).
- L2.5 examination at `web` Trigger-α firing — pre-approved per DEC-45 D2 (E26 unconditional FIRM verdict).

---

## Dependencies

- **E36S08** — `web/GlobalExceptionHandler` post-E36S08 is at `de.vvwt.tm.web` and currently catches `domain.timer.{InvalidTimerUrlException,NoActiveTournamentException}`. E26S01's authoring of `de.vvwt.tm.timer.{InvalidTimerUrlException,NoActiveTournamentException}` enables E26S03's import-line update.
- **E39S01** — DEC-44 D1+D2 framework + WebModuleTestConfig post-E39S01 state are needed by E26S03 (which authors web-module ITs); E26S01 does NOT author web-module ITs but is declared as dep for trace-clarity (Brief v2.1 D-15 reviewer F#2 option A precedent).

---

## Notes

**Legacy file inventory (audit (iv)) for E26S01 deletion scope:**

- `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/TimerDataService.java` — 446 LOC; 1 public method + 6-arg constructor + complex assembly logic.
- `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/InvalidTimerUrlException.java` — 20 LOC; UUID-argument constructor.
- `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/NoActiveTournamentException.java` — 20 LOC; (UUID, String) constructor.
- `vvwt-tm-web/src/main/java/de/vvwt/tm/domain/timer/TimerBreakType.java` — 15 LOC; 2 enum values.
- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerDataResponse.java` — 207 LOC (Pattern A relocation target).
- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerPhaseResponse.java` — 68 LOC (Pattern A relocation target).
- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerScheduleEntryResponse.java` — 175 LOC (Pattern A relocation target).
- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/web/timer/dto/TimerAudioResponse.java` — 67 LOC (Pattern A relocation target).

**Estimated LOC:**

- Production: ~1000 LOC (interface + impl + 2 exceptions + enum + 4 records + package-info; the 4 records contribute ~520 LOC of the legacy footprint, fresh-authored may compress modestly).
- Test: ≥ legacy floor (audit (v) baseline ~9-15 service-method-equivalent test cases derived from `TimerControllerIT` ACs + record-equality / factory-method tests for the 4 records; concrete count by Delivery RED-first).
- Total: ~1000 + ~1000 LOC ≈ 2000 LOC across ~10 files.

**Pre-identified C-3 escalation candidates (audit (iv) + Brief v1.1 C-3):**

- 6-arg constructor of `DefaultTimerDataService` (parameter type/order; consumer compile-stable).
- 1 public method signature of `TimerDataService` interface.
- `InvalidTimerUrlException` UUID constructor.
- `NoActiveTournamentException` (UUID, String) constructor.
- `TimerBreakType` enum value set (REGULAR, ADDITIONAL).
- 4 query-shape record component sets (11 + 4 + variable + 3 fields) + `TimerScheduleEntryResponse` factory method signatures (`round`, `breakEntry`).

If RED-first authoring surfaces a need to deviate (e.g., constructor injection of a different service type, additional interface method, additional enum value, additional record field, OR a record satisfying Clause B (a)/(c)/(d) for Pattern E split), Delivery STOPS and escalates per C-3 + base.rules.md "Conflict & Escalation Protocol".

**Pattern A justification per record (DEC-40 §2026-04-27 Clarification Decision Rule §289-296):**

| Record | Projection | Wire shape | Match? | Clause B (a)/(c)/(d) fires? | Pattern |
|---|---|---|---|---|---|
| `TimerDataResponse` | 11 fields built by `DefaultTimerDataService.buildTimerData` | 11 fields serialized to JSON for SPA | Yes | No | A |
| `TimerPhaseResponse` | 4 fields per phase | 4 fields serialized as nested JSON | Yes | No | A |
| `TimerScheduleEntryResponse` | factory-method-built variable shape (round vs break) | same shape serialized to JSON | Yes | No | A |
| `TimerAudioResponse` | 3 nullable URL strings | 3 nullable URL strings serialized to JSON | Yes | No | A |

All 4 satisfy Pattern A (placement at bounded-context module root). NO Pattern E split required.

**TDD-provenance verified for all referenced TDD-authored cross-cutting code** per Brief v1.1 D-12+D-13 + Cycle-1 reviewer F#4: `web/GlobalExceptionHandler` is E36S08-Q-1a; `auth.internal.SecurityConfig` is E15S07-Q-1a. E26S01 authoring is independent (no edits to those files; that's E26S03 scope). E26S01 authors fresh `timer.*` code only (incl. the 4 Pattern A records relocated from legacy `infrastructure/web/timer/dto/*`).

**E25S01 architectural-lesson precedent:** E25S01 first-attempt delivery (outer-repo commit `23d947b`) escalated due to Modulith cycle when 3 display query-shape records were placed at `de.vvwt.tm.web.internal.dto.*` per Brief v2.2 literal Clause B reading. The records were also referenced cross-module by `display.*` services (return types of public service methods), which (1) requires forbidden import of `web.internal.*` from `display`, and (2) creates a `display↔web` cycle rejected by `ApplicationModules.verify()`. DEC-40 §2026-04-27 Clarification was authored to formalize Pattern A as the canonical resolution. E26S01 applies the lesson prospectively at Discovery time — preventing a re-occurrence of the same escalation pattern at E26S03 delivery time.
