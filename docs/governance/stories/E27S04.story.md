<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E27S04.story.md at 53a6f2d1a6b1db008b87909560a47963635d3d0c 2026-04-28 -->
---
type: artefact
artefact_type: story
id: E27S04
epic: E27
related_backlog_id: E27S04
kind: cleanup
mandatory_ac_categories: [error-handling, testing, governance, security]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-28-e27-slotopt-integration-completion
related_decs: [DEC-13, DEC-21, DEC-22, DEC-23, DEC-31, DEC-49]
created_at: 2026-04-28
updated_at: 2026-04-28
---

# Story E27S04: Cutover Housekeeping (Cutover-1 of E27) — Operator Documentation + Javadoc Sweep + DEC-31 Propagate-Governance

## User Story

**As a** Tournament Manager operator deploying the slot-optimization integration,
**I want** clear operator-facing documentation describing the three routing legs, threshold semantic, configuration keys, admin-cancel + Best-So-Far semantics, and the in-memory tracking durability caveat,
**so that** I can configure and operate the system correctly without reading source code or DEC files.

---

## Context

E27S01–S03 landed all production code for the slot-opt integration (Modulith registration + Leg 1 + DEC-49 + Leg 3 cancelable in-process + admin-cancel REST + minimal Svelte UI + Leg 2 dispatcher HTTP + reachability + DEC-43 D3 admin-warning event-publication channel + Maven Enforcer DEC-11 boundary). This Cutover-1 housekeeping story closes the epic per E25S03/E26S04 pattern:

1. **Operator-facing documentation** at `vvwt-prj/vvwt-tm-web/docs/slot-optimization-routing.md` — explains the three legs, threshold (rounds), config keys (`tm.slotopt.dispatcher.url`, `tm.slotopt.exhaustive-max-n`, `tm.slotopt.dispatcher.reachability-timeout-ms`), admin-cancel + Best-So-Far semantics, Brief T-6 in-memory durability caveat (TM restart loses in-flight optimization).
2. **Javadoc-historical-provenance sweep** — grep-based audit at delivery time per E25S03/E26S04 precedent.
3. **DEC-31 propagate-governance two-commit** per §(c) — chore(E27S04) deletion-and-cleanup commit + docs(governance) propagating DEC-49 + updated `patterns/conventions.md` snapshot to `vvwt-prj/docs/governance/`.
4. **Optional UOE-branch cleanup** at `DirectSlotOptimizationClient.java:184-193` — story-author judgment per Brief T-1 LOSE-(c).
5. **E27 Success Metrics verification** per the Epic's Success Metrics list.
6. **E01S10 status update** — move from blocked to done (or annotate as superseded by E27).

NO production-code logic changes (other than optional UOE-branch deletion); ONLY documentation + governance commits + status updates.

---

## Acceptance Criteria

### Operator documentation (Brief D-10)

- [ ] **AC-OPERATOR-DOC-AUTHORED [governance]:** A new file `vvwt-prj/vvwt-tm-web/docs/slot-optimization-routing.md` is authored covering ALL of the following sections (story-author may consolidate sections — content matters more than literal section titles):
  1. **Three-leg routing overview** — for each leg: trigger condition, compute path, what the user sees.
  2. **Threshold semantic** — N = rowCount = number of rounds; PacketSolver evaluates N! permutations of round orderings; `tm.slotopt.exhaustive-max-n` (default 10) is the boundary.
  3. **Configuration keys** — all four: `tm.slotopt.exhaustive-max-n`, `tm.slotopt.fallback.field-count` (existing, untouched), `tm.slotopt.dispatcher.url`, `tm.slotopt.dispatcher.reachability-timeout-ms`. For each: default value, format, override mechanism (env var or `application-*.yml`).
  4. **Admin-cancel + Best-So-Far** — how to trigger cancel (UI button + REST endpoint), what cancel returns (Best-So-Far permutation applied to Match entities), per-tournament scope (cancel A doesn't cancel B).
  5. **In-memory durability caveat (per Brief T-6)** — TM restart mid-Leg-3 loses in-flight compute; admin must re-trigger from UI; documented as accepted V1 trade-off.
  6. **Offline-operability statement** — explicit cross-reference to gesamtkonzept §Offline-Strategie + DEC-15 (LAN-local self-host posture); explains that Leg 3 fallback is the operationalization.
  7. **DEC reference list** — DEC-4 V1 + DEC-49 (canonical routing decisions); DEC-11 (boundary); DEC-43 (algorithm-agility wire format); DEC-9 (no UUIDs across boundary).

- [ ] **AC-OPERATOR-DOC-LANGUAGE [governance]:** Document is written in **English** per `patterns/conventions.md` § Artefact Language (operator-facing documentation is artefact, not human-stakeholder planning prose). Per DEC-23 § (b)+(c): paths inside the document point to `vvwt-prj/docs/governance/` (relative paths from doc location); NO outer-repo `.gaai/` paths.

### Javadoc-historical-provenance sweep + Success Metrics

- [ ] **AC-JAVADOC-SWEEP-AUDIT-AT-DELIVERY [governance]:** At delivery time, story-author runs `grep -rn 'DirectSlotOptimizationClient' vvwt-prj/vvwt-tm-web/src/` AND `grep -rn 'FallbackSlotOptimizationClient' vvwt-prj/vvwt-tm-web/src/` AND `grep -rn 'SlotOptimizationClient' vvwt-prj/vvwt-tm-web/src/main/java/` to identify any cross-module Javadoc references that should be updated to mention `RoutingSlotOptimizationClient` as the canonical entry point. Allowlist categories per E25S03/E26S04 precedent: (a) production code, (b) test code, (c) memory artefacts, (d) committed audits, (e) story/epic artefacts, (f) `vvwt-prj/docs/governance/`. Findings enumerated in impl-report; (a) + (b) updates land in this story; (c)–(f) are point-in-time historical records and remain unmodified.

- [ ] **AC-NO-LEGACY-FQN-RESIDUE-POST-S04 [governance]:** Post-S04 grep `grep -rn 'SlotOptimizationClient' vvwt-prj/vvwt-tm-web/src/main/java/de/vvwt/tm/` shows: zero references to `FallbackSlotOptimizationClient` outside its own file's package-private body (Fallback is dead under new wiring per E27S01 + E27S02; Javadoc cross-references in non-Fallback files updated or removed). DirectSlotOptimizationClient references continue to exist (it remains a non-Primary bean); allowlisted.

- [ ] **AC-E27-SUCCESS-METRICS-VERIFIED [testing, governance]:** Impl-report explicitly lists each Success Metric from the E27 Epic and verifies pass/fail:
  1. `de.vvwt.tm.slotopt` declared as Spring Modulith bounded context.
  2. `de.vvwt.tm.slotopt.internal.*` not consumable cross-module.
  3. `web.allowedDependencies` includes `"slotopt"`.
  4. `ApplicationModules.of(...).verify()` GREEN.
  5. RoutingSlotOptimizationClient is `@Primary`; Direct retained as non-Primary; Fallback disabled.
  6. Pre-existing 11 Snapshot-Driven tests GREEN.
  7. All E27-authored production code is Q-1a TDD RED-first (impl-report enumerates RED-state commit hashes from S01-S03 impl-reports).
  8. DEC-49 authored + indexed.
  9. `tm.slotopt.dispatcher.url` + `tm.slotopt.dispatcher.reachability-timeout-ms` config in `application.yml`; existing `tm.slotopt.exhaustive-max-n` unchanged.
  10. N≤10 byte-equivalent to pre-E27.
  11. N>10 + dispatcher reachable: HTTP submit per DEC-11 + DEC-43.
  12. N>10 + dispatcher unreachable: in-process cancelable timeout; admin-cancel returns Best-So-Far + applies via `SlotResultApplicator` in same TX.
  13. Admin-cancel REST endpoints work end-to-end.
  14. Maven Enforcer rule blocks `vvwt-slotopt-dispatcher` from `vvwt-tm-web`.
  15. DEC-43 D2 algorithm field present; DEC-43 D1 algorithm list parsed from registration response; DEC-43 D3 admin-warning event-publication path exists.
  16. E01S10 unblocked (status update in `blocked.backlog.yaml` per AC-E01S10-STATUS-UPDATED).
  17. DEC-31 propagate-governance two-commit at S04 closure.
  18. Operator documentation exists.
  19. `mvn verify` GREEN on staging.

### Optional UOE-branch cleanup (story-author judgment per Brief T-1 LOSE-(c))

- [ ] **AC-UOE-BRANCH-CLEANUP-DECISION [governance]:** Story-author chooses ONE: (a) DELETE `DirectSlotOptimizationClient.java:184-193` UOE branch (now strictly dead code under new wiring) — requires Q-1a TDD new RED-first test asserting "N>threshold via DirectSlotOptimizationClient is unreachable from RoutingSlotOptimizationClient under all routing legs" before deletion (per `feedback_dec22_refactor_phase_first.md`: pre-DEC-22 code is NOT a trustworthy oracle for §refactor-clause; deletion under Q-1a TDD requires fresh RED-first); OR (b) KEEP the UOE branch as documentation of original contract, document explicitly in impl-report that it is dead code under new wiring and is allowlisted. Either choice acceptable; story-author decision documented in impl-report. **Default recommendation: KEEP** to minimize S04 scope (cleanup is optional, can be a future cleanup story).

### `blocked.backlog.yaml` E01S10 status update

- [ ] **AC-E01S10-STATUS-UPDATED [governance]:** `.gaai/project/contexts/backlog/blocked.backlog.yaml` E01S10 entry is updated: status moves from `blocked` to either `done` (if E27S03's Leg 2 is judged equivalent to E01S10's original scope) or `superseded` with `superseded_by: E27S03` annotation. Story-author chooses based on E01S10's original scope versus E27S03's actual delivery. Per generate-stories step 11 backlog-archive rule: if `done`, item must satisfy DEC-7-archiving-protocol (no non-done item depends on it). E01S11 stays `blocked` (per Epic D-6).

### DEC-31 propagate-governance two-commit (per §(c))

- [ ] **AC-DEC31-PROPAGATE-FIRST-COMMIT [governance]:** First commit `chore(E27S04): cleanup + cutover housekeeping` contains: operator documentation (AC-OPERATOR-DOC-AUTHORED), Javadoc-sweep updates (AC-JAVADOC-SWEEP-AUDIT-AT-DELIVERY findings), Success Metrics verification artefact (impl-report), optional UOE-branch cleanup decision (AC-UOE-BRANCH-CLEANUP-DECISION), `blocked.backlog.yaml` E01S10 update.

- [ ] **AC-DEC31-PROPAGATE-SECOND-COMMIT [governance]:** Second commit `docs(governance): propagate E27 snapshot per DEC-31` propagates the following to `vvwt-prj/docs/governance/`:
  - DEC-49 (NEW; per §(b)+(c) — never previously propagated).
  - Updated `patterns/conventions.md` snapshot (per §(f) byte-difference — `patterns/conventions.md` was updated by E27S01 with the Slot-Opt subsection; this is the first propagation since that update).
  - DEC-4 (re-snapshot per §(f) byte-difference — DEC-4's frontmatter `last_updated_at` advanced to 2026-04-28 with `amended_by: [DEC-49]` added, plus pointer paragraph; mandatory re-snapshot).
  - Story snapshots E27S01–E27S04 (additive per §(g) attribution prereq).
  - Epic snapshot E27 (additive per §(g)).

- [ ] **AC-DEC31-OUTER-REPO-FIRST [governance]:** Outer-repo (`/home/vvw/NetBeansProjects/`) commit precedes inner-repo (`/home/vvw/NetBeansProjects/vvwt-prj/`) commits per DEC-31 §(h) attribution prereq. Outer-repo commit contains: backlog status update (active.backlog.yaml: E27S04 done; blocked.backlog.yaml: E01S10 update), epic + story artefacts, DEC-49 file, memory/index.md update, patterns/conventions.md update.

### Iron Law N/A + S04 No-New-Surface Invariants (mandatory category coverage for cleanup stories)

- [ ] **AC-IRON-LAW-NA-FOR-DOC-AND-CONFIG [governance]:** Per DEC-22 documentation note + `feedback_dec22_refactor_phase_first.md` analogue: documentation, governance commits, and Javadoc-only edits are NOT new production code; Iron Law does not apply. The optional UOE-branch cleanup (AC-UOE-BRANCH-CLEANUP-DECISION) IS production code per its own AC requirement (RED-first new test). Other than that, no new RED-first tests are required by S04.

- [ ] **AC-NO-NEW-ERROR-PATHS-INTRODUCED [error-handling]:** S04 introduces ZERO new exception types, ZERO new `try/catch` blocks, ZERO new error-mapping branches in production `*.java` code. Verified by `git diff` against the merge-base of the story branch: the only `*.java` modifications are (a) Javadoc-only edits per AC-JAVADOC-SWEEP-AUDIT-AT-DELIVERY (no executable code lines changed), and (b) the optional UOE-branch cleanup per AC-UOE-BRANCH-CLEANUP-DECISION if exercised — and that cleanup is a pure DELETION of dead code (UOE-throwing branch becomes unreachable post-S01+S02; no NEW error path). All pre-existing error-handling behavior preserved end-to-end (verified via AC-MVN-VERIFY-GREEN — pre-existing 11 Snapshot-Driven tests + S01-S03 tests including all error-path assertions remain GREEN).

- [ ] **AC-NO-NEW-SECURITY-SURFACES-INTRODUCED [security]:** S04 introduces ZERO new authentication endpoints, ZERO new authorization checks, ZERO new cryptographic operations, ZERO new wire-protocol fields, ZERO new credential-handling code. The DEC-49 governance propagation (AC-DEC31-PROPAGATE-SECOND-COMMIT) carries the DEC-43 D3 admin-warning channel obligation that was IMPLEMENTED at S03 (event-publication code) — S04 does NOT introduce any new DEC-43 wire-format code or cryptographic surface. Maven Enforcer rule (DEC-11 boundary) was authored at S03 — S04 does NOT modify it. The Pattern C strict-additive doctrine continues to hold for security-relevant code: existing `de.vvwt.tm.slotopt.*` byte-equivalent; no new security entry points beyond the S02 admin-cancel REST + S03 dispatcher HTTP client (both authored at their respective stories under their own security ACs).

- [ ] **AC-MVN-VERIFY-GREEN [testing]:** `mvn verify` GREEN on staging post-S04 commits. ApplicationModulesTest GREEN. All pre-existing tests + S01-S03 tests stay green.

- [ ] **AC-NO-PRODUCTION-LOGIC-CHANGES [governance]:** Other than the optional UOE-branch cleanup (story-author judgment), `git diff` shows ONLY: documentation files, Javadoc comments, governance propagation files, backlog YAML updates, memory/index.md, patterns/conventions.md, propagated snapshots in vvwt-prj/docs/governance/. No new `*.java` source files; no modifications to `*.java` source files (other than Javadoc-only edits + optional UOE-branch cleanup).

---

## Out of Scope

- **New production-logic features** (Leg 2/3 logic, additional routing legs) — all production logic landed in S01-S03.
- **DEC-43 D3 admin-UI banner consumer** — separate cleanup story (per E27S03 AC-DEC43-D3-NO-ADMIN-UI-CONSUMER-IN-S03).
- **Persistence of in-flight job state across TM restarts** — accepted V1 trade-off per Brief D-11 + T-6.
- **Per-tenant or per-location dispatcher routing** — Brief D-12.
- **E01S11 unblocking** — stays blocked per Brief D-6 + S-4.
- **Wave-3 epics** — separate Discovery sessions.

---

## Dependencies

- **E27S01 + E27S02 + E27S03** — all three production-code stories must be done.
- **Session Brief:** `discovery-2026-04-28-e27-slotopt-integration-completion`.

---

## Notes

**E27 Epic closure:** post-S04 merge + governance-propagation, Epic E27 is done. Wave-2 trajectory (E25 + E26 + E27 per DEC-45 D2) closes. Future Wave-3 epics open under fresh `/gaai-discover` sessions.

**DEC-31 §(f) re-snapshot of DEC-4:** DEC-4 was last propagated to `vvwt-prj/docs/governance/` in earlier waves (Wave-1 batch). E27S04 advances DEC-4's `last_updated_at` to 2026-04-28 (frontmatter only — textual base unchanged per delta-pattern), making §(f) byte-difference re-snapshot mandatory. Hedged in AC-DEC31-PROPAGATE-SECOND-COMMIT.

**Operator documentation language:** English per `patterns/conventions.md` § Artefact Language. The `gesamtkonzept.md` precedent (German, planning prose for human stakeholders) does NOT extend to operator-facing technical documentation in `vvwt-prj/vvwt-tm-web/docs/`. Per DEC-23 + `patterns/conventions.md`: code, commits, backlog, decisions, stories, AND operator/technical docs in `vvwt-prj/` are English; only conversation with humans + planning prose in `planung/` may be German.

**Optional UOE-branch cleanup default:** Recommended KEEP at S04 (per AC default) to keep S04 scope tight on documentation + propagation. The UOE branch is dead code but harmless; future cleanup story can address it (or it lives forever — minimal cost). DELETION at S04 would expand scope to include a Q-1a TDD test (new code under Iron Law); this is not a S04 closing-housekeeping shape.

**Daemon-evidence-gate awareness (per DEC-47):** S04 produces impl-report + qa-report on disk per DEC-27 Post-Delivery Report-Pflicht. Daemon evidence-gate (DEC-47 Clause A) verifies these exist before transitioning S04 to `done`. Standard close-story.sh push-serialization per DEC-47 Clause D applies.
