<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E25S01.story.md at e84706b78711e3ab44cb99c01ed335b7cd68df1c 2026-04-27 -->
---
type: artefact
artefact_type: story
id: E25S01
related_backlog_id: E25S01
track: discovery
created_at: 2026-04-27
updated_at: 2026-04-27
mandatory_ac_categories: [error-handling, testing, governance, security]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-27-e25s01-escalation-resolution
related_decs: [DEC-13, DEC-21, DEC-22, DEC-24, DEC-31, DEC-35, DEC-36, DEC-40, DEC-41, DEC-44, DEC-45]
delivery_mode: source
---

# Story: Display domain Q-1a TDD rebuild — `DisplayOverviewService` interface + `DefaultDisplayOverviewService` impl + `NoActivePhaseException` + 3 query-shape records (`DisplayPhaseOverviewResponse`, `DisplayMatchesResponse`, `DisplayGroupStandingsResponse`) + `display/package-info.java` (D-7 γ delete-then-author-fresh at canonical FQN `de.vvwt.tm.display.*`)

## User Story

**As a** Tournament Manager Delivery Agent operating under DEC-21/-22/-44/-45 + Brief v2.2 governance,
**I want** to delete legacy `infrastructure.display.{DisplayOverviewService,NoActivePhaseException}` and author fresh Q-1a TDD RED-first replacements at canonical FQN `de.vvwt.tm.display.*` (interface in root, impl in `.internal`, exception in root) within ONE feature-branch lifecycle per D-7 Coexistence Option γ,
**so that** the `display` bounded-context joins the Spring Modulith module set with DEC-22 Iron Law TDD compliance + DEC-35 package-layout discipline + DEC-36 cross-package test typing + canonical 8-arg constructor preserved per C-3 signature-preservation + bounded-context-owned query-shape records authored at the module root per DEC-40 §2026-04-27 Clarification (Pattern A).

---

## Context

Per audit (ii) (commit `1aded97`): all 25 in-scope display tests are Snapshot-Driven; ZERO reusable as Contract Tests per DEC-41 §1. Per audit (i): legacy `DisplayOverviewService` is 653 LOC orchestrator with 3 public methods + 8-arg constructor + 4 UnauthorizedException-branch token-validation + read-only `@Transactional`. Per `feedback_dec22_refactor_phase_first.md`: legacy code is NOT a trustworthy oracle for §refactor-clause; fresh Q-1a TDD RED-first authoring is the only DEC-22-compliant path.

Per DEC-45 D2 (Wave-2 architecture review verdict, 2026-04-26): E25 has unconditional L2-stays pre-approval — no Trigger-(i)/(ii) firing for display per audit-i+ii+iii.

**Resumption note (2026-04-27):** This story was previously delivered under Brief v2.2 (`discovery-2026-04-27-e25-display-resumption`) and escalated by Delivery (outer-repo commit `23d947b`) due to a Modulith cycle: Brief v2.2 placed the 3 display REST DTOs at `de.vvwt.tm.web.internal.dto.*` per DEC-40 Clause B literal reading, which created `display→web.internal` cross-module access (forbidden under Modulith `.internal` privacy) AND a `display↔web` cycle (rejected by `ApplicationModules.verify()`). Discovery resumed 2026-04-27 with Brief `discovery-2026-04-27-e25s01-escalation-resolution` (Tier-2 Reviewer cycle-1 FAIL → cycle-2 PASS; human-validated). Resolution = Option A: 3 records authored at `de.vvwt.tm.display.*` (root, public) per DEC-40 §2026-04-27 Clarification (Pattern A — bounded-context-owned query-shape records). DEC-40 was textually clarified inline (no new DEC-49). The 3 record-authoring ACs (previously in E25S02) are RELOCATED into THIS story per compile-graph order (interface return types) + story cohesion (records belong to the display module).

This story authors the display **domain** (service + exception + 3 records + package-info). The web-side controllers + GlobalExceptionHandler import-line update + fresh ITs are E25S02 scope. Cutover-housekeeping is E25S03 scope.

---

## Acceptance Criteria

### Pre-flight: legacy deletion

- [ ] **AC-DELETE-LEGACY-FIRST:** Single commit on the story branch deletes the following files BEFORE any RED-first authoring of replacements (RED state visible per D-7 γ + DEC-22 Iron Law):
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/DisplayOverviewService.java` (653 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/NoActivePhaseException.java` (20 LOC)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/display/` test directory contents that test the above two production files (the legacy `DisplayOverviewControllerIT` and `DisplayViewControllerIT` test the controllers — those are E25S02 scope; deletion of those two test files is E25S02 AC-DELETE-LEGACY-FIRST). E25S01 deletes ONLY the legacy production-code files for the domain (no per-service test file existed in legacy under `infrastructure/display/` other than the controllers' ITs which are E25S02 scope). **Legacy DTOs at `infrastructure/display/dto/*` (DisplayPhaseOverviewResponse + DisplayMatchesResponse + DisplayGroupStandingsResponse) are deleted in E25S02 alongside controller deletion (legacy and new are distinct artefacts; legacy deletion happens at S02 cutover commit). The 3 NEW records are AUTHORED at `de.vvwt.tm.display.*` (root, public) IN THIS STORY per Brief `discovery-2026-04-27-e25s01-escalation-resolution` D-1 + D-3 + DEC-40 §2026-04-27 Clarification (Pattern A — bounded-context-owned query-shape records). The prior cycle-1 F#3 carry-forward statement (which placed records at `web.internal.dto.*` per DEC-40 Clause B literal reading) is SUPERSEDED by this Brief and by the DEC-40 inline clarification. See AC-RED-FIRST-DISPLAY-PHASE-OVERVIEW-RESPONSE / -MATCHES-RESPONSE / -GROUP-STANDINGS-RESPONSE below for the new authoring scope.**
- [ ] **AC-NO-LEGACY-CODE-REFERENCE:** Per Brief v2.2 C-17: legacy `infrastructure.display.*` source MUST NOT be inspected during RED-first authoring beyond audit-i empirical inventory (already captured in commit `1aded97`). Authoring proceeds from Brief + audits + DEC + URL/JSON-wire-contracts only.

### Q-1a TDD RED-first authoring

- [ ] **AC-RED-FIRST-DISPLAY-OVERVIEW-SERVICE-INTERFACE:** New `de.vvwt.tm.display.DisplayOverviewService` interface authored RED-first. Three public method signatures preserved per C-3 signature-preservation:
  - `DisplayPhaseOverviewResponse getPhaseOverview(String deviceToken)`
  - `DisplayMatchesResponse getMatchesByLap(String deviceToken, Integer lap)` (Integer nullable; null = current lap)
  - `DisplayGroupStandingsResponse getGroupStandings(String deviceToken)`
  - DTOs reside at `de.vvwt.tm.display.*` (root, public) per DEC-40 §2026-04-27 Clarification (Pattern A — bounded-context-owned query-shape records). E25S01 authors the records per AC-RED-FIRST-DISPLAY-*-RESPONSE below; E25S02's controller consumes them cross-module via the existing `web→display` allowedDependency (added by E25S02 AC-WEB-ALLOWED-DEPS-EXPANSION).
- [ ] **AC-RED-FIRST-DEFAULT-DISPLAY-OVERVIEW-SERVICE:** New `de.vvwt.tm.display.internal.DefaultDisplayOverviewService` impl class implements `DisplayOverviewService`; authored RED-first. 8-arg constructor signature preserved verbatim per C-3:
  ```
  DefaultDisplayOverviewService(
      DeviceRepository, TournamentRepository, PhaseRepository, MatchRepository,
      TeamAvatarRepository, TeamAvatarRatingRepository, TeamRepository, SetResultRepository)
  ```
  - All 8 are tournament-root-package types per audit (i) `@ApplicationModule` Declarations § (empirically verified all in `tournament` ROOT, not `internal`).
  - `@Service`, read-only `@Transactional` on each public method.

### Display-record Q-1a authoring (per DEC-40 §2026-04-27 Clarification — Pattern A)

The 3 records are authored at `de.vvwt.tm.display.*` (root, public) — NOT at `de.vvwt.tm.web.internal.dto.*` (which would create `display→web.internal` cross-module access + `display↔web` cycle, both rejected by `ApplicationModules.verify()`). Per existing E25S01 plan §step 4 precedent + DEC-22 records-no-logic precedent + DEC-35 mute-on-records spirit, records require only constructor smoke tests (instantiate with all fields populated; verify accessor return values; verify `equals` contract for two records with identical fields) — no full RED-first cycle on the auto-generated `equals`/`hashCode`/`toString` machinery.

- [ ] **AC-RED-FIRST-DISPLAY-PHASE-OVERVIEW-RESPONSE:** New `de.vvwt.tm.display.DisplayPhaseOverviewResponse` Java record authored RED-first (constructor smoke test). 9 fields preserved verbatim per audit (i) inventory + Brief v2.2 C-8 wire-shape:
  - `UUID phaseId`, `UUID tenantId`, `String phaseName`, `String phaseStatus`, `int lapCount`, `int currentLap`, `int fieldCount`, `boolean preparationPreview`, `List<GroupSummary> groups`
  - Nested record `GroupSummary(int groupNumber, int teamCount)` co-located in the same compilation unit per Java records canon.
  - JSON wire shape (Jackson default field-name serialization) preserved per Brief v2.2 C-8 + D-9.
  - The `tenantId` field is mandatory per Brief v2.2 O-9 (Svelte SPA builds `/topic/display/{tenantId}/events` STOMP-topic from this field). C-3 default = signature preservation.
- [ ] **AC-RED-FIRST-DISPLAY-MATCHES-RESPONSE:** New `de.vvwt.tm.display.DisplayMatchesResponse(UUID phaseId, int lap, List<MatchEntry> matches)` Java record authored RED-first (constructor smoke test). Nested record `MatchEntry(UUID matchId, Integer fieldNumber, String teamAName, String teamBName, List<SetResultEntry> setResults, String matchStatus, String refereeTeamName)`. Nested record `SetResultEntry(int setIndex, int scoreA, int scoreB)`. JSON wire shape preserved verbatim.
- [ ] **AC-RED-FIRST-DISPLAY-GROUP-STANDINGS-RESPONSE:** New `de.vvwt.tm.display.DisplayGroupStandingsResponse(UUID phaseId, List<GroupStandings> groups)` Java record authored RED-first (constructor smoke test). Nested record `GroupStandings(int groupNumber, List<TeamRanking> rankings)`. Nested record `TeamRanking(int position, String teamName, int points, int setsWon, int setsLost, int ballsWon, int ballsLost)`. JSON wire shape preserved verbatim.
- [ ] **AC-RECORDS-NO-WEB-INTERNAL-DTO-PACKAGE:** No `de.vvwt.tm.web.internal.dto.*` package is created by THIS story for these 3 records. The records live at `de.vvwt.tm.display.*` only. (Future stories may introduce `web.internal.dto.*` for OTHER bounded contexts where Pattern E applies — those are out-of-scope for E25.)

### Domain-exception authoring

- [ ] **AC-RED-FIRST-NO-ACTIVE-PHASE-EXCEPTION:** New `de.vvwt.tm.display.NoActivePhaseException` authored RED-first as public class extending `RuntimeException`. No-arg constructor: `super("No active phase exists for the current tenant")`. Caught cross-module by `web.GlobalExceptionHandler.handleNoActivePhase` (E25S02 updates the import). C-3 default = signature preservation; if RED-first authoring discovers a need to deviate (e.g., new constructor signature), STOP-and-escalate per C-3.

### Modulith module declaration

- [ ] **AC-RED-FIRST-PACKAGE-INFO:** New `vvwt-tm-web/src/main/java/de/vvwt/tm/display/package-info.java` authored RED-first; declares `@org.springframework.modulith.ApplicationModule(allowedDependencies = {"tournament", "tournament::exceptions"})`. ApplicationModulesTest verifies post-S01 GREEN.
- [ ] **AC-DISPLAY-ALLOWED-DEPS-EXACTLY-2:** Exactly 2 array entries: `"tournament"` + `"tournament::exceptions"`. NO `"tenant"` (audit (iii) confirms no direct compile-time tenant import in service); NO `"tournament::events"` (audit (iii) confirms no event consumption in display Java); NO `"tournament::dto"` (audit (i) confirms display has its own query-shape records which reside at `de.vvwt.tm.display.*` per DEC-40 §2026-04-27 Clarification Pattern A — not at `tournament::dto` and not at `web.internal.dto.*`).

### `validateDisplayDeviceToken` 4-branch RED-first coverage (per Brief v2.2 C-14 corrected)

- [ ] **AC-VALIDATE-TOKEN-BRANCH-1-MISSING:** RED-first test reproducing legacy line 395 — null or blank `deviceToken` → `UnauthorizedException("Missing or empty device token")`.
- [ ] **AC-VALIDATE-TOKEN-BRANCH-2-UNKNOWN-LAMBDA:** RED-first test reproducing legacy line 402 — token not found in `deviceRepository.findByDeviceToken(...)` → `Optional.empty().orElseThrow(() -> new UnauthorizedException("Invalid or unknown device token"))`. The lambda construction site is the second of the 4 branches; AC text MUST preserve "lambda inside `orElseThrow`" framing for traceability.
- [ ] **AC-VALIDATE-TOKEN-BRANCH-3-WRONG-TYPE:** RED-first test reproducing legacy line 405 — device's `device_type` is not `"DISPLAY"` (e.g., `SCORING_TABLET`) → `UnauthorizedException("Device token is not for a display device (type=…)")`.
- [ ] **AC-VALIDATE-TOKEN-BRANCH-4-INACTIVE-STATUS:** RED-first test reproducing legacy line 413 — device's `status` is neither `REGISTERED` nor `ASSIGNED` (e.g., `DISCONNECTED`) → `UnauthorizedException("Display device is not active (status=…)")`.

### Phase-detection coverage

- [ ] **AC-PHASE-OVERVIEW-ACTIVE:** RED-first test for `getPhaseOverview` with valid DISPLAY token + ACTIVE phase → returns `DisplayPhaseOverviewResponse` with `phaseId`, `tenantId` (per O-9 SPA-WS-topic resolution), `phaseName`, `phaseStatus="ACTIVE"`, `lapCount`, `currentLap`, `fieldCount`, `preparationPreview=false`, ordered list of `GroupSummary`.
- [ ] **AC-PHASE-OVERVIEW-PREPARATION-PREVIEW:** RED-first test — when no ACTIVE phase exists but PENDING phase has at least one match with non-null `lapNumber`, return same DTO with `phaseStatus="PENDING"` + `preparationPreview=true`.
- [ ] **AC-PHASE-OVERVIEW-NO-ACTIVE-PHASE:** RED-first test — neither ACTIVE phase nor PENDING-with-scheduled-matches → `NoActivePhaseException`.
- [ ] **AC-MATCHES-BY-LAP-EXPLICIT-LAP:** RED-first test for `getMatchesByLap(token, lap=1)` → returns matches filtered by lap=1 with team names resolved via TeamAvatar→Team lookup, set results in play-order, MatchState mapped to display-status (`OPEN/ENABLED → PENDING`, `INPROGRESS/ONCHECK → IN_PROGRESS`, `FINISHED_*/CANCELED → COMPLETED`).
- [ ] **AC-MATCHES-BY-LAP-CURRENT-LAP-DEFAULT:** RED-first test for `getMatchesByLap(token, lap=null)` → uses `phase.currentLapNumber` as effective lap.
- [ ] **AC-GROUP-STANDINGS-D33-SORT:** RED-first test for `getGroupStandings` — rankings sorted per D-33 (`points DESC`, then `setQuotient DESC`, then `ballQuotient DESC`, `withoutAssessment` last); `position` 1-indexed within each group.
- [ ] **AC-GROUP-STANDINGS-ZERO-RATING-FALLBACK:** RED-first test — when no `TeamAvatarRating` row exists for an avatar (e.g., tournament not yet started), service uses zero-score default (matchCount=0, points=0, setsWon/Lost=0, ballsWon/Lost=0, withoutAssessment=false) so DTO populates without NPE.

### DEC-22 Iron Law + DEC-41 hierarchy

- [ ] **AC-DEC22-IRON-LAW-RED-EVIDENCE:** Impl-report enumerates each new test method's RED-state commit hash + GREEN-state commit hash; reviewer-verifiable by `git log --oneline` per E36S03+S04+S05+S06+S07+S08 precedent.
- [ ] **AC-DEC41-FRESH-AUTHORING-NO-LEGACY-REUSE:** No reuse of the 25 Snapshot-Driven legacy tests (per audit (ii) Aggregate Verdict + DEC-41 §3 hierarchy item 1). Impl-report cites `E25-AUDIT-DEC41-TEST-CLASSIFICATION` (commit `1aded97`) verbatim per DEC-41 §4 audit obligation.
- [ ] **AC-Q7-METHOD-COUNT-RECONFIRMATION:** Delivery counts `@Test` in fresh `DefaultDisplayOverviewServiceTest` (same-package `de.vvwt.tm.display.internal` per DEC-36 white-box-allowed) + any same-package interface-level test (`de.vvwt.tm.display.DisplayOverviewServiceTest` if introduced by author choice). Audit baseline 15 (legacy `DisplayOverviewControllerIT` `@Test` count per audit (ii) classification table; service-layer behavior derivable from controller IT ACs) is reference floor for coverage scope; concrete number determined by Delivery's RED-first authoring + impl-report justification.

### DEC-35 + DEC-36 package layout + cross-package test typing

- [ ] **AC-DEC35-INTERFACE-IN-ROOT:** `DisplayOverviewService` interface is in `de.vvwt.tm.display` root; `DefaultDisplayOverviewService` impl is in `de.vvwt.tm.display.internal`. Naming canon: no `I`-prefix.
- [ ] **AC-DEC36-CROSS-PACKAGE-TEST-TYPING:** Any cross-package test (e.g., E25S02's `DisplayOverviewControllerIT` referencing the service via `@MockitoBean DisplayOverviewService` from web test pkg) MUST type-reference the interface FQN, never `DefaultDisplayOverviewService` impl class. Same-package tests in `de.vvwt.tm.display.internal` MAY white-box.

### Cross-cutting

- [ ] **AC-MVN-VERIFY-GREEN:** `mvn verify` GREEN on staging post-merge (DEC-22 Iron Law regression-gate).
- [ ] **AC-APPLICATIONMODULESTEST-GREEN:** Post-S01 commit, `ApplicationModulesTest` GREEN per DEC-21 invariant. New `display` module + its `allowedDependencies` are recognized by Spring Modulith.
- [ ] **AC-DEC31-PROPAGATE-GOVERNANCE:** Per DEC-31 §(c) two-commit contract at story closure via `coordinate-handoffs` → `propagate-governance` skill. Snapshot of `DEC-44/-45` (already propagated) + this story file land in `vvwt-prj/docs/governance/`.
- [ ] **AC-DELIVERY-MODE-SOURCE:** Story declares `delivery_mode: source` (modifies production-code under `vvwt-tm-web/src/main/java/`); triggers DEC-31 propagate-governance per §(g).
- [ ] **AC-DEC32-NOT-INVOKED:** No FQN-rewrite consumer-pattern usage in this story. New display root-package types created at canonical FQN; no existing-consumer-FQN-substitution. DEC-32 invocation count remains #1 (E21S13).
- [ ] **AC-DEC22-REFACTOR-CLAUSE-N/A:** Per `feedback_dec22_refactor_phase_first.md`: §refactor-clause is phase 3 of RED-GREEN-REFACTOR on TDD-authored code. E25S01 authors TDD-fresh code; refactor-clause N/A here. No legacy code modified by S01 (legacy domain files DELETED per AC-DELETE-LEGACY-FIRST).

---

## Out of Scope

- Web-side controllers (`DisplayOverviewController` + `DisplayViewController`), `web/package-info.java` `allowedDependencies` mutation, `web/GlobalExceptionHandler` import-line update, fresh `DisplayOverviewControllerIT` + `DisplayViewControllerIT` — **E25S02 scope**.
- Web-tier DTOs (HTTP-only DTOs in `de.vvwt.tm.web.internal.dto.*`) — **N/A for E25 display**: per DEC-40 §2026-04-27 Clarification Pattern A, the 3 records are bounded-context-owned at `display.*`. None of Clause B conditions (a)/(c)/(d) fire for E25 today (per Brief D-1 + O-10: token-gated only, no public variant; no cross-context aggregation; Java field names = JSON field names — no aliasing).
- Javadoc-historical-provenance sweep on cross-module references — **E25S03 scope**.
- Display-track Success Metrics verification — **E25S03 scope**.
- Svelte UI under `vvwt-tm-web/src/main/ui-display/` — preserved verbatim per Brief D-9.
- WebSocket Java code — display has zero (audit (iii)); WS plumbing in `tournament.internal.web` (already E21-reconstructed) is OUT-of-scope.
- DAO ITs (DEC-26 N/A) — display has no DAO classes.
- DEC-44 IT-annotation retrofit — operationalized by E39S01 (done).

---

## Dependencies

- **E36S08** — `web/GlobalExceptionHandler` post-E36S08 is at `de.vvwt.tm.web` and currently catches `infrastructure.display.NoActivePhaseException`. E25S01's authoring of `de.vvwt.tm.display.NoActivePhaseException` enables E25S02's import-line update.
- **E39S01** — DEC-44 D1+D2 framework + WebModuleTestConfig post-E39S01 state are needed by E25S02 (which authors web-module ITs); E25S01 does NOT author web-module ITs but is declared as dep for trace-clarity (Brief v2.1 D-15 reviewer F#2 option A).

---

## Notes

**Legacy file inventory (audit (i)) for E25S01 deletion scope:**

- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/DisplayOverviewService.java` — 653 LOC; 3 public methods + 4 UnauthorizedException-branch helper.
- `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/NoActivePhaseException.java` — 20 LOC; no-arg constructor + RuntimeException base.

(Controllers + DTOs + their tests are E25S02 + E25S03 scope.)

**Estimated LOC:**

- Production: ~750 LOC (interface + impl + exception + 3 records + package-info; +150 LOC vs. pre-resolution estimate due to relocated record-authoring scope).
- Test: ≥ legacy floor (audit (ii) baseline ~12-15 service-method-equivalent test cases derived from `DisplayOverviewControllerIT`'s service-driven assertions; concrete count by Delivery RED-first) + 3 record constructor smoke tests (~20 LOC each).
- Total: ~750 + ~860 LOC ≈ 1610 LOC across ~9 files.

**Pre-identified C-3 escalation candidates (audit (i) + Brief v2.2 C-3 + Brief escalation-resolution C-10):**

- 8-arg constructor of `DefaultDisplayOverviewService` (parameter type/order; consumer compile-stable).
- 3 public method signatures of `DisplayOverviewService` interface.
- 3 record constructor signatures (`DisplayPhaseOverviewResponse` 9 fields incl. `tenantId`; `DisplayMatchesResponse` + nested `MatchEntry`/`SetResultEntry`; `DisplayGroupStandingsResponse` + nested `GroupStandings`/`TeamRanking`) — wire-shape verbatim per audit (i) inventory + Brief v2.2 C-8.
- `NoActivePhaseException` no-arg constructor + parent class.

If RED-first authoring surfaces a need to deviate (e.g., constructor injection of a different service type, additional interface method), Delivery STOPS and escalates per C-3 + base.rules.md "Conflict & Escalation Protocol".

**Reviewer F#2 cycle-2 note carry-forward:** AC text preserves "lambda inside `orElseThrow`" framing for branch 2 (AC-VALIDATE-TOKEN-BRANCH-2-UNKNOWN-LAMBDA) so future grep-based audits cannot trip the off-by-one error that Cycle-1 Reviewer F#1 caught.

**Escalation-resolution provenance (2026-04-27):** The 3 record-authoring ACs in this story were originally placed in E25S02 under Brief v2.2; they were RELOCATED to E25S01 by Brief `discovery-2026-04-27-e25s01-escalation-resolution` (Tier-2 Reviewer cycle-1 FAIL → cycle-2 PASS; human-validated 2026-04-27) per (a) compile-graph: the `DisplayOverviewService` interface returns the records by FQN — won't compile without them at `display.*`; (b) story-cohesion: per DEC-40 §2026-04-27 Clarification Pattern A, the records belong to the display module; this story is the display-module-rebuild story. The DTO placement decision (was `web.internal.dto.*` per Brief v2.2; now `display.*` per DEC-40 §2026-04-27 Clarification) is the architectural fix that resolved the Modulith cycle that escalated the prior delivery (outer-repo commit `23d947b`).

**Orphan-record intermediate state:** Between this story's GREEN merge and E25S02's start, the 3 new `display.*` records exist unconsumed (legacy `infrastructure/display/dto/*` consumers still reference legacy types until S02 deletes them). This is EXPECTED per D-7 γ feature-branch atomicity pattern (audit (i) §"Strategy" + E36 photo/cert precedent + Brief escalation-resolution S-6). `ApplicationModulesTest` may emit informational messages about the unconsumed display-module records — verify empirically at delivery time and document in impl-report.
