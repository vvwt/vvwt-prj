<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E35S04.story.md at fc5aef7e6ae9d50ac3c24b94c2d75e266ec3971b 2026-04-23 -->
---
type: artefact
artefact_type: story
id: E35S04
epic: E35
kind: cleanup
track: discovery
related_backlog_id: E35S04
related_decs: [DEC-22, DEC-29, DEC-30, DEC-31, DEC-35, DEC-36, DEC-41]
depends_on: [E35S01, E35S02, E35S03]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-23-vvwt-worker-lib-reconstruction
mandatory_ac_categories: [governance, testing]
created_at: 2026-04-23
updated_at: 2026-04-23
---

# Story E35S04: vvwt-worker-lib cleanup + Module Package Layout (Non-Spring) conventions sub-section finalized

## User Story

**As a** Discovery Agent (and the human closing out E35),
**I want** the `patterns/conventions.md` "Module Package Layout — Non-Spring Modules" sub-section finalized with concrete `WorkerKeyManager` / `DefaultWorkerKeyManager` examples now visible on disk, the `score/legacy/NonVarietyRatingBuilder.java` fixture resolved per the E35S01 audit recommendation, the final `mvn verify` green across vvwt-worker-lib + 4 consumer modules, and the governance-snapshot propagated per DEC-31,
**so that** E35 closes with a complete and citable convention canon for non-Spring interface extraction (instantiated by E35S02), the legacy fixture is deliberately resolved (not lingering), and every downstream agent reading `vvwt-prj/docs/governance/` can cite the exact post-E35 baseline for `vvwt-worker-lib`.

---

## Context

E35S02 added a SKELETON sub-section to `patterns/conventions.md` titled "Module Package Layout — Non-Spring Modules (analogy from DEC-35)" with the rule statement and a placeholder for "Concrete example to be added in E35S04 once `DefaultWorkerKeyManager` is merged." This story finalizes that sub-section once `DefaultWorkerKeyManager` exists on disk.

E35S01 AC5 produced exactly one of three recommendation labels (KEEP-AS-FIXTURE / DELETE / RELOCATE-WITH-SCOPE-NOTE) for `score/legacy/NonVarietyRatingBuilder.java`. This story executes that recommendation.

DEC-31 / `coordinate-handoffs` Step 7→Step 10 mandates governance-snapshot propagation for code-affecting story closures. Since E35S02 + E35S03 affect inner-repo code, this story bundles the snapshot propagation as the closing act of the epic.

---

## Acceptance Criteria

- [ ] **AC1 — `patterns/conventions.md` sub-section FINAL with concrete example.** The "Module Package Layout — Non-Spring Modules (analogy from DEC-35)" sub-section (skeleton from E35S02 AC3) is enriched with a concrete code example: `WorkerKeyManager` interface in `de.vvwt.worker.identity` (signature snippet, 2–4 lines) + `DefaultWorkerKeyManager` impl in `de.vvwt.worker.identity.internal` (class declaration + constructor signature, 2–4 lines) + cross-reference to E35S02 as the operationalizing story. The "Concrete example to be added in E35S04 once `DefaultWorkerKeyManager` is merged" placeholder is removed. Final length target after enrichment: 18–28 lines (was 8–15 lines skeleton). (Governance.)

- [ ] **AC2 — `score/legacy/NonVarietyRatingBuilder.java` resolved per E35S01 AC5 recommendation.** The implementer reads `.gaai/project/contexts/artefacts/audits/E35S01-vvwt-worker-lib-test-classification.md` AC5 subsection and executes the recommended action: <br>(a) **KEEP-AS-FIXTURE** — add a Javadoc header to the file documenting its DEC-41 classification + the test files that reference it; no file move; no deletion. <br>(b) **DELETE** — remove the file; verify no references remain via `git grep NonVarietyRatingBuilder`; the impl-report includes the grep output. <br>(c) **RELOCATE-WITH-SCOPE-NOTE** — move the file to the audit-recommended target path; add a Javadoc header explaining the relocation; update any references. <br>The impl-report cites which option was executed and quotes the AC5 recommendation. (Governance.)

- [ ] **AC3 — Final `mvn verify` green across all 5 modules.** The implementer runs `mvn -pl vvwt-worker-lib,vvwt-dispatcher,vvwt-benchmark,vvwt-standalone-worker,vvwt-tm-web -am verify` from `vvwt-prj` root and confirms BUILD SUCCESS. The impl-report includes the final maven-build summary line (or equivalent). DEC-29 `failOnWarning=true`, DEC-30 `spotless:check`, and `ban-spring-boot-in-worker-lib` enforcer all green. (Testing, governance.)

- [ ] **AC4 — Post-reconstruction package map produced.** A short (15–30 lines) "Post-E35 vvwt-worker-lib Package Map" section is added to either the conventions.md sub-section (per AC1) OR a dedicated `vvwt-prj/vvwt-worker-lib/README.md` section (implementer's choice — whichever existing convention fits). The map enumerates the post-E35 state of each of the 5 main packages: identity (interface + Default impl in .internal), codec / solver / score / types (utility classes / records UNCHANGED in production; tests possibly updated per E35S03). The map cites E35S02 for identity changes and E35S03 for utility-package test corrections. (Governance.)

- [ ] **AC5 — Governance-snapshot propagation runs automatically per DEC-31 §(b)/(c)/(g).** Per DEC-31 §(b), `propagate-governance` is invoked AUTOMATICALLY by `coordinate-handoffs` between step 7 (squash-merge to `vvwt-prj/staging`) and step 10 (backlog `--set-status {id} done`) — NOT by the story author manually. The skill reads this story's `related_decs` frontmatter (DEC-22, DEC-29, DEC-30, DEC-31, DEC-35, DEC-36, DEC-41) plus the story file itself, and idempotently propagates each not-yet-present file to `vvwt-prj/docs/governance/decisions/` and `vvwt-prj/docs/governance/stories/` per DEC-23 path conventions, skipping byte-identical already-propagated files per DEC-31 §(f). For DECs already present in `vvwt-prj/docs/governance/decisions/` whose outer-repo source has advanced (e.g., DEC-22 was amended by DEC-34/DEC-36/DEC-41, last_updated 2026-04-22), the skill MAY propagate a fresh snapshot updating the file per DEC-31 §(f) re-snapshot clause; the implementer's verification (per AC8c) confirms via `git show` that the snapshot commit's diff includes any such updated DECs. Note per DEC-31 §(g): governance-only stories (E35S01) do NOT trigger their own story-file propagation, but their `related_decs` propagate at this story's closure if not already covered. The implementer's obligation is verification: post-merge, confirm via `git log vvwt-prj/staging --oneline -5` that the `docs(governance): propagate E35S04 snapshot per DEC-31` commit landed immediately following the cleanup commit (per AC8b). The impl-report enumerates the propagated files (read from the snapshot commit's diff). (Governance.)

- [ ] **AC6 — Concurrency-contract per-story refinement obligation closed.** Per Brief Deferred limitation, the implementer reads E35S02's impl-report concurrency-contract entry (E35S02 AC9) and confirms whether the contract is documented at the `WorkerKeyManager` interface Javadoc level. If yes, no action. If no, AC6 verifies that the impl-report explicitly stated "no concurrency contract change identified during reconstruction" — in which case no follow-up action is needed. (Governance, testing.)

- [ ] **AC7 — Epic E35 closure verification.** The implementer verifies all 4 stories (E35S01, E35S02, E35S03, this) have impl-report.md + qa-report.md per DEC-27. The impl-report includes a 4-row table: per-story (id / status / pr_url / completed_at / impl-report-path / qa-report-path). DEC-28 Done-Write-Integrity-Gate satisfied for all 4 stories. (Governance.)

- [ ] **AC8a — Outer-repo commit lands FIRST (DEC-31 §(h) attribution prereq).** Outer-repo edits (`patterns/conventions.md` final concrete example per AC1, this story file) committed to outer repo and pushed to outer-repo `staging` BEFORE the inner-repo cleanup commit. This ordering is required by DEC-31 §(h): the `propagate-governance` skill needs the outer-repo staging-HEAD SHA to write the attribution comment (`<!-- Snapshot of outer-repo .gaai/... at {commit-sha} {date} -->`) on each propagated file. (Governance.)

- [ ] **AC8b — Inner-repo cleanup commit per DEC-13 (separate from governance-snapshot per DEC-31 §(c)).** Inner-repo edits (`vvwt-prj/vvwt-worker-lib/...` cleanup actions per AC2 + per AC4 if AC4 chose `vvwt-prj/vvwt-worker-lib/README.md` as its target) committed as a single commit titled `chore(E35S04): vvwt-worker-lib cleanup` and pushed to `vvwt-prj/staging` per DEC-13. The `chore(...)` prefix is canonical for all AC2 branches (KEEP/DELETE/RELOCATE — none warrant `feat(...)` or `fix(...)`). This commit MUST NOT include the governance-snapshot files; those land in a SEPARATE adjacent commit per AC8c. The conventions.md example finalization per AC1 is OUTER-repo and lands in AC8a, NOT this commit. (Governance.)

- [ ] **AC8c — Governance-snapshot commit auto-generated per DEC-31 §(c) — verified by implementer.** Per DEC-31 §(c), `propagate-governance` (invoked by `coordinate-handoffs` between step 7 and step 10) produces a SEPARATE commit on `vvwt-prj/staging` titled exactly `docs(governance): propagate E35S04 snapshot per DEC-31`, distinct from AC8b's `chore(E35S04): ...` commit. Both commits land on `vvwt-prj/staging` in the same Delivery session, adjacent in the commit graph, with the governance snapshot immediately following the cleanup commit. The implementer VERIFIES via `git log vvwt-prj/staging --oneline -5` that both commits are present and adjacent; the impl-report cites the two commit SHAs and the snapshot commit's `--stat` diff. (Governance.)

> All acceptance criteria must be verifiable. Vague criteria are invalid.

---

## Out of Scope

- Reconstruction of utility-class production code (DEC-22 § item 5 grandfather under Brief T-7 X-scoping).
- WorkerKeyManager reconstruction work (E35S02).
- Snapshot-Driven test replacement (E35S03).
- Authoring a new DEC for non-Spring interface extraction (G3 chosen — inline conventions only per Brief D-9).
- Sibling-module internal reconstruction (own future Discoveries).
- DEC-28 memory-index drift remediation (separate session per Brief).

---

## Dependencies

- E35S01 (`done`) — AC2 reads E35S01 audit AC5 recommendation; AC4 cites E35S03 for tests.
- E35S02 (`done`) — AC1 enriches the skeleton sub-section with `WorkerKeyManager` / `DefaultWorkerKeyManager` examples that exist on disk only after E35S02 merges.
- E35S03 (`done`) — AC4 package map cites test-correction outcomes; AC5 governance-snapshot propagation includes E35S03 story file.
- DEC-22 (Iron Law context — no new code added by this cleanup story; production code untouched).
- DEC-29 / DEC-30 (must remain green per AC3).
- DEC-31 (governance-artefact propagation per AC5).
- DEC-35 (interface principle by-analogy — final concrete example per AC1).
- DEC-41 (classification rubric — used in AC2 KEEP-AS-FIXTURE Javadoc header only if applicable).
- E34S01 (`kind: reconstruction` trigger marker LIVE; this story's `kind: cleanup` correctly does NOT trigger DEC-41 qa-review self-loop).

---

## Notes

- This story closes the Epic. It depends on E35S01-S03 all being `done` per DEC-27 + DEC-28.
- `delivery_mode: source` for this story (set in backlog yaml entry); both inner-repo (vvwt-prj cleanup commits + governance propagation per DEC-23 paths) and outer-repo (conventions final + this story file) commits required.
- `kind: cleanup` correctly does NOT trigger the `patterns/conventions.md` line 103 reconstruction-story marker on the `kind` prong. The `depends_on` prong (E35S02 + E35S03 are reconstruction-titled or `kind: reconstruction`) is also addressed: line 103's exact text reads "depends_on a `bug-triage`-track or 'Reconstruction'-titled story". E35S02's title starts with "Reconstruct" not "Reconstruction" (singular vs nominalized form), and E35S03's title is "Replace Snapshot-Driven tests..." which contains no "Reconstruct" string. Strict reading of line 103 → no trigger fires from depends_on prong. The implementer SHOULD verify this conclusion in the impl-report (analogous to E35S01 AC7 explicit guard), and if `qa-review` interprets the prong more loosely, AC4 (no test reuse in this cleanup story) still satisfies the obligation vacuously.
- AC2's three-way KEEP/DELETE/RELOCATE branch is exhaustive — the implementer cannot deviate. If the AC5 audit recommendation is unclear, STOP and escalate per Conflict Protocol.
- AC5 governance-snapshot propagation is the explicit operationalization of Brief D-14(iv) / C-8.
- AC6 concurrency-contract closure honors Brief Deferred limitation ("per-story refinement obligation in E35S02 ... MUST explicitly state in-scope-or-not; no follow-up-DEC blanket pre-authorization") by simply verifying E35S02 made the call — not by re-litigating the call here.
