<!-- Snapshot of outer-repo .gaai/project/contexts/artefacts/stories/E25S02.story.md at e84706b78711e3ab44cb99c01ed335b7cd68df1c 2026-04-27 -->
---
type: artefact
artefact_type: story
id: E25S02
related_backlog_id: E25S02
track: discovery
created_at: 2026-04-27
updated_at: 2026-04-27
mandatory_ac_categories: [error-handling, testing, governance, security]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-04-27-e25s01-escalation-resolution
related_decs: [DEC-13, DEC-21, DEC-22, DEC-31, DEC-32, DEC-36, DEC-38, DEC-40, DEC-41, DEC-44, DEC-45]
delivery_mode: source
---

# Story: Display web Q-1a TDD rebuild — `DisplayOverviewController` + `DisplayViewController` at `de.vvwt.tm.web.*` (consuming `display.*` records authored by E25S01); `web/package-info.java` allowedDependencies += `"display"`; `web/GlobalExceptionHandler` import-line update; fresh ITs under DEC-44 D1 framework

## User Story

**As a** Tournament Manager Delivery Agent operating under DEC-21/-22/-40/-44/-45 + Brief escalation-resolution governance,
**I want** to delete legacy `infrastructure.display.{DisplayOverviewController,DisplayViewController,dto/*}` + their ITs and author fresh Q-1a TDD RED-first controller replacements at canonical FQNs `de.vvwt.tm.web.*` consuming the 3 query-shape records (`DisplayPhaseOverviewResponse`, `DisplayMatchesResponse`, `DisplayGroupStandingsResponse`) authored by E25S01 at `de.vvwt.tm.display.*` per DEC-40 §2026-04-27 Clarification Pattern A,
**so that** the display REST + MVC primary-adapter surface joins the `de.vvwt.tm.web` Modulith module (per DEC-40 Clause A); URL paths + JSON wire shapes preserved verbatim per C-8; Svelte SPA continues working unchanged per D-9; and the new web-module ITs land directly under the DEC-44 D1 `@SpringBootTest(RANDOM_PORT)` canon retrofitted by E39S01.

---

## Context

E25S01 authored fresh `display.DisplayOverviewService` interface + `display.internal.DefaultDisplayOverviewService` impl + `display.NoActivePhaseException` + 3 query-shape records at `de.vvwt.tm.display.*` (per DEC-40 §2026-04-27 Clarification Pattern A) + `display/package-info.java`. E25S02 builds the web-tier on top: 2 controllers consuming the 3 records + the `allowedDependencies` extension to `web` + the `web.GlobalExceptionHandler` import-line update + fresh ITs. **The 3 records do NOT live in `de.vvwt.tm.web.internal.dto.*`** — they are bounded-context-owned at `display.*` (E25S01 scope). The controllers consume them cross-module via the `web→display` allowedDependency added by AC-WEB-ALLOWED-DEPS-EXPANSION.

Per audit (i): `web/GlobalExceptionHandler.java` line 11 currently imports `de.vvwt.tm.infrastructure.display.NoActivePhaseException` and catches it via `@ExceptionHandler(NoActivePhaseException.class)` returning `NoActivePhaseResponse("NO_ACTIVE_PHASE")` body with HTTP 404. Per Brief v2.2 D-11: this single-line import update is the only mechanical refactor on Q-1a-authored E36S08 code (phase-3 refactor of RED-GREEN-REFACTOR on TDD-authored code per `feedback_dec22_refactor_phase_first.md`); DEC-32 §Scope explicitly excludes test/import-line edits → DEC-32 invocation count remains #1 (E21S13).

Per Brief v2.2 D-13 + D-14 + DEC-44 §"2026-04-27 Empirical Refinement": new web-module ITs use `@SpringBootTest(webEnvironment = RANDOM_PORT, classes = TournamentManagerApplication.class) + @ActiveProfiles("test") + @Import({WebModuleTestConfig.class, <IT>.TestAdminCredentials.class})` where TestAdminCredentials inner class provides `@Bean("webItAdminCredentialsProvider") @Primary AdminCredentialsProvider` returning the per-IT BCrypt password hash.

**Resumption note (2026-04-27):** The previous version of this story (Brief v2.2) included 3 `AC-RED-FIRST-DISPLAY-*-RESPONSE` ACs authoring the records at `de.vvwt.tm.web.internal.dto.*`. Those ACs are RELOCATED to E25S01 per Brief `discovery-2026-04-27-e25s01-escalation-resolution` D-3(c) + DEC-40 §2026-04-27 Clarification (Pattern A). E25S02 now consumes those records cross-module — no record authoring in this story.

---

## Acceptance Criteria

### Pre-flight: legacy deletion

- [ ] **AC-DELETE-LEGACY-FIRST:** Single commit on the story branch deletes the following BEFORE any RED-first authoring:
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/DisplayOverviewController.java` (130 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/DisplayViewController.java` (84 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/dto/DisplayPhaseOverviewResponse.java` (47 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/dto/DisplayMatchesResponse.java` (47 LOC)
  - `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/dto/DisplayGroupStandingsResponse.java` (47 LOC)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/display/DisplayOverviewControllerIT.java` (671 LOC)
  - `vvwt-tm-web/src/test/java/de/vvwt/tm/infrastructure/display/DisplayViewControllerIT.java` (298 LOC)
  - The (now-empty) `vvwt-tm-web/src/main/java/de/vvwt/tm/infrastructure/display/` and `.../dto/` directories (if Java packages become empty post-deletion of all files in them, the directory itself is also removed).
- [ ] **AC-NO-LEGACY-CODE-REFERENCE:** Per Brief v2.2 C-17: legacy source MUST NOT be inspected during RED-first authoring beyond audit-i empirical inventory. Authoring proceeds from Brief + audits + DEC + URL/JSON-wire-contracts only.

### Records: NOT authored by this story (relocated to E25S01)

- [ ] **AC-RECORDS-AUTHORED-BY-S01:** The 3 query-shape records (`de.vvwt.tm.display.DisplayPhaseOverviewResponse`, `de.vvwt.tm.display.DisplayMatchesResponse`, `de.vvwt.tm.display.DisplayGroupStandingsResponse`) are NOT authored by E25S02. They are authored by E25S01 at `de.vvwt.tm.display.*` (root, public) per DEC-40 §2026-04-27 Clarification (Pattern A — bounded-context-owned query-shape records). E25S02's controllers (below) consume them cross-module via the `web→display` allowedDependency added by `AC-WEB-ALLOWED-DEPS-EXPANSION`. **Out-of-scope:** authoring records at `de.vvwt.tm.web.internal.dto.*` — that placement was the architectural error in Brief v2.2 that escalated the prior delivery (commit `23d947b`). Per Brief escalation-resolution D-1 + DEC-40 §2026-04-27 Clarification, none of Clause B conditions (a)/(c)/(d) fire for E25 today, so Pattern A applies (no `web.internal.dto.*` package created for display records).

### REST controller Q-1a TDD authoring (`de.vvwt.tm.web.*`) — consuming `display.*` records

- [ ] **AC-RED-FIRST-DISPLAY-OVERVIEW-CONTROLLER:** New `de.vvwt.tm.web.DisplayOverviewController` `@RestController @RequestMapping("/api/display")` authored RED-first. 3 GET endpoints with URL + query parameter shape preserved verbatim per C-8 + D-9:
  - `@GetMapping("/overview")` `getPhaseOverview(@RequestParam("token") String token)` → `ResponseEntity<DisplayPhaseOverviewResponse>` (200 OK)
  - `@GetMapping("/overview/matches")` `getMatchesByLap(@RequestParam("token") String token, @RequestParam(value="lap", required=false) Integer lap)` → `ResponseEntity<DisplayMatchesResponse>` (200 OK)
  - `@GetMapping("/overview/groups")` `getGroupStandings(@RequestParam("token") String token)` → `ResponseEntity<DisplayGroupStandingsResponse>` (200 OK)
  - Constructor-injects `DisplayOverviewService` (interface FQN `de.vvwt.tm.display.DisplayOverviewService` per DEC-36 cross-package).
  - **Record imports:** `DisplayPhaseOverviewResponse`, `DisplayMatchesResponse`, `DisplayGroupStandingsResponse` are imported from `de.vvwt.tm.display.*` (NOT `de.vvwt.tm.web.internal.dto.*`) per DEC-40 §2026-04-27 Clarification Pattern A. The controller serializes them directly via Jackson with no web-tier DTO wrapping.
- [ ] **AC-RED-FIRST-DISPLAY-VIEW-CONTROLLER:** New `de.vvwt.tm.web.DisplayViewController` `@Controller` authored RED-first. 2 GET MVC routes:
  - `@GetMapping("/display/overview")` returns `"forward:/display/index.html"`.
  - `@GetMapping("/display/register")` returns `"forward:/display/index.html"`.
  - Both forward to the same Vite-built Svelte SPA shell at `classpath:/static/display/index.html` (preserved per D-9). The Svelte app uses `window.location.pathname` for client-side routing.

### `web/GlobalExceptionHandler` import-line update (D-11)

- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-IMPORT-UPDATE:** `vvwt-tm-web/src/main/java/de/vvwt/tm/web/GlobalExceptionHandler.java` line 11: `import de.vvwt.tm.infrastructure.display.NoActivePhaseException;` → `import de.vvwt.tm.display.NoActivePhaseException;`. Per Brief v2.2 D-11 + `feedback_dec22_refactor_phase_first.md`: this is phase-3 refactor of DEC-22 §refactor-clause on E36S08-Q-1a-reconstructed `web/GlobalExceptionHandler`; the underlying class has RED-first provenance, qualifying the import-line edit as trustworthy mechanical refactor (NOT Q-1b on legacy code).
- [ ] **AC-GLOBAL-EXCEPTION-HANDLER-PRESERVATION:** `@ExceptionHandler(NoActivePhaseException.class)` annotation, `handleNoActivePhase(NoActivePhaseException ex)` method body, and `NoActivePhaseResponse(String status)` record body shape (HTTP 404 + `{"status":"NO_ACTIVE_PHASE"}`) PRESERVED verbatim. Existing JavadocS preserved (only the import FQN line and any matching `@see` Javadoc tag in `GlobalExceptionHandler.java` lines 567-580 region update from `infrastructure.display.NoActivePhaseException` to `display.NoActivePhaseException`).
- [ ] **AC-DEC32-NOT-INVOKED:** Per DEC-32 §Scope: "Does NOT apply to: any change in tests" + import-line edits on Q-1a code excluded; DEC-32 count stays at #1 (E21S13).

### Modulith allowedDependencies extension

- [ ] **AC-WEB-ALLOWED-DEPS-EXPANSION:** `vvwt-tm-web/src/main/java/de/vvwt/tm/web/package-info.java` `@ApplicationModule.allowedDependencies` array gains `"display"`. Per `web/package-info.java` Javadoc precedent (e.g., the `print` row added at E24S06): add per-entry justification entry: `display — DisplayOverviewController + DisplayViewController consume display.DisplayOverviewService interface; web.GlobalExceptionHandler catches display.NoActivePhaseException`.
- [ ] **AC-DEC40-CLAUSE-E-PRE-APPROVED:** Per Brief v2.2 C-15 + DEC-45 D2: E25 has unconditional L2-stays pre-approval — Trigger-α firing at this addition does NOT require fresh L2.5 examination (already done in DEC-45 D2 for the entire Wave-2 trajectory). Story documents this in impl-report.

### Fresh web-module ITs under DEC-44 D1 framework

- [ ] **AC-RED-FIRST-DISPLAY-OVERVIEW-CONTROLLER-IT:** New `vvwt-tm-web/src/test/java/de/vvwt/tm/web/DisplayOverviewControllerIT.java` authored RED-first. Annotation form per Brief v2.2 D-13:
  ```java
  @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT,
                  classes = de.vvwt.tm.TournamentManagerApplication.class,
                  properties = {"spring.datasource.url=jdbc:h2:mem:e25s02overviewitdb;…"})
  @ActiveProfiles("test")
  @Import({WebModuleTestConfig.class, DisplayOverviewControllerIT.TestAdminCredentials.class})
  ```
  - Inner `TestAdminCredentials` `@TestConfiguration` provides `@Bean("webItAdminCredentialsProvider") @Primary AdminCredentialsProvider` returning per-IT BCrypt-hashed test password (matches E39S01 inline-pointer-refinement pattern).
  - Tests: AC1-AC11 coverage analogous to the legacy test (15 @Test methods reference floor per Brief v2.2 C-11 + audit (ii) Aggregate Verdict; final count by Delivery's RED-first authoring).
- [ ] **AC-RED-FIRST-DISPLAY-VIEW-CONTROLLER-IT:** New `vvwt-tm-web/src/test/java/de/vvwt/tm/web/DisplayViewControllerIT.java` authored RED-first. Same annotation form as above (different `properties` datasource name). Tests cover: HTTP 200 + content-type text/html for both routes; SPA mount-point `<div id="app">` present in body; no external CDN `<script>`/`<link>` per DEC-16 offline (analogous to legacy 10 @Test reference floor).
- [ ] **AC-DEC44-D2-EMPIRICAL-REFINEMENT-RESPECT:** Neither IT introduces a `UserDetailsService` substitute bean nor a `SecurityFilterChain` substitute bean. Authentication flows the production `UserDetailsService` ← `@Primary AdminCredentialsProvider` per-IT-override per DEC-44 §"2026-04-27 Empirical Refinement". If empirical observation at RED-first reveals divergent need, STOP-and-escalate per C-3 + base.rules.md.
- [ ] **AC-DEC36-CROSS-PACKAGE-TEST-TYPING:** Both ITs reference `de.vvwt.tm.display.DisplayOverviewService` interface (NOT `display.internal.DefaultDisplayOverviewService`); `@MockitoBean` types (if any) reference interface FQN.
- [ ] **AC-DEC38-SLICE-TEST-OPTIONAL:** Story author MAY introduce `DisplayOverviewControllerSliceTest` + `DisplayViewControllerSliceTest` (`@WebMvcTest`) for slice-layer coverage; if introduced, follows DEC-38 erratum slice/IT pattern. NOT required by this story (audit (vi) E39S01 IT-retrofit-plan precedent — slice tests are optional per existing pattern).

### URL + JSON-wire preservation

- [ ] **AC-URL-PATHS-PRESERVED:** REST URL set verbatim per C-8 + D-9: `GET /api/display/overview`, `GET /api/display/overview/matches?lap=N`, `GET /api/display/overview/groups`. MVC routes: `GET /display/overview`, `GET /display/register`. Both `forward:/display/index.html`. Static asset path `/display/assets/**` preserved (Spring Boot auto-served from `target/classes/static/display/`).
- [ ] **AC-JSON-WIRE-PRESERVED:** Response JSON shapes verbatim per audit (i) DTO inventory. Field names + nested record fields + tenantId field in DisplayPhaseOverviewResponse all preserved. Jackson default serialization.
- [ ] **AC-AUTHENTICATION-FLOW-PRESERVED:** Token query parameter validation flows through `DisplayOverviewService.getPhaseOverview/getMatchesByLap/getGroupStandings` (per E25S01 ACs); 4 UnauthorizedException branches → HTTP 401 via `web.GlobalExceptionHandler`.

### DEC-22 Iron Law + DEC-41 hierarchy

- [ ] **AC-DEC22-IRON-LAW-RED-EVIDENCE:** Impl-report enumerates each new test method's RED-state commit hash + GREEN-state commit hash; reviewer-verifiable per E36 precedent.
- [ ] **AC-DEC41-FRESH-AUTHORING-NO-LEGACY-REUSE:** No reuse of legacy `DisplayOverviewControllerIT` (15 @Test) + `DisplayViewControllerIT` (10 @Test) per audit (ii) 25/25 Snapshot-Driven verdict + DEC-41 §3 hierarchy item 1. Impl-report cites `E25-AUDIT-DEC41-TEST-CLASSIFICATION` (commit `1aded97`) + the corrected aggregate (15+10=25 baseline).
- [ ] **AC-Q7-METHOD-COUNT-RECONFIRMATION:** Delivery counts `@Test` in fresh `DisplayOverviewControllerIT` + `DisplayViewControllerIT`; reports any deviation from audit baseline (15+10=25) in impl-report; final count by Delivery's RED-first authoring.

### Cross-cutting

- [ ] **AC-MVN-VERIFY-GREEN:** `mvn verify` GREEN on staging post-merge.
- [ ] **AC-APPLICATIONMODULESTEST-GREEN:** Post-S02 commit, `ApplicationModulesTest` GREEN per DEC-21. `display` module now consumed by `web` per the allowedDependencies expansion.
- [ ] **AC-DEC31-PROPAGATE-GOVERNANCE:** Per DEC-31 §(c) two-commit contract at story closure.
- [ ] **AC-DELIVERY-MODE-SOURCE:** `delivery_mode: source`.
- [ ] **AC-NO-PRODUCTION-CODE-LOGIC-CHANGE-OUTSIDE-DISPLAY-WEB:** Outside the explicit AC scope (display + web), no production code modified. `web/GlobalExceptionHandler` is the single mechanical-import-edit cross-cutting change per D-11.
- [ ] **AC-SLICE-TESTS-NOT-REGRESSED:** All existing `*ControllerSliceTest.java` files (`@WebMvcTest`) UNCHANGED.
- [ ] **AC-DEC-26-DAO-IT-N/A:** Display has no DAO IT (DEC-26 N/A per Brief D-2).

---

## Out of Scope

- Domain layer (`display.DisplayOverviewService` + impl + `display.NoActivePhaseException` + 3 query-shape records at `de.vvwt.tm.display.*` + `display/package-info.java`) — **E25S01 scope**.
- Web-tier DTOs (`de.vvwt.tm.web.internal.dto.*` for display) — **N/A per DEC-40 §2026-04-27 Clarification Pattern A**: records are bounded-context-owned at `display.*`; no `web.internal.dto.*` package created for E25 display. (None of Clause B conditions (a)/(c)/(d) fire for E25 today.)
- Javadoc-historical-provenance sweep + Display-track Success Metrics + propagate-governance commits — **E25S03 scope**.
- Svelte UI under `vvwt-tm-web/src/main/ui-display/` — preserved verbatim per D-9.
- WebSocket Java code — display has zero (audit (iii)).
- DEC-44 IT-annotation retrofit — operationalized by E39S01 (done).
- L2.5 examination at `web` Trigger-α firing — pre-approved per DEC-45 D2 (E25 unconditional).

---

## Dependencies

- **E25S01** — provides `display.DisplayOverviewService` interface + `display.internal.DefaultDisplayOverviewService` impl + `display.NoActivePhaseException`. Without S01, S02 controllers cannot compile (interface FQN missing) and `web/GlobalExceptionHandler` cannot import the new exception.
- **E39S01** — provides `WebModuleTestConfig` post-E39S01 state (`@Bean("webItPasswordEncoder") @Primary` + `@Bean("webItAdminCredentialsProvider")` placeholder pattern); required for new web-module ITs to land under DEC-44 D1 framework.

---

## Notes

**Estimated LOC:**

- Production: ~150 LOC across 2 files (2 controllers; 3 records relocated to E25S01 scope per Brief escalation-resolution D-3(c)) + 1-line `web/GlobalExceptionHandler` import edit + 1-line `web/package-info.java` `allowedDependencies` extension.
- Test: ~600-1000 LOC across 2 fresh ITs (analogous to E36S02 + E36S06 controller-IT precedent).

**`tenantId` field criticality (per O-9):** the `tenantId` field in `DisplayPhaseOverviewResponse` MUST be preserved verbatim because the Svelte SPA reads it post-overview-fetch to construct the WebSocket STOMP-topic URL `/topic/display/{tenantId}/events`. Removing or renaming this field would break the WebSocket-event-driven SPA refresh per E07S06 contract. C-3 default = signature preservation.

**`@TestAdminCredentials` empirical-verification AC (Brief v2.1 cycle-2 reviewer F#1 carry-forward):** the inner class IS expected to be needed (DEC-44 inline-pointer-refinement empirically established 8 of 10 retrofitted ITs needed it; only 2 — Print + CertificateRender — already had it pre-DEC-44). Display ITs introduce admin-test-context indirectly via `tenantContextBinder` + `TestRestTemplate` pulling AuthConfiguration into the bean-graph. If empirical RED-first observation at delivery-time reveals the inner class is NOT needed (unlikely), Delivery documents the deviation in impl-report per E39S01 deviation-documentation pattern.

**Reviewer F#2 cycle-2 informational note carry-forward:** dependency `E39S01` is technically transitive via `E25S01:[E39S01]`; Brief v2.1 D-15 chose explicit duplication for trace-clarity (acceptable per `base.rules.md` Backlog Archiving Rules §3).

**Escalation-resolution provenance (2026-04-27):** Pre-escalation (Brief v2.2), this story authored 3 records at `de.vvwt.tm.web.internal.dto.*` per `AC-RED-FIRST-DISPLAY-PHASE-OVERVIEW-RESPONSE` / -MATCHES-RESPONSE / -GROUP-STANDINGS-RESPONSE. Those 3 ACs are now RELOCATED to E25S01 per Brief `discovery-2026-04-27-e25s01-escalation-resolution` D-3(c) + DEC-40 §2026-04-27 Clarification (Pattern A — bounded-context-owned query-shape records). The records live at `de.vvwt.tm.display.*` (not `web.internal.dto.*`); E25S02's controllers consume them cross-module via the `web→display` allowedDependency added by `AC-WEB-ALLOWED-DEPS-EXPANSION`. The architectural fix resolves the Modulith cycle that escalated the prior delivery (outer-repo commit `23d947b`).
