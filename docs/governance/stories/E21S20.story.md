---
type: artefact
artefact_type: story
id: E21S20
epic: E21
track: bug-triage
related_backlog_id: E21S20
related_decs: [DEC-2, DEC-13, DEC-21, DEC-22, DEC-27, DEC-29, DEC-30, DEC-40, DEC-44]
depends_on: [E21S19]
skills_invoked: [generate-stories]
session_brief_ref: discovery-2026-05-04-tournament-draft-get-put-rest-regression
mandatory_ac_categories: [testing, governance, security]
delivery_mode: source
tier: 2
created_at: 2026-05-04
updated_at: 2026-05-04
---

# Story E21S20: Cleanup — absorb the remaining PR #90 (E22S08) residuals after partial supersession by E22S11 + E21S19; Q-1b `DeviceAdminController` whole-class relocation `tournament` → `web` + Q-1b `TournamentRulesController` class+URL rename to `ScoringRulesController` (`/api/tournament-rules` → `/api/scoring/rules`) + atomic Svelte URL update + close PR #90

## User Story

**As a** developer reading the `vvwt-tm-web` Modulith module,
**I want** the `tournament` package to contain ZERO REST controllers (per DEC-40 Clause A) and the `TournamentRulesController` to be renamed `ScoringRulesController` per E22S08's original DDD-ownership decision,
**so that** the Wave-2 Track-3 Modulith reconstruction is structurally complete on its DEC-40 invariants and PR #90's residual scope is fully absorbed without leaving stale governance artefacts on GitHub.

---

## Context

This Story is the second half of the Alt-1 absorption of PR #90 (E22S08). Background:

- 2026-04-22: E22S08 Discovery refined a Brief proposing whole-class Q-1b relocation of three controllers (`TournamentRulesController`, `DraftController`, `DeviceAdminController`) from `de.vvwt.tm.tournament` to `de.vvwt.tm.web` per DEC-40 Clause A, plus a class+URL rename `TournamentRulesController` → `ScoringRulesController` (`/api/tournament-rules` → `/api/scoring/rules`) per D-2 Option B (DDD ownership: the controller serves scoring rules, not tournaments).
- 2026-04-23: E22S08 Delivery completed in worktree; PR #90 opened. Backlog marked `status: done` with `pr_status: open` (process anomaly — see memory note `project_e22s08_pr90_relevance_check.md`).
- 2026-04-23 → 2026-05-04 (11 days): staging accumulated structural Wave-2 commits — E22S11 (#94) "atomic cutover", E23/E24/E25/E26/E45 Modulith extensions modifying `de.vvwt.tm.web/package-info.java` `allowedDependencies`. PR #90 was never merged.
- 2026-05-04: Discovery-Bug-Triage for the `/api/tournaments/{id}/draft` 404 surfaced PR #90's stale state. Empirical verification (Discovery 2026-05-04 Brief): `gh pr merge 90 --squash` fails on conflicts; PR #90 is **partially superseded** by intervening Wave-2 work:
  - **TournamentRulesController** has already been relocated `tournament` → `web` by E22S11 (#94) cutover, but the **rename + URL change** were NOT done — the class is still `TournamentRulesController` at `/api/tournament-rules`. Residual: **rename + URL change + frontend update only.**
  - **DraftController + DeviceAdminController** remain at `de.vvwt.tm.tournament.*` (not yet relocated). Residual: full Q-1b relocation.
- 2026-05-04: E21S19 (sibling Story authored 2026-05-04 in the same Discovery session) absorbed the **DraftController-only** subset of PR #90's residuals (relocation + new `tournament::draft-dto` named-interface + new GET/PUT endpoints).
- **2026-05-04 (THIS Story):** absorbs the **remaining** PR #90 residuals — `DeviceAdminController` Q-1b relocation + `TournamentRulesController` rename + URL change + frontend update + closure of PR #90 itself.

After this Story merges, the `de.vvwt.tm.tournament` package contains **zero** REST controllers (DEC-40 Clause A invariant fully realized for Wave-2 Track-3) and PR #90 is closed with a comment referencing E21S19 + E21S20 as its absorbers. The F-8 process anomaly (backlog `done` ↔ GitHub `OPEN` state mismatch) remains for a separate Discovery thread per memory `project_e22s08_pr90_relevance_check.md`.

### Q-1a authorship audit (mandatory pre-relocation check, 2026-05-04)

Before authorizing Q-1b whole-class relocation, the Q-1a authorship discipline of the affected classes was empirically verified to ensure no legacy-code residue is being relocated under DEC-22 §refactor-clause cover:

- **`DeviceAdminController.java`** — created by E21S06 commit `c04a05f` (GREEN) at 2026-04-20 18:17:16, **AFTER** the corresponding RED test commit `73e73ac` (`test(E21S06): RED tests — Device aggregate + DeviceLimit + DeviceController + DeviceAdminController`) at 2026-04-20 18:04:57. RED preceded GREEN by 13 minutes. Subsequent E21S15 (`e208880`) made fixture fixes (`DeviceAdminControllerIT 409-vs-200`); E21S13 atomic cutover (`52730b6`) executed FQN-rewrites under DEC-32 carve-out. ✅ Genuine Q-1a authorship.
- **`DeviceAdminControllerIT.java` + `DeviceAdminControllerSliceTest.java`** — RED-first creation under E21S06 (`73e73ac`). ✅ Q-1a.
- **`TournamentRulesController.java`** — created by E21S10 commit `ead359c` (`feat(E21S10): [TDD-GREEN] 7 classes — ApiErrorResponse + GlobalExceptionHandler + JacksonConfig + WebSocketConfig + EventMessage + DomainEventBridge + TournamentRulesController`) at 2026-04-20 23:43:09, **AFTER** the corresponding RED commit `432eb2f` (`test(E21S10): [TDD-RED] TournamentRulesController slice + IT tests; fix DomainEventBridgeTest compile`) at 2026-04-20 23:37:06. RED preceded GREEN by 6 minutes. ✅ Genuine Q-1a authorship.
- **`TournamentRulesControllerIT.java` + `TournamentRulesControllerSliceTest.java`** — RED-first creation under E21S10 (`432eb2f`). ✅ Q-1a.

**Legacy markers in current files**: `grep -nE "legacy|infrastructure.web|domain."` returns:
- `DeviceAdminController.java:18,21` — TWO outdated Javadoc lines referencing the deleted legacy `de.vvwt.tm.infrastructure.web.DeviceAdminController` (Javadoc context, NOT legacy code; should be cleaned up during the relocation per AC-RELOCATE-DEVICEADMINCONTROLLER-Q1B).
- `TournamentRulesController.java` — clean (no legacy markers).

DeviceAdminController + TournamentRulesController + their tests are GENUINE Q-1a-authored TDD code. DEC-22 §refactor-clause applies in full strength for Q-1b whole-class relocation + class+URL rename (per memory `feedback_dec22_refactor_phase_first` — "refactor-clause is RED-GREEN-REFACTOR phase 3 on TDD-authored code"; the rename is a phase-3 refactor on Q-1a code).

### Modulith allowedDependencies analysis (no new entries needed)

Empirical inspection (2026-05-04) of imports + current `web/package-info.java` allowedDependencies confirms:

- **DeviceAdminController** imports only `de.vvwt.tm.tournament.internal.dto.DeviceSummaryResponse` from outside `web`. This DTO is at `tournament.internal.dto.*` (top-level, not a sub-package) — already covered by the existing `tournament::dto` named interface. Web's `allowedDependencies` already lists `tournament::dto`. ✅ Cross-module access works post-relocation.
- **TournamentRulesController** imports `de.vvwt.tm.scoring.{ScoringRuleRegistry, SetValidationRuleRegistry}` (covered by `scoring`) + `de.vvwt.tm.tournament.{MatchFormat, MatchGeneratorRegistry}` (covered by `tournament`). All imports already permitted by existing `allowedDependencies`. ✅ No allowedDeps changes needed for this Story.

This is a structural simplification vs. E21S19 (which had to introduce a new `tournament::draft-dto` named interface + extend allowedDependencies). E21S20's scope does NOT touch any `package-info.java` file beyond the natural relocation-side updates.

### SecurityConfig analysis (no path-pattern updates needed)

`grep -nE "tournament-rules|/api/tournament" /home/vvw/NetBeansProjects/vvwt-prj/vvwt-tm-web/src/main/java/de/vvwt/tm/auth/internal/SecurityConfig.java` returns no explicit `/api/tournament-rules` pattern (only `/api/tournaments/{id}/audio` for public audio). The URL change from `/api/tournament-rules` → `/api/scoring/rules` is therefore covered by the SecurityConfig's general `/api/**` authentication policy — no SecurityConfig edit required. ✅ Verified at story-authoring time.

### Test annotation strategy (DEC-44 web-module IT canon)

Per DEC-44 (web-module-IT canon, established by E22S07's relocated controller ITs): when an IT moves into the `de.vvwt.tm.web` module, its annotation switches from `@ApplicationModuleTest(...)` (bounded-context-module canon per DEC-38) to `@SpringBootTest(RANDOM_PORT, classes = TournamentManagerApplication.class)` (web-module-IT canon per DEC-44). Slice tests retain `@WebMvcTest`. This Story's IT relocations follow that precedent — Delivery applies it mechanically.

---

## Reproduction Scenarios (Given / When / Then)

This Story is structural cleanup, not a bug fix — there is no end-user-observable bug to reproduce. The Story's correctness is verified by structural assertions (post-fix file paths, ApplicationModulesTest GREEN, /api/scoring/rules reachable, /api/tournament-rules NOT reachable, frontend builds clean).

### Scenario A — Post-fix DEC-40 Clause A invariant

**Given** the `de.vvwt.tm.tournament` package after this Story's PR is merged to staging,

**When** the operator runs `find vvwt-tm-web/src/main/java/de/vvwt/tm/tournament -name "*Controller.java"`,

**Then** the output is **empty** (zero REST controllers in the bounded-context module). All controllers reside under `de.vvwt.tm.web`.

### Scenario B — `/api/scoring/rules` reachable; `/api/tournament-rules` returns 404

**Given** the application post-fix,

**When** the client issues `GET /api/scoring/rules` (authenticated),

**Then** the response is `200 OK` with the same JSON body shape that `/api/tournament-rules` returned pre-fix.

**When** the client issues `GET /api/tournament-rules` (authenticated),

**Then** the response is `404 Not Found` (no fallback / redirect — clean cutover per E22S08's "atomic — no transient 404" decision; here "atomic" = atomic cutover, not "no transient 404 forever" — the old URL is intentionally removed).

### Scenario C — Frontend uses new URL; no transient 404

**Given** the Vite build post-fix,

**When** the user opens the TournamentForm view,

**Then** the form's dropdowns (matchFormat, scoringRuleId, setValidationRuleId, matchGeneratorId) populate from `GET /api/scoring/rules` (NOT `/api/tournament-rules`). Verified: `grep -E "tournament-rules" vvwt-tm-web/src/main/ui/src/` returns zero matches; `grep -E "scoring/rules" vvwt-tm-web/src/main/ui/src/` returns the three expected lines (`tournamentStore.ts:41`, `tournamentStore.ts:207`, `TournamentForm.svelte:12`).

### Scenario D — Pre-existing tests as regression gate (DEC-22 §refactor-clause, Q-1b)

**Given** the existing test suite (DeviceAdminControllerIT + DeviceAdminControllerSliceTest + TournamentRulesControllerIT + TournamentRulesControllerSliceTest) pre-fix,

**When** Delivery executes the Q-1b whole-class moves + rename,

**Then** all pre-existing tests continue to pass against the relocated/renamed classes (modulo mechanical updates: package decl, IT annotation switch per DEC-44, `@WebMvcTest(ScoringRulesController.class)` reference, URL string in IT path expectations). No RED-first new tests are required for the moves themselves — existing tests = regression gate per DEC-22 §refactor-clause Q-1b discipline.

### Scenario E — PR #90 closure

**Given** this Story's PR is merged to staging,

**When** the operator (or Delivery, see AC-CLOSE-PR-90) runs `gh pr view 90 --json state`,

**Then** the response is `{"state":"CLOSED"}` with a closure comment referencing E21S19 + E21S20 as the absorbers and the `project_e22s08_pr90_relevance_check.md` memo for the F-8 process anomaly.

---

## Acceptance Criteria

### Testing — pre-existing tests as regression gate (testing)

- [ ] **AC-TEST-PREEXISTING-DEVICEADMIN-GREEN** (testing — DEC-22 §refactor-clause Q-1b): `DeviceAdminControllerIT` (relocated to `web/`) and `DeviceAdminControllerSliceTest` (relocated to `web/`) continue to pass post-fix. The IT switches from its current annotation to `@SpringBootTest(RANDOM_PORT, classes = TournamentManagerApplication.class)` per DEC-44 web-module-IT canon. The slice test retains `@WebMvcTest(DeviceAdminController.class)` (with package-import update). Test counts unchanged.
- [ ] **AC-TEST-PREEXISTING-SCORINGRULES-GREEN** (testing — DEC-22 §refactor-clause Q-1b): `ScoringRulesControllerIT` (renamed from `TournamentRulesControllerIT`) and `ScoringRulesControllerSliceTest` (renamed from `TournamentRulesControllerSliceTest`) continue to pass post-fix. URL expectations in the IT update from `/api/tournament-rules` to `/api/scoring/rules`. `@WebMvcTest` reference updates to `ScoringRulesController.class`. Test counts unchanged.
- [ ] **AC-TEST-MODULITH-VERIFY-GREEN** (testing — DEC-21): `ApplicationModulesTest` (the project-wide Modulith verifier) is GREEN post-fix. Verifies that the relocated/renamed controllers honor the existing `de.vvwt.tm.web/package-info.java` `allowedDependencies` — no new entries are added by this Story (per Notes §Modulith allowedDependencies analysis).
- [ ] **AC-TEST-PREEXISTING-SUITES-GREEN** (testing): All pre-existing tests across the codebase continue to pass post-fix (full Maven `mvn verify` GREEN). Baseline test count is verified empirically at Delivery time and recorded in `impl-report.md`.
- [ ] **AC-TEST-NO-NEW-RED-FIRST-FOR-MOVES** (testing — DEC-22 §refactor-clause Q-1b discipline): No new RED-first tests are authored for the moves/rename themselves (existing tests = regression gate per Q-1b precedent E22S07/E22S08). If Delivery determines that a regression-coverage gap exists (e.g., URL routing test missing), Delivery STOPs and escalates rather than silently authoring new tests inside this Story's scope.

### Security — tenant isolation (security)

- [ ] **AC-TENANT-CONTEXT-PRESERVED** (security): The relocated `DeviceAdminController` and renamed `ScoringRulesController` preserve their existing tenant-scoping behavior verbatim. The `@PreAuthorize` annotations on `DeviceAdminController` (currently present per its imports) are preserved through the move. No new auth code; no auth code removed.
- [ ] **AC-SECURITYCONFIG-UNCHANGED** (security): `vvwt-tm-web/src/main/java/de/vvwt/tm/auth/internal/SecurityConfig.java` is NOT modified by this Story. The `/api/scoring/rules` URL is covered by the SecurityConfig's general `/api/**` authentication policy (verified at story-authoring per Notes §SecurityConfig analysis). Verifiable: `git diff --name-only` shows zero changes to `SecurityConfig.java`.

### Governance — DEC compliance + relocation/rename ACs + frontend atomicity + reports + traceability + PR #90 closure (governance)

- [ ] **AC-RELOCATE-DEVICEADMINCONTROLLER-Q1B** (governance — DEC-40 Clause A / DEC-22 §refactor-clause / Q-1b whole-class move): `DeviceAdminController.java` is moved via `git mv` from `vvwt-tm-web/src/main/java/de/vvwt/tm/tournament/DeviceAdminController.java` to `vvwt-tm-web/src/main/java/de/vvwt/tm/web/DeviceAdminController.java`. Package declaration rewritten to `package de.vvwt.tm.web;`. Imports adjusted to reference `de.vvwt.tm.tournament.internal.dto.DeviceSummaryResponse` (cross-module via existing `tournament::dto` named interface). `@RestController` bean name preserved verbatim. `@RequestMapping` URL preserved verbatim. The two outdated Javadoc lines at `DeviceAdminController.java:18,21` (referencing the deleted legacy `de.vvwt.tm.infrastructure.web.DeviceAdminController`) are removed in the same commit (cleanup of stale documentation). Q-1a authorship verified at story-authoring time per Notes §Q-1a authorship audit.
- [ ] **AC-RELOCATE-DEVICEADMIN-TESTS-Q1B** (governance — DEC-44 web-module-IT canon): `DeviceAdminControllerIT.java` and `DeviceAdminControllerSliceTest.java` are moved via `git mv` from `vvwt-tm-web/src/test/java/de/vvwt/tm/tournament/Device*.java` to `vvwt-tm-web/src/test/java/de/vvwt/tm/web/Device*.java`. Package declarations rewritten. IT annotation switches from `@ApplicationModuleTest(...)` to `@SpringBootTest(RANDOM_PORT, classes = TournamentManagerApplication.class)` per DEC-44 (precedent E22S07's `DeviceControllerIT`). Slice test retains `@WebMvcTest(DeviceAdminController.class)`. Cold-boot measurement is OUT of scope (E22S07 baseline established).
- [ ] **AC-RENAME-TOURNAMENTRULES-TO-SCORINGRULES** (governance — DEC-40 Clause A DDD-ownership / DEC-22 §refactor-clause Q-1b): The class `de.vvwt.tm.web.TournamentRulesController` is renamed to `de.vvwt.tm.web.ScoringRulesController` via `git mv` (file rename) + class-declaration rewrite. The `@RestController` bean name updates from `tmTournamentRulesController` to `tmScoringRulesController`. The `@RequestMapping` URL updates from `/api/tournament-rules` to `/api/scoring/rules`. All Java consumers (none expected post-E22S11; verifiable via `grep -rn "tmTournamentRulesController\|TournamentRulesController" vvwt-tm-web/src/main/java/`) update their references. Q-1a authorship verified at story-authoring time per Notes §Q-1a authorship audit.
- [ ] **AC-RENAME-SCORINGRULES-TESTS** (governance — DEC-44 web-module-IT canon): `TournamentRulesControllerIT.java` is renamed via `git mv` to `ScoringRulesControllerIT.java`; `TournamentRulesControllerSliceTest.java` is renamed via `git mv` to `ScoringRulesControllerSliceTest.java`. Class declarations updated. URL string assertions in the IT update from `/api/tournament-rules` to `/api/scoring/rules`. `@WebMvcTest(TournamentRulesController.class)` updates to `@WebMvcTest(ScoringRulesController.class)`. Test method names containing the substring `TournamentRules` are renamed to `ScoringRules` (mechanical sed-style rewrite).
- [ ] **AC-FRONTEND-URL-UPDATE-ATOMIC** (governance — Brief E22S08 D-2 Option B atomic-cutover precedent / DEC-2): The Svelte frontend's three references to `/api/tournament-rules` are updated to `/api/scoring/rules` in the **same atomic commit** as the backend rename, preventing transient 404s post-merge. Verifiable: post-fix `grep -rn "tournament-rules" vvwt-tm-web/src/main/ui/src/` returns zero matches. The three exact files+lines: `vvwt-tm-web/src/main/ui/src/stores/tournamentStore.ts:41`, `vvwt-tm-web/src/main/ui/src/stores/tournamentStore.ts:207`, `vvwt-tm-web/src/main/ui/src/routes/TournamentForm.svelte:12`. Comments referencing the URL (e.g., `TournamentForm.svelte:12` comment AC8 mentioning `/api/tournament-rules`) are also updated.
- [ ] **AC-NO-DRAFTCONTROLLER-MODIFICATION** (governance): This Story does NOT modify `DraftController.java` (handled by E21S19, sibling Story). Verifiable: `git diff --name-only staging` against the post-E21S19 baseline shows zero changes under `de.vvwt.tm.web.DraftController.java`, `de.vvwt.tm.tournament.internal.dto.draft/`, or `de.vvwt.tm.web/package-info.java`.
- [ ] **AC-NO-DTOS-RELOCATED** (governance): No DTO is relocated by this Story. `DeviceSummaryResponse` remains at `de.vvwt.tm.tournament.internal.dto.DeviceSummaryResponse` (covered by existing `tournament::dto` named interface). Verifiable: `git diff --name-only` shows zero changes under `tournament/internal/dto/*.java` (excluding the draft sub-package owned by E21S19).
- [ ] **AC-NO-NEW-ALLOWEDDEPS** (governance — Notes §Modulith allowedDependencies analysis): `vvwt-tm-web/src/main/java/de/vvwt/tm/web/package-info.java` is NOT modified by this Story (or is modified only to update Javadoc text in lockstep with renamed types, never the `allowedDependencies` array literal). Verifiable: `git diff vvwt-tm-web/src/main/java/de/vvwt/tm/web/package-info.java` shows zero changes inside the `allowedDependencies = { ... }` literal block.
- [ ] **AC-DEC-32-NOT-INVOKED** (governance — Brief): DEC-32 (mechanical FQN-rewrite carve-out) is NOT invoked by this Story. The Q-1b whole-class moves + class-rename + URL-string rewrite are within DEC-22 §refactor-clause discipline; the frontend URL string updates are TypeScript (not Java FQN-rewrites and outside DEC-32 scope by definition). DEC-32 invocation count remains at 1 (E21S13).
- [ ] **AC-CLOSE-PR-90** (governance — Brief A-scope (A) per user direction 2026-05-04): After this Story's PR is merged to staging, Delivery (or operator post-merge) executes `gh pr close 90 --comment "Superseded — DraftController scope absorbed by E21S19 (PR #merged-link); DeviceAdminController + ScoringRules-rename + Svelte URL update absorbed by E21S20 (this PR's merge link). The backlog \\\`status: done\\\` ↔ GitHub \\\`OPEN\\\` state mismatch (E22S08) is the responsibility of a separate F-8 Discovery thread per memory \\\`project_e22s08_pr90_relevance_check.md\\\`. PR #90 was never merged because intervening Wave-2 commits (E22S11 atomic cutover + E23/E24/E25/E26/E45 Modulith extensions) created irreconcilable conflicts with PR #90's branch tip f4843e2."`. Verifiable post-action: `gh pr view 90 --json state,closedAt` returns `{"state":"CLOSED","closedAt":"<timestamp>"}`. The closing comment text need not be byte-identical to the above; it MUST contain (a) the substring "E21S19", (b) the substring "E21S20", and (c) a reference to the F-8 memo or the broader process anomaly.
- [ ] **AC-RELATED-DECS** (governance): The Story's frontmatter `related_decs` lists DEC-2, DEC-13, DEC-21, DEC-22, DEC-27, DEC-29, DEC-30, DEC-40, DEC-44. The Delivery commit message and `impl-report.md` cite each that materially constrains the implementation.
- [ ] **AC-IMPL-REPORT-CITATIONS** (governance — DEC-27): `contexts/artefacts/impl-reports/E21S20.impl-report.md` is committed before the Story moves to `status: done`. It enumerates: the four `git mv` operations (DeviceAdminController + IT + SliceTest moves to web; DraftControllerIT/SliceTest already moved by E21S19); the rename operations (TournamentRulesController → ScoringRulesController + IT + SliceTest); the frontend three-line update with line numbers; the IT annotation switches per DEC-44; the PR #90 closure command text + post-action verification; and a per-DEC compliance note for DEC-22 (§refactor-clause Q-1b), DEC-40 (location + DDD-rename), DEC-21 (Modulith verify), DEC-44 (web-module-IT canon), DEC-27 (this report).
- [ ] **AC-QA-REPORT** (governance — DEC-27): `contexts/artefacts/qa-reports/E21S20.qa-report.md` is committed before the Story moves to `status: done`. It covers (a) per-Scenario verification (A: tournament package empty; B: /api/scoring/rules reachable + /api/tournament-rules 404; C: frontend grep zero-matches; D: pre-existing tests GREEN; E: PR #90 closed); (b) `ApplicationModulesTest` GREEN; (c) full pre-existing test suite GREEN; (d) Vite build (`npm run build` from `vvwt-tm-web/src/main/ui`) succeeds + TypeScript type-check clean; (e) PR #90 closure verified.
- [ ] **AC-NO-VVWT-PRJ-FRONTEND-BUILD-FAILURE** (governance): The Vite build (`npm run build` from `vvwt-tm-web/src/main/ui`) succeeds post-fix; TypeScript type-check is clean. Verified by Delivery and recorded in `impl-report.md`.

---

## Out of Scope

- `DraftController` relocation + `tournament::draft-dto` named interface + new GET/PUT endpoints — handled by sibling Story E21S19 (depends_on this Story).
- F-8 process anomaly investigation (E22S08 backlog `done` ↔ GitHub `OPEN` state mismatch root cause). Deferred to a separate Discovery thread per memory note `project_e22s08_pr90_relevance_check.md`. PR #90 closure (AC-CLOSE-PR-90) is in-scope here; **investigating WHY the anomaly happened** is out-of-scope.
- New REST endpoints (this Story is structural cleanup only — no API contract changes beyond the URL-rename in TournamentRules → ScoringRules).
- Schema changes.
- Modifications to `DraftController` or any draft-related artefact (handled by E21S19).
- Modifications to `web/package-info.java` `allowedDependencies` array literal (no new named-interfaces needed).
- Modifications to `SecurityConfig.java` (URL change covered by general `/api/**` policy).
- New feature flags or backward-compatibility shims (e.g., a transitional `/api/tournament-rules` redirect to `/api/scoring/rules` is REJECTED — atomic cutover per E22S08 D-2 Option B; backward-compat would be a different governance call).
- DEC-32 invocation (Q-1b within §refactor-clause discipline, no consumer-legacy FQN-rewrite).
- Cold-boot measurement for the relocated ITs (E22S07's baseline already established).

---

## Dependencies

- **E21S19** (HARD — must be `status: done` on `staging` before this Story commences). Provides: the `de.vvwt.tm.tournament` package without `DraftController` (so this Story's "tournament package contains zero controllers post-fix" invariant is well-defined); the `tournament::draft-dto` named interface (already in web allowedDeps post-E21S19; this Story does NOT need to re-create it). Verified at Delivery start by `git log --oneline staging | grep "chore(E21S19): done"` returning a commit AND `find vvwt-tm-web/src/main/java/de/vvwt/tm/tournament -name "*Controller.java"` returning only `DeviceAdminController.java` (the one this Story moves).
- **DEC-22** §refactor-clause (Q-1b whole-class relocation + Q-1b class+URL rename on Q-1a-authored code per Notes §Q-1a authorship audit; no Q-1a fresh RED-first work in this Story — pre-existing tests are the regression gate).
- **DEC-27** (`impl-report.md` + `qa-report.md` on disk before `status: done`).
- **DEC-40** Clause A (controller location at `de.vvwt.tm.web`) + Clause A DDD-ownership rationale (Scoring controller in scoring-named class).
- **DEC-21** (Spring Modulith — `ApplicationModulesTest` GREEN post-fix).
- **DEC-44** (web-module IT canon — `@SpringBootTest(RANDOM_PORT, classes = TournamentManagerApplication.class)`).
- **DEC-2** (Vite + Svelte + TypeScript — frontend URL update scope).
- **DEC-13** (staging branch only).
- **DEC-29** / **DEC-30** (compiler-hygiene + Spotless — inherited via parent POM).

---

## Notes

**Bug-triage provenance.** This Story is the second half of the Alt-1 absorption of PR #90 (E22S08), authored in the same Discovery session as E21S19 (sibling Story handling DraftController-only subset). The Discovery session was triggered 2026-05-04 by the user's report of a 404 on `/api/tournaments/{uuid}/draft` and expanded — through the Brief v3/v4 → cycle-2-FAIL → loop-limit-escalation → Alt-1-light-governance progression — to absorb PR #90's residuals into self-contained Stories.

**Brief v4 references (inherited from sibling E21S19).** This Story shares Brief `discovery-2026-05-04-tournament-draft-get-put-rest-regression` with E21S19. Items referenced:
- D-1, D-2 (track: bug-triage; delivery_mode: source) — inherited.
- E-1 (PR #90 process anomaly escalation marker) — referenced via memory note `project_e22s08_pr90_relevance_check.md`; investigation NOT in this Story's scope.
- T-3 alternatives (a/b/c) for the controller-location question — applied here at the DEC-40 location.
- Alt-1 light-governance pattern — applied here without Brief v5 / reviewer cycle (substituted by Q-1a authorship audit + Modulith allowedDependencies analysis at story-authoring time).

**Q-1a authorship audit substitutes reviewer cycle.** Per the Alt-1 light-governance pattern established by E21S19's amendments, this Story does NOT undergo a separate Brief + Reviewer cycle. Instead, the Q-1a authorship audit (Notes §) provides empirical evidence that the Q-1b relocation/rename targets are GENUINE TDD-authored code — the substantive risk that the original reviewer cycle would have probed. Pre-existing tests serve as the regression gate per DEC-22 §refactor-clause Q-1b discipline. Memory `feedback_dec22_refactor_phase_first` constraint satisfied: §refactor-clause applies because the code IS Q-1a-authored.

**HOW vs WHAT.** Per Bug-Triage agent rule, Discovery defines WHAT the fix achieves — not HOW. The exact `git mv` invocation order, the IT-annotation rewrite mechanics, the Svelte-edit sequence, and the `gh pr close` command's exact comment-text are HOW decisions Delivery makes. The ACs constrain observable outcomes (file paths, URL routing, test GREEN, PR state).

**Why atomic cutover (no transitional `/api/tournament-rules` redirect).** E22S08's original Discovery decision (D-2 Option B) was atomic — backend rename + frontend update in same commit, no transient 404, no compatibility shim. This Story preserves that decision. Argument for keeping atomicity: zero external API consumers (this is a self-hosted multi-tenant TM, not a public API per DEC-15); the URL is consumed only by the bundled Svelte frontend; a redirect would add complexity without value. Argument against (theoretical): if external curl/Postman scripts in operator runbooks reference `/api/tournament-rules`, they break post-merge. **No operator runbook reference exists** (verifiable: `grep -rn "tournament-rules" vvwt-prj/` outside the source tree returns no documentation hits).

**PR #90 closure rationale.** PR #90 is closed by THIS Story's Delivery (AC-CLOSE-PR-90) rather than by F-8 because:
- (a) PR #90's full scope is now empirically absorbed by E21S19 + E21S20. Leaving it open after both absorbing Stories merge would be governance noise.
- (b) The F-8 thread investigates the PROCESS anomaly (why `done` ↔ `OPEN`) — not the PR's content disposition. The two concerns are orthogonal and can be cleanly separated.
- (c) Closing now avoids a future operator/Discovery rediscovering "PR #90 still open — should I merge it?" and reaching the same conflict-on-merge dead end.

**Estimated LOC.** ~80–150 across ~10 files: 3 file moves for DeviceAdmin (controller + IT + slice test); 4 file renames for TournamentRules → ScoringRules (controller + IT + slice test + class+URL rewrites inside each); 3 frontend URL string edits; ~10 LOC of mechanical IT-annotation switches per DEC-44; ~5 LOC of Javadoc cleanup on DeviceAdminController (deleting outdated legacy refs).

**Related DECs.** DEC-2 (frontend stack), DEC-13 (staging branch), DEC-21 (Modulith verify), DEC-22 (§refactor-clause Q-1b on Q-1a-authored code), DEC-27 (impl/qa-report on disk), DEC-29 / DEC-30 (compiler-hygiene + Spotless), DEC-40 (controller location + DDD-naming rationale for Scoring), DEC-44 (web-module IT canon).

**validate-artefacts:** PENDING (per Alt-1 light-governance pattern: format gate may be invoked by Discovery before backlog registration; reviewer Tier-2 NOT re-invoked; Q-1a authorship audit + Modulith allowedDependencies analysis substitute as governance evidence).
**review[tier-2]:** NOT INVOKED per user Alt-1 direction (lighter governance, faster path; the Q-1a audit is the substantive substantive-quality check; pre-existing tests are the regression gate).
