---
type: artefact
artefact_type: qa-report
id: E03S09
story: E03S09
verdict: PASS
skills_invoked: [qa-review]
created_at: 2026-04-12
---

# QA Report — E03S09: MatchGenerator Strategy interface + RoundRobinMatchGenerator

## Verdict: PASS

## Story Compliance Check

| AC | Criterion | Status | Verification |
|----|-----------|--------|--------------|
| AC1 | `MatchGenerator` interface with `generate(Phase, List<TeamAvatar>)` + `getBeanId()` | PASS | `MatchGenerator.java` — correct package `de.vvwt.tm.domain.generator`, correct signatures |
| AC2 | `@Component("roundRobin")` creates matches with all required fields; `N*(N-1)/2` count | PASS | Implementation verified; unit test `matchFieldsAreCorrect` + parameterized counts |
| AC3 | Odd N: phantom bye technique; N=9→36 matches; no phantom in output | PASS | `PHANTOM_ID = new UUID(0L,0L)`; dropped from pairs; test `oddTeamCountN9` |
| AC4 | `set_limit = Tournament.match_format.maxSets` | PASS | `MatchFormat.fromPersistedName(tournament.getMatchFormat()).getMaxSets()` |
| AC5 | Deterministic ordering for same input | PASS | Circle method is purely algorithmic; `legacyParityN9` verifies double-call same output |
| AC6 | `MatchGeneratorRegistry` injects all `MatchGenerator` beans; `get()` throws on unknown | PASS | `MatchGeneratorRegistry.java` — `IllegalArgumentException` with known IDs on miss |
| AC7 | `TournamentRuleResolver.resolveMatchGenerator(Tournament)` | PASS | Method exists; null/blank ID guard; delegates to registry |
| AC8 | Unit tests N=4,6,8,12 with count and every-pair assertions | PASS | `@ParameterizedTest` with `@CsvSource({"4,6","6,15","8,28","12,66"})` + `assertEveryPairExactlyOnce` |
| AC9 | Unit test N=9: 36 matches, no phantom, every pair (i<j) exactly once | PASS | `oddTeamCountN9` — all three assertions |
| AC10 | Legacy parity for N=9: deterministic output matches circle method | PASS | `legacyParityN9`: 36 pairs, same order on double call, all pairs covered |
| AC11 | Integration test: Tournament, Phase, 9 Teams, 9 TeamAvatars; generate + persist; assert 36 rows with correct fields | PASS | `E03S09MatchGeneratorIT.endToEndN9` — all field assertions: tenantId, tournamentId, phaseId, state=0, setLimit=3, lapNumber=null, fieldNumber=null |
| AC12 | <2 avatars → empty list | PASS | `zeroAvatarsReturnsEmpty`, `oneAvatarReturnsEmpty` unit tests + IT `oneAvatarProducesNoMatches` |
| AC13 | null phase, null list, null entry, duplicate IDs → `IllegalArgumentException` | PASS | Four unit tests, each asserting correct message fragment |
| AC14 | Unknown generator ID → `IllegalArgumentException` with message | PASS | IT `unknownGeneratorIdFails` — message contains "unknownGenerator" and "roundRobin" |
| AC15 | INFO: count/beanId/avatarCount; DEBUG: each pairing | PASS | `log.info` and `log.debug` present in correct locations |
| AC16 | Pure function — accepts ID-bearing avatars, does not mutate inputs, returns new list | PASS | Only reads `.getId()` from avatars; never calls setters; returns `Collections.unmodifiableList` |

## Scope Integrity Check

Files modified:
- 3 new production files in `de.vvwt.tm.domain.generator` — in scope
- 1 modified production file `TournamentRuleResolver.java` — explicitly required by AC7
- 2 new test files — in scope
- 1 modified test file `ScoringRuleRegistryTest.java` — necessary fix for new 3-arg constructor (not a scope change, the constructor change is required by AC7)

No out-of-scope files modified. No feature creep detected.

## Rule Enforcement

- DEC-1 (Java): all code is Java ✓
- DEC-5/DEC-17 (tenant scope): `tenantId` from `phase.getTenantId()` on every generated match ✓
- DEC-9 (TeamAvatar is pairing subject): generator accepts `List<TeamAvatar>`, references `avatar.getId()`, not team.getId() ✓
- DEC-3 (no proprietary): no new dependencies introduced ✓
- DEC-7 (rewrite): circle method is a clean Java implementation, not copy-paste of legacy ✓

## Regression Scan

- All 159 pre-existing unit tests pass with the constructor change to `TournamentRuleResolver`
- All 96 pre-existing integration tests pass (E03S05, TournamentRuleResolverIT, JlinkSmokeIT, migration ITs)
- 7 new IT tests added for E03S09 — all pass
- Total: 262 tests, 0 failures, 0 errors

## Quality Checks

- Error handling: null phase, null list, null entries, duplicate IDs, unknown tournament ID — all explicitly guarded ✓
- Input validation: all external inputs validated before execution ✓
- Identifiers: descriptive throughout (`PHANTOM_ID`, `generatePairs`, `setLimit`, `tournamentRepository`) ✓
- Single responsibility: `MatchGenerator` (interface), `RoundRobinMatchGenerator` (algorithm + entity construction), `MatchGeneratorRegistry` (lookup), `TournamentRuleResolver` (resolver facade) — each has one responsibility ✓
- No dead code or unreachable branches ✓
- No skipped tests ✓

## Notes

- AC10 "legacy parity" is verified via algorithm determinism (same input → same output) and full pair-set coverage rather than a hardcoded fixture. The note in the story says "V1 must produce the same pair set" — this is satisfied since the circle method is the same algorithm as `MatchGenerator4roundrobin`. A future test enhancement could add a hardcoded 36-pair fixture once legacy binary output is captured.
- `createdAt` is set to `LocalDateTime.now()` in the generator (rather than null) because Spring Data JDBC sends explicit null values to H2, bypassing the DB column DEFAULT. This is consistent with the pattern in E03S05 IT helpers.
