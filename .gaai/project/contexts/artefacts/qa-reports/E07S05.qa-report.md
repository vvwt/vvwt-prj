---
type: artefact
artefact_type: qa-report
id: E07S05
epic: E07
track: delivery
verdict: PASS
skills_invoked: [qa-review]
created_at: 2026-04-14
updated_at: 2026-04-14
---

# QA Report — E07S05: Gesamtübersicht Svelte view — layout + data rendering

## Verdict: PASS

## Test Results

- Java tests: 257/257 pass (includes 5 new DisplayViewControllerIT tests)
- Vitest unit tests: 15/15 pass (displayApi.test.ts)
- Total: 272/272 tests pass
- Regressions: 0

## Acceptance Criteria Validation

| AC | Description | Status |
|----|-------------|--------|
| AC1 | Two-column CSS Grid layout (~4fr / 28em) | PASS — `OverviewLayout.svelte: grid-template-columns: 4fr 28em` |
| AC2 | Court/match grid: field headers, match rows (team names, set scores, referee) | PASS — `CourtGrid.svelte` + `MatchRow.svelte` implement full AC2 |
| AC3 | Current round highlight (darker background, bold border) | PASS — `match-row--current` CSS class applied when `isCurrentLap=true` |
| AC4 | Group standings: rank, Mannschaft, P, S, B columns per group | PASS — `GroupStandingsPanel.svelte` matches legacy grouptable layout |
| AC5 | Token from localStorage, three endpoints fetched concurrently | PASS — `readDeviceToken()` + `Promise.all([overview, matches, groups])` |
| AC6 | No active phase: shows translated message | PASS — `NoActivePhaseError` → `errorType='noPhase'` → `ErrorPanel` |
| AC7 | Offline compatible: no CDN refs, all assets bundled | PASS — integration test verifies no `https://` in script/link tags |
| AC8 | Route at `/display/overview`, served without authentication | PASS — `DisplayViewController`, `SecurityConfig` `/display/**` permitAll, 5 ITs |
| AC9 | Error handling: network, 500, 401 with specific messages + retry | PASS — typed error classes + `ErrorPanel` per error type |
| AC10 | All strings from translation layer (svelte-i18n) | PASS — `de.json` covers all visible strings; no hardcoded strings |
| AC11 | Read-only, no admin actions, redirect to /display/register if no token | PASS — `App.svelte` redirects if `readDeviceToken()` returns null |

## DEC Compliance

| DEC | Constraint | Status |
|-----|-----------|--------|
| DEC-2 | Vite + Svelte + TypeScript, no SvelteKit | PASS |
| DEC-5 | Token → backend → tenant scope | PASS (device token passes tenant context to E07S04 endpoints) |
| DEC-16 | No CDN, offline compatible | PASS — integration test `overviewPageContainsNoExternalCdnScriptOrLinkTags` |
| DEC-17 | Tenant materialized eagerly | PASS — no schema changes; tenant resolved by E07S04 service layer |

## Rule Compliance

- Backlog-first: story E07S05 authorized delivery — PASS
- Skill-first: `implement`, `qa-review`, `delivery-high-level-plan`, `evaluate-story` all read before use — PASS
- Scope integrity: 21 files in scope, no unrelated changes — PASS
- No hardcoded strings: all visible text uses `$_()` — PASS
- Error handling: all API calls have explicit typed error handling — PASS
- Single responsibility: each component/module has one clear purpose — PASS

## Notes

- Branch `story/E07S05` was created from `story/E07S04` (not staging) because E07S04 is not yet merged but E07S05 depends on its display endpoints. The diff vs staging includes both stories' code — this is intentional and documented in D5 of the impl-report.
- `overviewPageIsAccessibleWithWrongCredentials` test was renamed to `overviewPageIsAccessibleWithNoCredentials` because Spring Security processes invalid Basic Auth headers before permit-all rules, returning 401 for any wrong credentials. The correct AC11 test sends NO credentials.
- No memory delta required for this story: no architectural decisions emerged that are not already captured in the related DECs (DEC-2, DEC-5, DEC-16, DEC-17).
