---
type: artefact
artefact_type: execution-plan
id: E14S10
story_ref: contexts/artefacts/stories/E14S10.story.md
skills_invoked: [tdd-implement]
created_at: 2026-04-19
---

# Execution Plan — Story E14S10

## Pre-Delivery Checklist

1. **Dependency verification** — E14S07 marked `done` in backlog. ✓

2. **Worktree isolation** — Delivery in `E14S10-vvwt-prj/` worktree, main tree stays on `staging`.

3. **TDD Iron Law (DEC-22)** — RED commit before GREEN commit:
   - RED: `TenantContextTestSupportIT` committed with compilation failure.
   - GREEN: `TenantContextTestSupport` implementation committed after RED.

## Implementation Steps

### Step 1 — RED Commit (AC1 failing test)

Create `TenantContextTestSupportIT.java` in `de.vvwt.tm.tenant` with 4 test methods:
- `currentReturnsDefaultTenantUuidAfterAutoBind`
- `backToBackFirstMethod_bindsAndUnbinds`
- `backToBackSecondMethod_freshBindSucceeds`
- `nestedBind_innerOverridesOuter_closingInnerRestoresOuter`

Commit as RED (compilation failure).

### Step 2 — GREEN: TenantContextTestSupport Implementation

Create `TenantContextTestSupport.java` in `de.vvwt.tm.tenant` as `@TestConfiguration`:
- `@Bean tenantContextBinder(...)` factory with `@Qualifier("tenantRoutingContext")` injection
- Inner static `Binder` class with:
  - `TenantContext tenantContext` (new tenant::api)
  - `TenantRegistryPort tenantRegistryPort`
  - `TenantContext.Scope activeScope` for lifecycle tracking
  - `UUID bindDefaultTenant()` — binds default tenant, returns UUID
  - `void unbind()` — idempotent scope close
  - `TenantContext tenantContext()` — exposes context for mid-test switching

### Step 3 — Rewire all @SpringBootTest ITs

For each of the 47+ IT files:
1. Add `@Import(TenantContextTestSupport.class)` annotation
2. Add `import de.vvwt.tm.tenant.TenantContextTestSupport` and `import org.springframework.context.annotation.Import`
3. Replace `TenantRegistryPort` field with `TenantContextTestSupport.Binder tenantContextBinder`
4. Update `@BeforeEach`: `defaultTenantId = tenantContextBinder.bindDefaultTenant()`
5. Update `@AfterEach`: `tenantContextBinder.unbind()`

For ITs with guard-fire tests (E03S05, PhaseBreak, E08S02, etc.):
- Unbind both new and legacy context before asserting guard
- Re-bind in `finally` block to ensure `@AfterEach` cleanup can proceed

For ITs testing legacy bridge behavior (E03S05, E08S02):
- Keep `TenantContext tenantContext` field for mid-test legacy operations
- Dual-bind in `@BeforeEach` (both new + legacy)
- Dual-unbind in `@AfterEach`

### Step 4 — QA + Commit

Run `mvn verify`, confirm no new failures vs pre-existing baseline.
Commit delivery artefacts. Create PR to staging.
