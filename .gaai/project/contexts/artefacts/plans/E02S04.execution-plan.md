---
type: artefact
artefact_type: execution-plan
id: E02S04
story_id: E02S04
tier: 2
skills_invoked: [evaluate-story, compose-team, deliver-high-level-plan]
created_at: 2026-04-12
---

# Execution Plan — E02S04: Default-tenant bootstrap-time UUID generation

## Tier
2 — Core Team (13 ACs, no specialist triggers)

## DEC Constraints Verified
- DEC-3: UUID.randomUUID() only (Java SE, no proprietary)
- DEC-5: Tenant + default location created atomically in same transaction
- DEC-14: JdbcTemplate for DB access (no JPA auto-schema)
- DEC-17: UUID generated at first startup, persisted in DB, not hardcoded
- DEC-18: TM does not implement federation logic — uniqueness is sufficient

## Step 1 — DefaultTenantProvider interface
- File: `vvwt-tm-web/src/main/java/de/vvwt/tm/tenant/DefaultTenantProvider.java`
- Interface with `UUID getDefaultTenantId()`
- Javadoc: fail-fast contract (IllegalStateException before bootstrap)

## Step 2 — DefaultTenantBootstrap component
- File: `vvwt-tm-web/src/main/java/de/vvwt/tm/tenant/DefaultTenantBootstrap.java`
- @Component @Order(1) implements ApplicationRunner + DefaultTenantProvider
- AtomicReference<UUID> for resolved tenant ID
- run(): query → branch on size (0=insert, 1=resolve, >1=abort)
- insertDefaultTenantWithLocation(): @Transactional, UUID.randomUUID() ×2, INSERT tenant + location, catch DataIntegrityViolationException for race, catch Exception for unexpected failures
- getDefaultTenantId(): fail-fast if resolvedTenantId is null

## Step 3 — Integration tests
- File: `vvwt-tm-web/src/test/java/de/vvwt/tm/tenant/DefaultTenantBootstrapIT.java`
- @SpringBootTest(webEnvironment=NONE) @ActiveProfiles("test") @DirtiesContext(AFTER_EACH)
- 5 tests: firstStart, secondStart, twoInstances, concurrentRace, inconsistentState

## Step 4 — Update TournamentManagerApplicationIT
- File: `vvwt-tm-web/src/test/java/de/vvwt/tm/TournamentManagerApplicationIT.java`
- `duplicateDefaultTenantIsRejected`: remove first insert (bootstrap already occupies slot),
  test directly that a duplicate insert fails with DataIntegrityViolationException
