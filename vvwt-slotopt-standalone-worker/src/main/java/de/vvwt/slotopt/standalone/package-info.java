/**
 * Public API surface for the vvwt-slotopt-standalone-worker module.
 *
 * <p>Public-API interfaces and the main entry point live here per DEC-35-by-analogy (E35S04
 * non-Spring Modulith precedent). Implementations reside in {@code
 * de.vvwt.slotopt.standalone.internal}.
 *
 * @see de.vvwt.slotopt.standalone.internal
 */
package de.vvwt.slotopt.standalone;
