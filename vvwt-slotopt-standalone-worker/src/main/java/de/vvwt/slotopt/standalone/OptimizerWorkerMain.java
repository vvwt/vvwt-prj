package de.vvwt.slotopt.standalone;

/**
 * Entry point for the vvwt-slotopt-standalone-worker process.
 *
 * <p>Bare stub — E41S01 skeleton. The Picocli {@code @Command} annotation, CLI argument schema
 * (WorkerConfig + WorkerConfigLoader), and main logic are added by E41S02.
 *
 * <p>This class exists at E41S01 to satisfy the context-load test ({@link
 * OptimizerWorkerMainContextLoadTest}) per AC-FIRST-CONTEXT-LOAD-TEST.
 */
public class OptimizerWorkerMain {}
