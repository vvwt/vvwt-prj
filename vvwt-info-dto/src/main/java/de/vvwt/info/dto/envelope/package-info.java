/**
 * Envelope package — {@link de.vvwt.info.dto.envelope.Envelope} wraps every cross-subsystem payload
 * with a {@code schemaVersion} field (AC10 / AC5, Story E38S02).
 */
package de.vvwt.info.dto.envelope;
